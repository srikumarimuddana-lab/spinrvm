# Integration Matrix — every third-party integration (Tier B matrix, part 1/2)

**Lane:** ad hoc Tier-B matrix build (this session) · **Written:** 2026-09-25 · **Report-only, no code/config/data changes.**
Evidence labels: VERIFIED (code read this session) / VERIFIED-LIVE (production Supabase, read-only, this session or `10-live-checks.md`) / INFERRED / ASSUMED / UNKNOWN, per the master audit convention.

## §0 Method

1. Read `docs/audit/clean-sheet-prompt/greenfield-extensions.md` §2 (Tier B list), `W0-SUMMARY.md`, and the four
   lane reports named in the brief (`02-findings/integrations.md` [R15], `security.md` [R10], `dispatch.md`,
   `reliability.md`) in full or by targeted grep before writing anything here, per the task's "cite existing
   finding IDs instead of re-deriving" instruction.
2. **This file does not re-run the vendor-client code reads R15's `integrations.md` already did** (timeout/retry/
   circuit-breaker/failure-path reads of `sms_service.py`, `stripe_config.py`, `email_provider.py`, `maps_eta.py`,
   `zoho_desk_service.py`, `maps_budget.py`, `ai/providers/*`, `apns_client.py`, `firebase_admin` SDK internals,
   `repositories/_base.py`'s circuit breaker). It restructures that lane's §3 vendor matrix and §2 finding cards
   into the exact column set this task's brief asks for (auth/secret source name, timeout/retry y/n, failure
   mode, PII class, region, DPA status, owning finding id), adds two integrations that lane's matrix did not
   carry as their own rows (Redis pub/sub as a dispatch-fanout dependency — `reliability.md` REL-005; the
   Supabase-project-level facts from `10-live-checks.md`, published 2026-09-25, after R15's pass), and reconciles
   one contradiction between `integrations.md`'s vendor matrix (Sentry "assumed US, not independently
   re-verified") and this session's own grep of the Sentry SDK init call.
3. Where a fact changed between R15's write (2026-09-24) and this session (2026-09-25), the newer live evidence
   wins and is marked accordingly — see the "Updates since R15" callouts.
4. Code entry points below are cited at file:line granularity **only where R15/R10 already verified the exact
   line**; where a lane cited a file without a line number (most of §3's matrix), this file keeps that
   granularity rather than re-opening every client to add line numbers neither lane's own charter required —
   flagged per row as "(file-level only)".
5. Absence claims ("no timeout", "no fallback") are R15's own, arrived at by grepping each vendor client for
   `timeout=`, `retry`, `except`, `Circuit` per that lane's §0.4 — not re-run here; see `integrations.md` §0 for
   its own search-spelling discipline.

## §1 Vendor integration table

Legend: TO=explicit timeout set in code, RT=retry/backoff present, CB=circuit/failure-rate breaker, FB=fallback
provider/path. Secret source names only (never values) — all vendor keys route through the `app_settings`
Supabase table via `backend/settings_loader.py` (60s TTL cache) unless noted otherwise.

| Vendor | Purpose | Code entry point(s) | Auth/secret source (name only) | TO | RT | CB | Failure mode | Data sent (PII class, CLAUDE.md scale) | Region/residency | DPA/vendor-register status | Owning finding ID(s) |
|---|---|---|---|:-:|:-:|:-:|---|---|---|---|---|
| **Supabase (Postgres)** | Sole durable store: rides, users, payments mirror, wallets, driver docs metadata | `backend/repositories/_base.py:70-155` (breaker), `db_supabase.py` (66 helper re-exports) | `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (env, not `app_settings` — pre-dates that pattern) | ✅ | ✅ (H2 GOAWAY, 1×) | ✅ best-built breaker in the codebase (half-open, single-probe recovery) | Fails fast (503) after 5 failures/30s rather than hanging | C1-C4 (everything: names, rides, GPS, payment mirror, driver license #s) | **VERIFIED-LIVE `ca-central-1`, Postgres 17.6, ACTIVE_HEALTHY** (`10-live-checks.md` §1, re-confirms `integrations.md`) | `dpa-register.md:28` marks it "⚠ VERIFY" despite the region being known — INT-003-adjacent doc-drift | SEC-R10-001 (key rotation unknown), SEC-R10-008/LIVE-002/LIVE-003 (search_path/SECURITY DEFINER), LIVE-001 (migration tracking gap) |
| **Supabase Storage** | Driver docs, profile images | `routes/users.py:631` (compression only found) | same service-role key | UNKNOWN | UNKNOWN | none found | Document upload blocked on outage; cached docs may still serve (unverified) | C3-C4 (docs, license #s) | Assumed same project region — not independently confirmed | UNKNOWN | — |
| **Stripe** | Charges, Connect payouts (incl. **instant payout — VERIFIED-LIVE on in all 6 service areas**, `10-live-checks.md` §1), webhooks | `utils/stripe_config.py` (30s timeout, 2 retries, API version pinned), `routes/webhooks.py` (`claim_stripe_event` idempotency), `utils/payment_retry.py` | `stripe_secret_key`/`stripe_webhook_secret` via `app_settings` | ✅ 30s | ✅ SDK 2× + retry loop | none integration-layer (idempotency table instead) | New charges/payouts fail per-call; webhook backlog queued via `claim_stripe_event` + Stripe's own retry schedule | C3-C4 (no PAN at Spinr) | US | ✅ standard DPA (auto-accept) | money-cra.md territory (not re-derived); `integrations.md` §3 |
| **Twilio** | OTP SMS | `backend/sms_service.py:42` (`Client(twilio_sid, twilio_token)`, **no `http_client=` arg**) | `twilio_sid`/`twilio_token` via `app_settings` | **❌ INT-001** | ❌ (caller decides) | none | **Hangs, does not fail**, on a slow-not-down Twilio incident — worse than a fast failure; runs on the shared default `asyncio.to_thread` executor pool, so a burst can starve unrelated `to_thread` callers too (SES) | C3 (phone number) | US/global | ⚠ VERIFY (`dpa-register.md:31`) | **INT-001 (CRITICAL, unfixed)** |
| **Firebase Auth** | Phone-verification identity checks (NOT primary session auth — that's the custom JWT scheme) | `backend/utils/firebase_identity.py` | Firebase service-account JSON (`FIREBASE_SERVICE_ACCOUNT_JSON`) | n/a | n/a | n/a | Low direct impact | C3 | US | ✅ Google Cloud DPA | `integrations.md` §3; security.md §1 RLS note |
| **FCM (push)** | Rider/driver push, dispatch offers. **`minimal_fcm_offer_payload_enabled` is VERIFIED-LIVE `false`** (`10-live-checks.md` §1) — offer pushes still carry full trip coordinates, not a minimized payload | `backend/features.py:1494-1520` (`_FCM_BATCH_SIZE=500`, `send_each`) | Firebase service-account JSON | ✅ SDK 120s | ✅ SDK 4× on 500/503 | none needed (SDK retry + `push_retry.py` loop) | Backgrounded drivers miss offer pushes; `push_retry` 30s cadence **exceeds** the ~15s dispatch-offer window (see §4 scenario) | C2 (token, ride status — **not** GPS/PII per CLAUDE.md, but see the minimal-payload gap above, which is a payload-content finding, not a transport one) | US | ✅ Google Cloud DPA | `integrations.md` §3/§4; cross-ref SKB-002 (per coordinator's new live fact — not this session's own finding, cited from `10-live-checks.md`) |
| **APNs** | iOS push transport | `backend/utils/apns_client.py:167` `http2=True, timeout=10.0` | APNs auth key (`app_settings`) | ✅ 10s | UNKNOWN | none found | Same class as FCM row | C2 | US | Apple Developer DPA | `integrations.md` §3 |
| **Google Maps Platform** | Address search, ETA, static maps, geocode | `routes/maps_proxy.py`, `utils/{maps_eta,route_distance,google_places_new,route_snapshot}.py` | `google_maps_api_key` via `app_settings` | ✅ all call sites | ❌ single attempt → 502 | ✅ cost-only breaker (`maps_budget.py`); ❌ no failure-rate breaker | ETA degrades gracefully (OSRM→Google→haversine, 3-tier — the one client that does this well); Places/Autocomplete/Geocode/Static have **no fallback**, so a Google 5xx incident is a hard "can't search a destination" outage, slow-failing rather than fast-failing | C3 (addresses queried) | US | ✅ Google Cloud DPA | **INT-004** (no failure-rate breaker); the accepted 3.5s fare-estimate SLA exception (CLAUDE.md Performance SLAs) is a separate, already-decided trade-off, not this row's finding |
| **OSRM (self-hosted)** | Route matching, ETA primary | `deploy/osrm/`, `utils/route_distance.py`, `maps_eta.py` | n/a (self-hosted) | per `_TIMEOUT_S` | ✅ 1-shot bounded retry per chunk | none (own infra) | Falls through to Google/haversine — no user-facing break given the fallback chain | C2 (coordinates only) | Same infra as backend (Fly `yyz`/Railway) | n/a (self-hosted, not a DPA-scope sub-processor) | `integrations.md` §3 |
| **Zoho Desk** | Admin support tickets, SOS-linked ticket | `services/zoho_desk_service.py` (15s TO, 1× retry on 401), `zoho_desk_integration.py:79-82` `_link_ticket` | Zoho OAuth creds via `app_settings` | ✅ 15s | ✅ 1× on 401 | none | Admin can't view tickets (no local mirror by design); **SOS-linked ticket creation silently drops on a Zoho outage** — `logger.warning` not `.error`, no Sentry, no retry queue | C3 (name, email, phone) | Canada DC by default (`data_center="ca"`, VERIFIED — good) | ⚠ **not in `dpa-register.md` at all** | **INT-002 (HIGH, unfixed)** — cross-ref `trust-safety-fraud.md` TSF-001 (dark paging webhook; INT-002 is the Zoho-ticket channel specifically, filed separately to avoid duplication) |
| **Sentry** | Error tracking, crash reports | `backend/utils/sentry_runtime.py`, `server.py:701-702` | `SENTRY_DSN` (env, pre-`app_settings`) | SDK-internal, non-blocking | SDK-internal | n/a | App continues without error reporting on a Sentry outage — silent *observability* gap, not a *service* gap | C2/C3 (stack traces, user_id; PII scrubbed via `utils/pii.py`/`sentry_scrub.py` `before_send` hooks — VERIFIED, `security.md` §1.6) | US (assumed by `integrations.md`; **not independently re-verified by either lane** — this session's own grep of `sentry_runtime.py` found no explicit `region=`/DC selector in the SDK init call, consistent with Sentry's SaaS default US ingestion, but this is still INFERRED not VERIFIED against a fetched vendor page) | ✅ per `dpa-register.md:39`, "if in use" caveat | `integrations.md` §3 |
| **Meta (SDK + Conversions API)** | Install attribution, per-ride `Purchase` events | `utils/meta_capi.py:256,349`, `services/meta_conversions_service.py` | `meta_access_token`/`meta_dataset_id_rider` via `app_settings` | ✅ (`_REQUEST_TIMEOUT_SECONDS`) | UNKNOWN | none found | Attribution data loss only; invisible to users by design | C3-C4 (hashed email/phone, ride_id, fare value) | US | ❌ **not in `dpa-register.md` or subprocessor list** (`strategy.md` STRAT-004) | **Update since R15 (VERIFIED-LIVE 2026-09-25, `10-live-checks.md` §1): both the Meta access token AND the rider dataset id are set in `settings` — `meta_capi.py` sends whenever both are present, so STRAT-004 moves from "configured" to "live" (VERIFIED config; INFERRED that sends are actually occurring — no send log was read, per that doc's own §5).** This raises STRAT-004's real-world urgency: an undisclosed sub-processor that R15/strategy.md flagged as a compliance gap **is currently transmitting data**, not sitting dormant. |
| **Anthropic / OpenAI / Gemini / OpenRouter** | AI assistant (support, booking help), embeddings | `backend/ai/providers/*`, `ai/embeddings.py` | `ai_api_key_{provider}` via `app_settings` (`backend/ai/providers/__init__.py:62` — **same `settings` row as Stripe/Twilio/Maps**, SEC-R10-001) | ❌ **INT-005** | UNKNOWN | none | Assistant chat hangs/errors on a slow-not-down provider; rest of app fully unaffected (AI not on critical path) | C2-C3 (user chat text; scrubbed on ingest) | US (all four) | ✅ Google (Gemini) only; ❌ Anthropic/OpenAI **not in `dpa-register.md`** | **INT-005 (MEDIUM, unfixed)** |
| **LogRocket** | Session replay (iOS default-on) | `rider-app/app/_layout.tsx:246-250,388,543`, `driver-app/app/_layout.tsx:207-211,441,611` | LogRocket project id (build-time env) | n/a (client SDK) | n/a | n/a | n/a — the live issue is unmasked PII capture, not availability | C3-C4 (unmasked OTP screen, saved addresses, map pins, earnings — no `textSanitizer`/`inputSanitizer` set) | US | ❌ **not in `dpa-register.md`** | **SEC-R10-003 (HIGH, unfixed)** — every iOS session recorded and shipped to a US vendor, keyed to user id, unmasked, undisclosed |
| **PostHog** | Session replay (dark-launched, masked) | `shared/services/posthogReplay.ts:163-164` (`maskAllTextInputs: true, maskAllImages: true`) | PostHog project key | n/a | n/a | n/a | n/a | C3 (masked) | US (assumed) | ❌ not in `dpa-register.md` | SEC-R10-003 (cited as the "team knows the control exists" contrast to LogRocket) |
| **AWS SES / Resend** | Transactional email (receipts, password resets, dispute notices) | `utils/email_provider.py:83-85` (SES `_DEFAULT_SES_REGION="ca-central-1"`), `:444` (Resend `_try_resend`), `utils/outbox_worker.py` (retry/dead-letter) | SES via AWS creds (`app_settings`/env); Resend API key via `app_settings` | ✅ 10s (Resend leg) | ✅ two-provider fallback + outbox retry | n/a (fallback chain serves the purpose) | Genuinely resilient — only breaks if **both** SES and Resend are down simultaneously | C3 (email address; receipt $ amounts only) | SES: **`ca-central-1`** (pinned, VERIFIED, PIPEDA-motivated comment in code); Resend: **UNKNOWN** — no region/DC selector in the client | ✅ on contract (per `vendor-inventory.md`) | **INT-007 (MEDIUM, unfixed)** — the SES guardrail's residency intent was never extended to its own fallback |
| **Cloudinary** | Nominally image management | `backend/utils/cloudinary.py` (full `CloudinaryService` class) | `cloudinary_url`/API key (never read — dead code) | n/a — dead code | n/a | n/a | **Nothing — zero call sites anywhere in `routes/`/`services/`** (grep, VERIFIED) | n/a (no live call site) | n/a | ❌ not disclosed anywhere (moot — unused) | **INT-006 (LOW, dead-vendor hygiene)** |
| **Expo / EAS** | Mobile build pipeline, OTA | `[build]` commit-message gate, `app.config.ts` `runtimeVersion` fencing | Expo access token (CI secret) | n/a (build-time) | n/a | n/a | No production runtime impact | C2 (source, build artifacts) | US | ⚠ VERIFY (`dpa-register.md:34`) | `integrations.md` §3 |
| **Vercel** | Admin dashboard hosting | auto-deploy from `main` | Vercel project token (CI secret) | n/a | n/a | n/a | Admin dashboard down; rider/driver apps and backend API fully unaffected (separate deploy target) | C2 (admin session only) | **Internal docs disagree**: `subprocessor-list.md` says Canada (`yyz1`); `vendor-inventory.md`/`dpa-register.md` both say US | ✅ standard DPA | `integrations.md` §3 (INT-003-adjacent mini-finding, folded there) |
| **Fly.io** | Backend primary (Toronto) | `backend/fly.toml` | Fly API token (CI secret) | n/a (infra) | n/a | n/a | Railway standby + Cloudflare DNS failover, verified healthy 2026-09-24 per CLAUDE.md; the failover mechanism itself has never been drilled under live traffic (C1, open) | all app data transits it | **Canada (`yyz`)** | ⚠ VERIFY — **no DPA row at all** (only "Railway" is listed) | **INT-003 (HIGH, unfixed)** — `vendor-inventory.md` omits Fly.io entirely and lists a phantom "Render" vendor that appears nowhere in deploy config |
| **Railway** | Backend standby (US) | `railway.json` | Railway project token (CI secret) | n/a | n/a | n/a | Standby only — zero impact if Railway itself is down while Fly is healthy | all app data transits it if failed over | US | ⚠ VERIFY | INT-003 (mislabeled as primary in `vendor-inventory.md` — the opposite of the real Fly-primary/Railway-standby topology) |
| **Cloudflare** | DNS + CDN, `api-spinr.spinr.ca` | Cloudflare-proxied CNAME | Cloudflare API token (CI secret) | n/a | n/a | n/a — **this is the failover mechanism itself** | DNS layer down = total outage regardless of Fly/Railway health; not otherwise mitigated | request metadata only | Global edge | ✅ (per `vendor-inventory.md`) | `integrations.md` §3 |
| **Redis** | Cache, pub/sub, rate-limit, OTP lockout, **WS cross-replica fan-out** | `utils/redis_client.py`; `utils/ws_pubsub.py` (`spinr:ws:dispatch` channel, per CLAUDE.md's WS topology) | `REDIS_URL`/`RATE_LIMIT_REDIS_URL`/`WS_REDIS_URL` (env) | n/a | n/a | n/a | **In-process dict fallback on `REDIS_URL` unset** — rate limits/OTP lockouts reset, and **cross-replica WS fan-out silently degrades to local-only delivery with no metric distinguishing the two cases** (REL-005, `reliability.md:371`) | C2-C3 (session/OTP hash; WS payloads relayed, not stored) | Provider/plan UNKNOWN | ⚠ VERIFY (`dpa-register.md:38`) | `integrations.md` §3 + **REL-005** (`reliability.md`, cited here as the WS-fanout-specific consequence this lane's matrix didn't carry as its own row) |
| **Supabase pgsodium / supabase_vault** | Column-level encryption for the driver licence-number column, `drivers.sin` (write-only), `emergency_contacts.*` | migrations 32/137/138/244/289/357 | Vault master key (Supabase-managed) | n/a | n/a | n/a | Vendor-deprecation exposure, not an outage | C3-C4 | **VERIFIED-LIVE: pgsodium 3.1.8, supabase_vault 0.3.1 both installed** (`10-live-checks.md` §1, confirms `security.md` SEC-R10-010) | n/a (DB extension, not a DPA-scope vendor) | SEC-R10-010 (rotation runbook never executed) |
| **pgaudit** | DB-level audit logging of who ran what query | would sit at the Postgres extension layer if enabled | n/a | n/a | n/a | n/a | **VERIFIED-LIVE: NOT installed** (`10-live-checks.md` §1, `list_extensions` shows `installed_version: null`) — means the 2026-07-30 service-role-key exposure incident's "was the key used?" question is structurally unanswerable from the DB side, and stays that way for any future key exposure until this is turned on | n/a | n/a | n/a | **SEC-R10-001** (this is the specific mechanism that finding's recommendation #4 asks to enable — confirmed still off as of 2026-09-25, one day after that finding was written) |

## §2 Updates since R15's `integrations.md` pass (2026-09-24 → this session, 2026-09-25)

1. **Meta CAPI: configured → live.** `10-live-checks.md` confirms both the access token and rider dataset id
   are set, and `meta_capi.py`'s own code only sends when both are present. R15/`strategy.md`'s STRAT-004
   (undisclosed sub-processor) should be re-read as describing an **active** data flow, not a dormant
   configuration risk — the DPA-register gap it names now has a live consequence attached to it, which changes
   nothing about the *fix* but changes the urgency framing.
2. **Supabase region and extension state now VERIFIED-LIVE**, not INFERRED/citation-only: `ca-central-1`
   confirmed; `pgaudit` confirmed **absent** (closes the "was it ever checked?" ambiguity in SEC-R10-001's own
   evidence list — it was checked, twice now, and the answer is still no); `pgsodium`/`supabase_vault` confirmed
   present at the versions SEC-R10-010 already cited.
3. **Minimal FCM payload confirmed off** — `integrations.md`'s FCM row already flagged the payload as carrying
   full trip coordinates by implication (no minimization mentioned); `10-live-checks.md` makes this an explicit,
   dated, VERIFIED-LIVE fact rather than an absence-of-mention.
4. **Instant payout confirmed live in all 6 service areas** — relevant to this file only insofar as it sits on
   the Stripe integration row above; the velocity-cap/fraud-control implications are `trust-safety-fraud.md`/
   `money-cra.md` territory (BENCH-004, ROADMAP N22 per the live-checks doc), not re-derived here.

## §3 What could not be checked (this file's own boundary)

- No vendor documentation page was fetched this session (same egress-proxy block R15 hit, `integrations.md` §0)
  — every vendor pricing/limit/SDK-default claim not independently re-verified against a live page is INFERRED
  or cites R15's own VERIFIED-SDK (installed-package-source) reads, not a fresh fetch.
- Redis provider identity/plan/region: still UNKNOWN (`A3-006`, not re-attempted).
- Whether Meta events are *actually being received* by Meta (vs. just being sent by Spinr's own code) needs
  Meta's Events Manager console — `10-live-checks.md` §5 lists this as its own open item; not re-attempted here.
- Railway/Fly env var contents (`ALERT_WEBHOOK_URL`, `SPINR_PROCESS_ROLE`) — policy forbids Fly access from this
  session; Railway would expose secret values.
- Sentry/Stripe/Twilio/Redis MCP connectors all failed to connect this session (proxy/connection errors) —
  consistent with `10-live-checks.md` §0's "Not touched" list; this file relies entirely on prior lane reads and
  the two live-checks passes rather than a fresh connector query.
