================================================================================
SPINR RIDER APP — PRODUCTION-READINESS AUDIT v1
================================================================================
Date        : 2026-04-19
Branch      : claude/rider-app-audit-iVxpH (audit findings — read-only)
Auditor     : Claude (claude-sonnet-4-6) via automated audit framework
Scope       : rider-app/ · shared/ · backend/routes/ (rider side)
Platform    : Expo 54 · React Native · Zustand · WebSocket · Stripe Connect

AUDIT GROUND RULES
──────────────────
• OTP is 4 digits by design — compensating controls documented, NOT flagged.
• Hard-coded dev values (OTP "1234", test keys) → severity LOW/RECOMMENDATION only.
• Severity scale: CRITICAL | HIGH | MEDIUM | LOW | PASS | RECOMMENDATION
• See audit-framework/ground-rules.md for full list.

PRE-AUDIT CHECKS (baseline)
─────────────────────────────
npm audit (rider-app):    1 CRITICAL vuln — protobufjs (npm audit fix needed) — P1
TypeScript errors:        317 real errors (mostly implicit :any params) — P2
Explicit :any usages:     80 — P2
console.log in prod:      40 — P3
Magic ride-status strings: 78 — P2
Hermes JS engine:         NOT configured on Android — P3

SHARED FIXES CONFIRMED (from driver audit — verified carry-over):
  OfflineBanner insets.top   → PASS
  Auth revoked_at / refresh  → PASS
  OTP hashing on ride create → PASS (backend/routes/rides.py imports hash_otp)
  Decimal monetary math      → PASS

Dimensions run: D01 (Feature Completeness)

================================================================================
FINDINGS
================================================================================

================================================================================
TASK 01 — FEATURE COMPLETENESS (Dimension 01)
================================================================================

[01-1] HIGH | Home Screen SOS Button Shows False "Alert Sent" — No API Call Made
File: rider-app/app/(tabs)/index.tsx:236–252
Finding: The SOS button on the home/map screen is a custom TouchableOpacity whose
  onPress fires only setAlertState() with title "SOS Triggered" and message
  "Emergency services have been alerted. Stay calm and stay where you are."
  No API call is made, no emergency contact is notified, no 911 prompt is shown.
  The message is factually false — nothing has been sent.
  The shared SOSButton component (shared/components/SOSButton.tsx), which calls
  the backend AND prompts the rider to call 911, is used correctly in
  ride-in-progress.tsx but is absent from the home screen.
  A rider who taps SOS outside of an active ride (e.g. after being dropped off in
  an unsafe area, or before their ride starts) receives a false confirmation and
  no real help.
Fix: Replace the home-screen SOS implementation with the SOSButton component from
  @shared/components/SOSButton. Because no rideId is available pre-ride, provide
  a fallback that skips the backend call and opens tel:911 directly, plus shows
  the rider's current GPS coordinates in the prompt.
  Effort: 2 hours.

[01-2] HIGH | PIPEDA Data Export and Account Deletion Are UI Stubs — No API Called
File: rider-app/app/privacy-settings.tsx:104–107 (export), 29–47 (delete)
Finding: "Download My Data" shows a success alert reading "Your data export has
  been requested. You will receive an email with a download link." without making
  any API call. "Delete Account" shows a confirmation flow that resolves to
  "Your account deletion request has been submitted." — again, no API call.
  PIPEDA (Canadian federal privacy law, analogous to GDPR) gives data subjects the
  right to access and delete their personal information. Presenting stub UI that
  falsely confirms these actions is both a legal compliance gap and a trust
  violation.
Fix:
  • Wire "Download My Data" to POST /user/data-export (backend must queue an email
    with a signed download link).
  • Wire "Delete Account" to DELETE /user/account (with server-side 30-day
    deactivation window per PIPEDA breach-notification rules).
  Effort: 4 hours (frontend) + backend endpoint implementation.

[01-3] HIGH | become-driver.tsx Navigates to `/(driver)` After Submit — Route Does Not Exist
File: rider-app/app/become-driver.tsx:257
Finding: After a successful registerDriver() call, the screen calls:
    router.replace('/(driver)' as any)
  The rider app has no (driver) route group. Expo Router will throw an "Unmatched
  Route" error and render a blank screen (or crash in production builds), meaning
  the user successfully completes a 5-step driver registration form and is then
  shown nothing. This is the main CTA of the become-driver flow.
Fix: Replace with router.replace('/(tabs)') and show a modal/alert telling the
  rider: "Application submitted! Download the Spinr Driver app to start driving."
  Include an App Store / Play Store deep link.
  Effort: 1 hour.

[01-4] MEDIUM | Activity Tab Shows Only Ride History — Scheduled (Upcoming) Rides Not Visible
File: rider-app/app/(tabs)/activity.tsx:47
Finding: The activity tab calls GET /rides/history which returns only completed
  and cancelled rides. Scheduled rides are only accessible via Account →
  Scheduled Rides (account.tsx:183). Users naturally look in "Activity" for
  upcoming bookings — there is no "Upcoming" section, no tab, and no banner
  linking to scheduled-rides.tsx. A rider with a scheduled trip tomorrow has no
  obvious way to find it from the Activity tab.
Fix: Add an "Upcoming" filter tab or a sticky banner at the top of the activity
  screen when scheduledRides.length > 0. The store already exposes
  scheduledRides + fetchScheduledRides — no new API work needed.
  Effort: 3 hours.

[01-5] MEDIUM | Corporate Ride Booking Has No UI Entry Point
File: rider-app/store/rideStore.ts:319–352
Finding: createRide() does not include a corporate_account_id field in its
  payload. activity.tsx exposes a "Business" filter (line 151) that can filter
  rides by corporate_account_id — implying corporate rides are expected — but
  there is no booking path that sets this field. The backend schema
  (rides table) supports corporate_account_id. Riders who have a corporate
  account linked cannot book rides against it.
Fix: Add a corporate account selector in ride-options.tsx or payment-confirm.tsx.
  When a corporate card/account is selected, pass corporate_account_id in the
  createRide payload.
  Effort: 1 day (requires corporate account listing endpoint from backend).

[01-6] LOW | rate-ride.tsx Is an Orphaned Screen — No Navigation Entry Point Exists
File: rider-app/app/rate-ride.tsx
Finding: rate-ride.tsx is a fully functional rating + tip screen, registered in
  _layout.tsx (line 370). However, no screen in the rider app calls
  router.push('/rate-ride'). Ride-completed.tsx implements its own inline rating
  section (lines 323–397) and calls rateRide() directly on submit — the standalone
  rate-ride.tsx screen is never reached.
  This is dead code: safe but creates a maintenance burden (two divergent rating
  implementations). The tip options also differ ($2/$5/$10 in ride-completed vs
  $1/$3/$5 in rate-ride).
Fix: Remove rate-ride.tsx and ensure ride-completed.tsx is the single rating
  implementation. Or consolidate: have ride-completed.tsx navigate to rate-ride
  and delete the inline rating section.
  Effort: 2 hours (cleanup only).

[01-7] RECOMMENDATION | Legal Screen Not Presented During Onboarding
File: rider-app/app/profile-setup.tsx (missing ToS acceptance)
Finding: legal.tsx exists and fetches live ToS and Privacy Policy from the
  backend. It is accessible from Account → Legal. However, new riders completing
  sign-up are never shown or asked to accept the Terms of Service. App Store
  Review Guideline 4.0 requires apps to obtain user acceptance of ToS before the
  user commits to a service. This is also a PIPEDA consent requirement — processing
  personal data requires informed, explicit consent.
Fix: Add a "By continuing you agree to our Terms of Service and Privacy Policy"
  link in profile-setup.tsx or otp.tsx, with a required checkbox or explicit
  tap-to-agree CTA before the profile can be submitted.
  Effort: 2 hours.

[01-8] PASS | fare-split.tsx Has a Reachable Entry Point
File: rider-app/app/payment-confirm.tsx:278
Finding: router.push('/fare-split' as any) is called from the payment confirmation
  screen. fare-split.tsx is functional: up to 5 participants, live fare
  calculation, store integration via walletStore.createFareSplit(). The screen
  correctly guards against no-active-ride state (lines 60–68).

[01-9] PASS | chat-driver.tsx Delivers Messages via WebSocket
File: rider-app/app/chat-driver.tsx:39–57
Finding: On mount, the screen loads history once via GET /rides/{id}/messages.
  Subsequent messages arrive through chatMessages in rideStore, which is
  populated in real-time by the useRiderSocket WebSocket hook (addChatMessage).
  No polling interval is present. Messages from the driver are delivered in
  <100 ms via WebSocket, not on a polling cycle.

[01-10] PASS | scheduled-rides.tsx Is Accessible from Account Tab
File: rider-app/app/(tabs)/account.tsx:183
Finding: router.push('/scheduled-rides') is wired under Account → Scheduled
  Rides. The screen shows countdown timers, route cards, and per-ride cancel
  actions. Imminent rides (< 15 min) are highlighted in amber.

[01-11] PASS | legal.tsx Exists and Surfaces Dynamic Backend Content
File: rider-app/app/legal.tsx:34–48
Finding: Legal screen fetches ToS and Privacy Policy from GET /settings/legal.
  Falls back to "No Terms of Service have been added yet." when backend is empty.
  Accessible from Account → Legal (account.tsx:240). Both tos and privacy
  document types are supported via the ?type= param.

[01-12] PASS | become-driver.tsx Multi-Step Form Is Feature-Complete
File: rider-app/app/become-driver.tsx
Finding: All 5 steps present (Intro, Personal, Vehicle, Documents, Review).
  Vehicle age validation enforces the 9-year rule (line 146). Document
  requirements are fetched dynamically from /drivers/requirements. Mandatory
  docs are enforced before step advance. Post-submit routing has a bug (see
  01-3) but the form itself correctly collects and submits all required data.

────────────────────────────────────────────────────────────────────────────────
TASK 01 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     3 |
| MEDIUM          |     2 |
| LOW             |     1 |
| PASS            |     5 |
| RECOMMENDATION  |     1 |
| TOTAL           |    12 |

================================================================================
TASK 02 — AUTHENTICATION & SESSION MANAGEMENT (Dimension 02)
================================================================================

[02-1] HIGH | OTP Brute-Force Lockout Silently Bypassed When Redis Is Unavailable
File: backend/routes/auth.py:75–77
Finding: _check_otp_lockout() wraps its Redis call in a bare except that returns
  silently on any exception. When Redis is unavailable (network partition, OOM,
  restart), the brute-force lockout is skipped entirely — the function returns as
  if the account is not locked. The SlowAPI rate limit (5 attempts/min on
  /verify-otp) remains, but 5 guesses/min * 33 min = 10,000 attempts exhausts
  the full 4-digit OTP keyspace while Redis is down. This removes the compensating
  control required for the 4-digit OTP design decision.
  Per audit ground rules: compensating controls must be confirmed present.
  If any compensating control is MISSING or can be bypassed → HIGH.
Fix: On Redis failure, _check_otp_lockout should fail closed — either raise a
  503 ("Service temporarily unavailable") or block the attempt and log the error.
  Do not silently return a no-lock result. Increment a Prometheus/Datadog counter
  on Redis errors so ops is alerted before an attack window opens.
  Effort: 1 hour.

[02-2] MEDIUM | Firebase Token Does Not Enforce Rider App Audience — Cross-App Auth Possible
File: backend/dependencies/__init__.py:134–172
Finding: get_current_user() calls firebase_auth.verify_id_token(token) without
  validating a rider-specific audience or app ID. The driver-facing auth path in
  routes/auth.py checks FIREBASE_DRIVER_APP_ID; the rider dependency has no
  equivalent check. A valid Firebase ID token issued by the driver app (or any
  other app sharing the same Firebase project) passes the rider API's
  authentication check. If the UID is not found in the users table, a new rider
  account is auto-created (lines 157–165). A driver with a valid Firebase token
  can bootstrap a rider identity without going through phone OTP.
Fix: Add an audience/app-ID check analogous to the driver path. Store
  FIREBASE_RIDER_APP_ID in settings and reject Firebase tokens whose app_id does
  not match.
  Effort: 1 hour.

[02-3] MEDIUM | Firebase-Authenticated Users Bypass Token Version and Session Revocation
File: backend/dependencies/__init__.py:134–172
Finding: The Firebase authentication path (lines 134–172) returns the user
  directly without checking token_version or session_id. The JWT path enforces
  both (token_version mismatch check at line 221, session_id check at line 213).
  Calling /auth/logout-all bumps token_version in the DB, which immediately
  invalidates all JWT sessions — but Firebase-authenticated sessions are not
  affected. An admin force-logout-all (e.g., suspected account compromise) does
  not invalidate active Firebase tokens. Firebase tokens are valid for up to 1
  hour and cannot be revoked server-side from the Spinr backend.
Fix: After the Firebase token is verified, perform the same token_version and
  session_id checks against the resolved user row. If a force-logout event must
  cover Firebase users, additionally call firebase_auth.revoke_refresh_tokens(uid)
  from the /auth/logout-all handler.
  Effort: 2 hours.

[02-4] MEDIUM | OTP Comparison Not Constant-Time
File: backend/routes/auth.py (verify-otp path)
Finding: OTP verification issues a database query keyed on (phone, hash_otp(code)).
  While hashing protects the raw value, the path-length timing of a B-tree index
  lookup on the 64-char hex hash can leak prefix-matching information under
  repeated timing measurements. Per D02 checklist, OTP comparison should use
  hmac.compare_digest on the locally re-computed hash vs the stored hash, keeping
  the database lookup keyed only on phone/expiry and performing the constant-time
  comparison in application code.
Fix:
    stored_hash = record["otp_hash"]
    submitted_hash = hash_otp(code)
    if not hmac.compare_digest(stored_hash, submitted_hash):
        _record_otp_failure(phone)
        raise HTTPException(400, "Invalid OTP")
  Effort: 30 minutes.

[02-5] LOW | Access Token Persisted to SecureStore — Memory-Only Pattern Not Followed
File: shared/store/authStore.ts:154
Finding: setTokens() writes the access token to expo-secure-store (line 154:
  storage.setItem('auth_token', token)) as well as holding it in _inMemoryToken.
  The standard security pattern for short-lived access tokens is memory-only:
  persist only the refresh token to disk so the access token is destroyed on app
  restart. SecureStore is hardware-backed (iOS Keychain / Android Keystore) and
  encrypted, so this is low-risk — but the access token now survives app restarts,
  extending the effective session window beyond the intended 15-minute JWT TTL.
  Severity: LOW (SecureStore is a secure enclave; risk is theoretical).
Fix: Remove the storage.setItem call for 'auth_token'. On cold start, exchange
  the persisted refresh token for a new access token via refreshTokens() rather
  than reading the access token from disk.
  Effort: 1 hour.

[02-6] PASS | JWT Algorithm Pinned to HS256 — Algorithm Confusion Not Possible
File: backend/dependencies/__init__.py:36, 106
Finding: JWT_ALGORITHM = "HS256" is declared as a named constant (line 36).
  jwt.decode() is called with algorithms=[JWT_ALGORITHM] (line 106). Passing a
  list of exactly one algorithm prevents the "alg:none" and RSA→HMAC confusion
  attacks. create_jwt_token also specifies algorithm=JWT_ALGORITHM explicitly.

[02-7] PASS | Token Version Enforced on Every JWT-Authenticated Request
File: backend/dependencies/__init__.py:114–124, 221
Finding: _token_version_mismatch() compares the token's token_version claim
  against the users table on every request. Force-logout-all increments the DB
  value; pre-bump tokens are immediately rejected with 401. Missing claim
  treated as 0 (backwards-compatible for pre-migration tokens).

[02-8] PASS | OTP Hashed SHA-256 — Not Stored Plaintext
File: backend/dependencies/__init__.py:63–65 (hash_otp); backend/routes/auth.py
Finding: hash_otp() (hashlib.sha256) is applied before OTP storage and on the
  submitted value before verification. Raw OTP never written to the database.

[02-9] PASS | OTP Expiry 5 Minutes — Timezone-Aware
File: backend/dependencies/__init__.py:37 (OTP_EXPIRY_MINUTES = 5)
Finding: OTP records carry an expires_at field set to now + 5 minutes using
  timezone-aware datetimes. Expired OTPs are rejected at verification time.

[02-10] PASS | OTP Rate Limiting Present (3/min send, 5/min verify)
File: backend/routes/auth.py:141, 206
Finding: SlowAPI decorators apply: 3 requests/minute on /auth/send-otp and
  5 requests/minute on /auth/verify-otp. Independent of the Redis lockout
  mechanism — even if Redis is down (see 02-1) rate limiting provides a
  floor of protection.

[02-11] PASS | Refresh Token Rotation with Revoked_at Column
File: backend/routes/auth.py (refresh endpoint)
Finding: Each refresh issues a new token and marks the previous one with
  revoked_at. Refresh token replay is blocked. The replaces chain provides
  audit history.

[02-12] PASS | Refresh Token Audience Validated as "rider"
File: backend/routes/auth.py:519
Finding: Refresh endpoint verifies audience == "rider" before accepting the token.
  Driver refresh tokens cannot be used against rider endpoints.

[02-13] PASS | Concurrent 401 Deduplication — Single Refresh Promise
File: shared/api/client.ts:158–163
Finding: _refreshPromise singleton ensures that when N inflight requests all
  receive 401, only one refresh attempt is made. All other callers await the
  same promise. No refresh flood, no double-invalidation.

[02-14] PASS | Profile-Complete Gate Enforced at Login and Cold Start
File: rider-app/app/otp.tsx:91–100; rider-app/app/index.tsx:33–76
Finding: Post-OTP routing checks profile_complete || hasProfileData before
  routing to /(tabs). Splash screen repeats the check on cold start. First-time
  riders cannot bypass profile setup.

[02-15] PASS | Logout Clears All Token State
File: shared/store/authStore.ts:468–483
Finding: logout() removes 'auth_token' from SecureStore, clears _inMemoryToken,
  and resets Zustand state. No token artifact survives a clean logout.

[02-16] PASS | +1 Canadian Phone Number Enforced at Entry
File: rider-app/app/login.tsx:60, 77
Finding: Phone field limited to 10 digits (line 60). "+1" prepended before API
  submission (line 77). Non-North-American numbers cannot reach the OTP flow.

[02-17] PASS | OTP Dev Bypass Gated to Development Environment Only
File: backend/routes/auth.py:116
Finding: The "1234" OTP bypass is guarded by ENV == "development". Production
  deployments cannot use the bypass. Per ground rules: hard-coded dev values in
  __DEV__/__DEV__ / development ENV blocks → LOW/RECOMMENDATION. This meets the
  bar for PASS given the gate.

────────────────────────────────────────────────────────────────────────────────
TASK 02 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     1 |
| MEDIUM          |     3 |
| LOW             |     1 |
| PASS            |    12 |
| RECOMMENDATION  |     0 |
| TOTAL           |    17 |

================================================================================
TASK 03 — ENCRYPTION & SECRETS MANAGEMENT (Dimension 03)
================================================================================

[03-1] CRITICAL | Real Supabase Service-Role Key Committed in backend/.env.example
File: backend/.env.example:3
Finding: backend/.env.example contains:
    SUPABASE_URL=https://dbbadhihiwztmnqnbdke.supabase.co
    SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzd…
  Decoding the JWT payload:
    {"iss":"supabase","ref":"dbbadhihiwztmnqnbdke","role":"service_role",
     "iat":1770836564,"exp":2086412564}
  The role is "service_role" — this key bypasses Supabase Row Level Security
  entirely and grants unrestricted read/write access to every table in the
  project. The .env.example is committed to the repository and visible to
  everyone with repo access (or if the repo is ever made public).
  An .env.example file should contain only placeholder text, not real credentials.
  The backend/core/middleware.py comment (line 171) even warns "NEVER commit
  this value — it bypasses RLS." — but the .env.example was committed anyway.
Fix:
  IMMEDIATE: Rotate the key in the Supabase dashboard (Settings → API → Rotate
  service-role key). The committed key will no longer function.
  Then replace .env.example line 3 with a placeholder:
    SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key-from-supabase-dashboard
  Verify git history does not contain the real key in prior commits by running:
    trufflehog git file://. --only-verified
  If found in history, use git-filter-repo to expunge and force-push.
  Effort: 30 minutes (rotate key) + 1 hour (history audit).

[03-2] MEDIUM | Test/Preview EAS Build Profiles Hardcode Production Backend URL
File: rider-app/eas.json:23, 32
Finding: Both "test" and "preview" build profiles set:
    "EXPO_PUBLIC_BACKEND_URL": "https://spinr-backend-production.up.railway.app"
  Internal testers who install test/preview builds will read from and write to
  the live production database. A tester completing the driver registration flow,
  cancelling rides, or testing payment flows will mutate production data.
  The "development" profile uses a placeholder ("http://your-local-ip:8000")
  while the production profile correctly uses "$EXPO_PUBLIC_BACKEND_URL".
Fix: Add a dedicated staging environment and set test/preview to the staging URL.
  If staging is not yet set up, use EAS environment variables (not hardcoded inline
  in eas.json) so the URL can be swapped without a code change:
    "env": { "EXPO_PUBLIC_BACKEND_URL": "$SPINR_TEST_BACKEND_URL" }
  Effort: 2 hours (staging env setup or EAS secret wiring).

[03-3] MEDIUM | TruffleHog Scans Only Last Commit and Requires Verification
File: .github/workflows/ci.yml:428–429
Finding: The TruffleHog step uses:
    trufflehog git file://. --only-verified --since-commit HEAD~1 --fail
  Two weaknesses:
  (a) --since-commit HEAD~1: only the most recent commit is scanned. A secret
    committed 2 commits ago will never be caught by CI. The committed Supabase
    service-role key (finding 03-1) exists in history and would not be caught.
  (b) --only-verified: TruffleHog verifies secrets by making live API calls.
    A rotated or expired key — or one from a project TruffleHog can't reach —
    passes through silently without blocking the PR.
Fix:
  (a) Remove --since-commit HEAD~1 for PR scans; keep it for push events to
    reduce scan time, but use --since-commit origin/$BASE_BRANCH for PRs.
  (b) Remove --only-verified to report unverified matches too. Treat them as
    a manual review gate (mark as exception or rotate) rather than auto-passing.
  Effort: 1 hour.

[03-4] LOW | Rider-App CI Job Missing EXPO_PUBLIC_ Secrets Scan
File: .github/workflows/ci.yml:176–209 (rider-app-test job)
Finding: The driver-app-test CI job (lines 153–159) runs a grep check for:
    EXPO_PUBLIC_.*SECRET | EXPO_PUBLIC_.*PRIVATE | EXPO_PUBLIC_.*KEY
  to prevent private keys being accidentally placed in EXPO_PUBLIC_ variables.
  The rider-app-test job (lines 176–209) has no equivalent check. A developer
  could add an EXPO_PUBLIC_SOME_PRIVATE_KEY to rider-app/.env or app.config.ts
  without CI catching it.
  Note: the driver-app check's regex also matches GOOGLE_MAPS_API_KEY (which
  is intentionally EXPO_PUBLIC_) — both apps should use a refined pattern
  that targets truly private values like SERVICE_ACCOUNT, JWT_SECRET, etc.
Fix: Add a parallel check to rider-app-test. Refine the pattern to exclude known-
  public keys (Maps, Firebase config):
    grep -r "EXPO_PUBLIC_.*(SECRET|PRIVATE|SERVICE_ACCOUNT|JWT)" \
      rider-app/.env* rider-app/app.config.ts
  Effort: 30 minutes.

[03-5] LOW | play-service-account.json Not in .gitignore
File: rider-app/eas.json:54; rider-app/.gitignore
Finding: eas.json references ./play-service-account.json for the Android
  production submit track. The file does not currently exist in the repo
  (confirmed by filesystem scan — PASS on the file itself). However,
  rider-app/.gitignore only excludes bugreport-*.zip. If a developer creates
  play-service-account.json in rider-app/ for a local submit, git will stage
  it without warning and it could be committed in the next commit.
Fix: Add to rider-app/.gitignore:
    play-service-account.json
  Effort: 5 minutes.

[03-6] PASS | Pickup OTP Hashed (SHA-256) Before Database Storage
File: backend/routes/rides.py:651
Finding: pickup_otp=hash_otp(pickup_otp_plain) — the plain OTP is generated
  by generate_pickup_otp(), the hash is stored in the DB, and the plain text
  is returned to the rider only in the single ride-creation response (line 691).
  Database dump or query does not yield usable OTP codes. Confirmed for the
  rider-created ride path (addressing P0-3 from the pre-populated checklist).

[03-7] PASS | Stripe Publishable Key Fetched at Runtime — Not Bundled
File: rider-app/app/_layout.tsx:126
Finding: GET /settings returns the stripe_publishable_key at boot. The key is
  not in eas.json, app.config.ts, or any .env.example. Operators can rotate
  the Stripe publishable key without an app release. The key is a publishable
  key (pk_test_* / pk_live_*) which is designed to be client-visible.

[03-8] PASS | bcrypt Cost Factor 12 for Admin Passwords
File: backend/utils/password.py:54
Finding: _BCRYPT_ROUNDS = 12 — roughly 250 ms per hash on a modern CPU.
  Meets the D03 minimum of ≥ 12. Legacy SHA256 rows are auto-upgraded on
  next login with constant-time compare for the SHA256 comparison path.

[03-9] PASS | JWT_SECRET Minimum Length and Placeholder Check at Production Boot
File: backend/core/middleware.py:207–208
Finding: _validate_production_config() enforces:
  (a) JWT_SECRET must not equal known placeholder strings (middleware.py:157–161)
  (b) len(JWT_SECRET) ≥ _MIN_JWT_SECRET_LENGTH (32 chars)
  Server refuses to start in ENV=production with a weak or missing JWT_SECRET.

[03-10] PASS | HSTS Set with 1-Year Max-Age, includeSubDomains, preload
File: backend/core/middleware.py:133
Finding: Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  applied via SecurityHeadersMiddleware when ENV=production. Skipped in dev to
  avoid browser HSTS caching on plain-HTTP local servers.

[03-11] PASS | Firebase App Check Enforced on All API Endpoints
File: backend/core/middleware.py:27, 351
Finding: _APP_CHECK_ENFORCEMENT = True and FirebaseAppCheckMiddleware is
  registered with enforcement_enabled=True. Every /api/* request must carry a
  valid X-Firebase-AppCheck token. WebSockets, health probes, and docs are
  correctly exempted (line 33). The app.config.ts registers
  @react-native-firebase/app-check (line 93) on the client side.

[03-12] PASS | Firebase Config Files Are Not Secrets (Per Ground Rules)
File: rider-app/google-services.json; rider-app/GoogleService-Info.plist
Finding: Both files contain standard Firebase project identifiers and API keys.
  Per Firebase documentation and audit ground rules, these values are public by
  design — security is enforced by Firebase Security Rules, not key secrecy.
  No private credentials or service account JSON are present in these files.

[03-13] PASS | No Supabase Service-Role Key or Private Keys in Mobile Bundle
File: rider-app/app.config.ts; shared/config/
Finding: EXPO_PUBLIC_ variables in app.config.ts are: BACKEND_URL and
  GOOGLE_MAPS_API_KEY — both appropriate for client exposure.
  shared/config/supabase.ts contains only placeholder strings
  ('YOUR_SUPABASE_URL', 'YOUR_SUPABASE_ANON_KEY'). No service-role key,
  Firebase service account JSON, or Stripe secret key is in any mobile config.

[03-14] PASS | eas.json Production Profile Uses $VAR References, Not Inline Values
File: rider-app/eas.json:39–40
Finding: The production build profile uses "$EXPO_PUBLIC_BACKEND_URL" and
  "$EXPO_PUBLIC_GOOGLE_MAPS_API_KEY" — resolved from EAS Secrets at build time,
  not committed inline.

[03-15] PASS | Play Store Service Account Not Committed
File: rider-app/eas.json:54; filesystem scan
Finding: eas.json references ./play-service-account.json but the file does not
  exist in the repository. The file would need to be created locally for a
  manual submit. See 03-5 for the .gitignore gap.

[03-16] PASS | CI Secrets Stored as GitHub Actions Secrets
File: .github/workflows/ci.yml:74–75
Finding: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in the CI job are read
  from ${{ secrets.* }} references, not hardcoded in the workflow YAML.
  Render, Railway, EAS, Vercel tokens likewise use secrets.

[03-17] PASS | No Stripe Live Keys (sk_live_*) Anywhere in Codebase
Finding: Full grep for sk_live_ across all source, config, and example files
  found zero matches. Only the @stripe/stripe-react-native plugin declaration
  (app.config.ts:75) is present.

────────────────────────────────────────────────────────────────────────────────
TASK 03 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     1 |
| HIGH            |     0 |
| MEDIUM          |     2 |
| LOW             |     2 |
| PASS            |    12 |
| RECOMMENDATION  |     0 |
| TOTAL           |    17 |

================================================================================
TASK 04 — INPUT VALIDATION
================================================================================
Scope    : rider-app/app/ forms · backend/routes/ validation layer ·
           backend/schemas.py · backend/validators.py
Framework: audit-framework/dimensions/04-input-validation.md
Checks   : Phone/identity format · monetary bounds · coordinate validation ·
           scheduled time · fare split phones · saved addresses · Zod client-side

────────────────────────────────────────────────────────────────────────────────

[04-1] HIGH | Backend Phone Schema Accepts Any International Code
File: backend/schemas.py:17, 21
Finding: SendOTPRequest and VerifyOTPRequest both declare:
  phone: str = Field(..., min_length=10, max_length=15, pattern=r'^\+\d+$')
  The regex `^\+\d+$` accepts any E.164 number (e.g. "+447911123456", "+33612345678").
  The Canada/US enforcement (+1 followed by exactly 10 digits) exists only in the
  login.tsx UI layer (handlePhoneChange limits to 10 digits; "+1" prepended at
  login.tsx:77). A direct POST to /auth/send-otp with a UK or French mobile number
  bypasses this restriction entirely, allowing OTP provisioning for out-of-market
  numbers and potentially violating the Canadian-market-only assumption embedded
  in other flows.
  Per ground rules: "Phone accepts any country code → HIGH finding."
Recommendation: Change pattern to r'^\+1\d{10}$' in SendOTPRequest and
  VerifyOTPRequest; document the +1 constraint in the API schema.

[04-2] MEDIUM | Tip Endpoints Use Raw request.json() — NaN Bypasses All Guards
File: backend/routes/rides.py:946, 973
Finding: Two tip-handling endpoints read amount outside of Pydantic:
  add_tip (line 946):
    tip_amount = float(data.get("amount", 0))
    if tip_amount <= 0: ...          # NaN → False, not blocked
    if tip_amount > 500: ...         # NaN → False, not blocked
  process_payment (line 973):
    tip_amount = float(data.get("tip_amount", 0))   # same issue
  In Python, both `float("nan") <= 0` and `float("nan") > 500` evaluate to
  False, so NaN passes every guard and is forwarded to payment/database logic.
  Downstream Decimal() conversion raises an unexpected InvalidOperation exception
  that may surface as a 500 error or be swallowed depending on the error handler.
  Additionally, neither endpoint uses a Pydantic model — raw dict access bypasses
  the full schema-validation pipeline.
Recommendation: Introduce a Pydantic model (e.g. TipRequest) with
  `amount: Decimal = Field(..., gt=0, le=500)`. Replace float() parsing with
  model validation. Add `math.isfinite()` check as defence-in-depth if raw
  float is retained.

[04-3] MEDIUM | stops Array — No Maximum Count and No Coordinate Validation
File: backend/schemas.py:273 (CreateRideRequest)
Finding: `stops: Optional[List[Dict[str, Any]]] = []`
  The stops list has no max_items constraint. An attacker can send an arbitrarily
  long stops array, potentially causing O(n) processing on every route-calculation
  path. Individual stop dicts are Dict[str, Any] — no lat/lng range validators
  are applied to stop coordinates, unlike pickup/dropoff which have explicit
  validate_lat / validate_lng validators (lines 290–300). A stop with lat=999,
  lng=999 is accepted silently.
Recommendation: Add `max_length=5` to the stops field; add a @validator that
  checks each stop dict for lat ∈ [−90, 90] and lng ∈ [−180, 180].

[04-4] MEDIUM | scheduled_time — No Past-Timestamp Rejection at Schema Level
File: backend/schemas.py:275 (CreateRideRequest)
Finding: `scheduled_time: Optional[datetime] = None`
  No @validator checks that the provided datetime is in the future. A client
  can submit a scheduled_time in the past (or a unix epoch date such as
  "1970-01-01T00:00:00") and the ride is accepted. This causes undefined
  dispatch behaviour: the scheduler may attempt to match a driver for a ride
  that was due minutes or years ago.
Recommendation: Add a validator that raises ValueError if
  scheduled_time < datetime.utcnow() + timedelta(minutes=5).

[04-5] MEDIUM | Fare Split Phone List — Individual Phone Strings Unconstrained
File: backend/routes/fare_split.py (CreateFareSplitRequest)
          rider-app/app/fare-split.tsx (validPhones filter)
Finding: The backend CreateFareSplitRequest enforces the list count
  (max_length=5, PASS) but imposes no format constraint on individual phone
  strings. Any string, including empty string, single character, or arbitrary
  text, passes as a valid participant phone. The fare-split.tsx client filters
  phones by `p.trim().length >= 10` — this rejects very short strings but
  accepts non-numeric text 10+ chars long and does not require a +1 prefix,
  format, or digit-only constraint.
Recommendation: Add pattern=r'^\+1\d{10}$' or similar E.164 constraint to
  each phone string in the list validator; mirror the constraint client-side.

[04-6] MEDIUM | SavedAddressCreate — No Length Limit or HTML Sanitization
File: backend/schemas.py:150–155 (SavedAddressCreate)
          backend/routes/addresses.py (create_saved_address handler)
Finding: SavedAddressCreate.name and SavedAddressCreate.address have no
  max_length or min_length constraints. More critically, backend/validators.py
  defines sanitize_string() which strips HTML tags — but addresses.py does not
  call it on either field before storing. When saved address names are rendered
  in the UI (map labels, autocomplete lists), an unsanitized string containing
  HTML/script fragments could cause display corruption. Note: the project uses
  React Native which does not execute injected HTML in Text components, limiting
  practical XSS; the primary risk is display corruption and future web dashboard
  rendering.
Recommendation: Add max_length=100 to name, max_length=300 to address;
  apply sanitize_string() in the create_saved_address handler (or via a
  @validator). Longer term, replace the naive regex stripper with bleach.

[04-7] MEDIUM | WalletPayRequest.amount — No Maximum Cap
File: backend/routes/wallet.py:89–91
Finding: `WalletPayRequest: amount: float = Field(..., gt=0)`
  There is no upper bound. While the balance check prevents spending more than
  the current balance, there is no server-side ceiling on the requested payment
  amount. A race condition or client bug could trigger a wallet debit for an
  implausibly large amount (e.g. $99,999) if the balance ever exceeds that figure.
  Compare: TopUpRequest correctly has le=500.
Recommendation: Add `le=500` or a configurable max payment cap consistent with
  the wallet top-up ceiling.

[04-8] MEDIUM | Wallet Monetary Amounts Use float, Not Decimal
File: backend/routes/wallet.py:86, 91, 96
Finding: TopUpRequest.amount and WalletPayRequest.amount are typed as `float`.
  The wallet handler internally converts via `_d(req.amount)` (Decimal with
  ROUND_HALF_UP), which mitigates most precision issues. However, the Pydantic
  model accepts the raw float, so JSON values like 9.99 are subject to IEEE 754
  representation errors before reaching _d(). For a financial service, monetary
  inputs should be typed as Decimal at the Pydantic boundary.
  Contrast: CreateRideRequest fares are Decimal throughout (PASS).
Recommendation: Change `amount: float` to `amount: Decimal` in both wallet
  request models; remove the intermediate _d() conversion where no longer needed.

[04-9] LOW | pickup/dropoff Address Minimum Length Too Permissive (3 chars)
File: backend/schemas.py:282–285 (CreateRideRequest.validate_address)
Finding: `if len(v) < 3: raise ValueError('Address must be at least 3 characters')`
  Three characters is too short to represent any meaningful address. A single
  letter + digit + space passes. This provides no useful data-quality guard and
  could allow nonsense pickup/dropoff strings into the dispatch system.
Recommendation: Raise minimum to 10 characters (or better: require the address
  to be geocoded/resolved server-side before ride creation).

[04-10] RECOMMENDATION | No Zod Runtime Validation on API Responses (Client-Side)
File: rider-app/store/rideStore.ts, rider-app/hooks/useRiderSocket.ts, all screens
Finding: Zero Zod imports exist anywhere in rider-app/ or shared/. API responses
  and WebSocket messages are consumed as `any` (TypeScript) without runtime shape
  validation. A backend contract change (renamed field, type change, null where
  not expected) will cause silent misbehaviour rather than a caught parse error.
  This is the root cause of the broad `: any` typing noted in R-P2-4.
Recommendation: Add Zod (or equivalent) for at minimum the ride object, driver
  location update, and auth response shapes. Use safeParse() on WS messages so
  malformed payloads are discarded before reaching store state.

────────────────────────────────────────────────────────────────────────────────
PASSES
────────────────────────────────────────────────────────────────────────────────

[04-11] PASS | +1 Prefix Enforced in Login UI
File: rider-app/app/login.tsx:60, 66, 77
Finding: handlePhoneChange strips non-digits and limits to 10 chars (line 60);
  handleSendCode rejects length < 10 (line 66); "+1" prepended before API call
  (line 77). Client enforces Canada/US format before reaching backend.

[04-12] PASS | Pickup and Dropoff Lat/Lng Range Validated
File: backend/schemas.py:290–300 (CreateRideRequest validators)
Finding: validate_lat enforces [−90, 90]; validate_lng enforces [−180, 180].
  Both applied to pickup and dropoff coordinates at the Pydantic boundary.

[04-13] PASS | Ride Creation Uses Decimal for All Monetary Fields
File: backend/schemas.py:226–236 (Ride model); backend/routes/rides.py
Finding: base_fare, distance_fare, time_fare, booking_fee, platform_fee,
  tip_amount all typed as Decimal. No float-precision risk in fare calculation.

[04-14] PASS | RideRatingRequest tip_amount Uses Decimal with Non-Negative Guard
File: backend/schemas.py:262
Finding: `tip_amount: Decimal = Field(default=Decimal("0.0"), ge=0)`
  Decimal type and lower bound enforced at schema level for the rating endpoint.
  (Upper bound absent — partially mitigated by 04-2 note; the add_tip endpoint
  is the primary concern there.)

[04-15] PASS | Fare Split Participant Count Capped at 5
File: backend/routes/fare_split.py (CreateFareSplitRequest)
Finding: `participant_phones: List[str] = Field(..., min_length=1, max_length=5)`
  Backend hard-caps fare splits at 5 participants; matches UI guard in fare-split.tsx.

[04-16] PASS | Wallet Top-Up Enforces Min and Max Bounds
File: backend/routes/wallet.py:86
Finding: `amount: float = Field(..., gt=0, le=500)`
  Minimum (>0) and maximum ($500) enforced at Pydantic level. See 04-8 for the
  float-vs-Decimal type concern.

[04-17] PASS | sanitize_string() Utility Exists in validators.py
File: backend/validators.py
Finding: sanitize_string() uses re.sub(r"<[^>]*>", "", value) to strip HTML
  tags. It is already called in corporate_accounts.py. The gap (not called for
  saved addresses) is tracked in 04-6; the utility itself is available.

[04-18] PASS | validate_ride_location Called at Ride Creation (Null Island Rejected)
File: backend/routes/rides.py:424, 525
Finding: validate_ride_location() is called for both pickup and dropoff at ride
  creation, rejecting (0.0, 0.0) null-island coordinates and other clearly
  invalid GPS inputs.

[04-19] PASS | Promo Discount Cap Exists for Percentage-Based Promos
File: backend/routes/promotions.py
Finding: Percentage promo codes apply max_discount from the promo record.
  Absolute-amount promos are bounded by the promo's discount_value field.
  (The server-side vs. client-supplied fare issue is separately tracked in R-P1-8.)

[04-20] PASS | Rating Bounded 1–5 Stars
File: backend/schemas.py:260 (RideRatingRequest)
Finding: `rating: int = Field(ge=1, le=5)` — out-of-range ratings rejected
  at schema level.

────────────────────────────────────────────────────────────────────────────────
TASK 04 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     1 |
| MEDIUM          |     7 |
| LOW             |     1 |
| PASS            |    10 |
| RECOMMENDATION  |     1 |
| TOTAL           |    20 |

================================================================================
TASK 05 — ANDROID & iOS UI/UX QUALITY
================================================================================
Scope    : rider-app/app/ (all 36 screens) · rider-app/components/ ·
           shared/components/
Framework: audit-framework/dimensions/05-ui-ux-android-ios.md
Checks   : Safe area insets · BackHandler · KeyboardAvoidingView · touch targets ·
           map provider · empty states · error states · font scaling

────────────────────────────────────────────────────────────────────────────────

[05-1] HIGH | ride-in-progress.tsx — BackHandler Missing, Exits Active Ride on Android
File: rider-app/app/ride-in-progress.tsx (no BackHandler anywhere in file)
Finding: The hardware back button on Android exits an active ride with no
  confirmation dialog. driver-arriving.tsx (line 192–198) and driver-arrived.tsx
  (line 67–70) both correctly intercept the hardware back press and show a
  cancellation-fee dialog. ride-in-progress.tsx has no BackHandler import and no
  addEventListener call — pressing back takes the rider to the home screen while
  the ride is live. Note: this is the unresolved portion of R-P0-4 (the other two
  screens were fixed; ride-in-progress was missed).
Recommendation: Add BackHandler to ride-in-progress.tsx matching the pattern
  in driver-arriving.tsx: intercept back, show "End ride early? Full fare applies"
  confirmation dialog before allowing navigation away.

[05-2] MEDIUM | search-destination.tsx — No KeyboardAvoidingView; Submit Button Hidden
File: rider-app/app/search-destination.tsx (no KAV import or use)
Finding: The "Search Ride" primary action button is at the bottom of the screen
  (line 615–625), below the predictions FlatList. No KeyboardAvoidingView wraps
  the screen. When the keyboard is open, the button is hidden behind it. While
  tapping a prediction dismisses the keyboard, users who manually enter both
  addresses without using autocomplete cannot see or tap "Search Ride" until they
  manually dismiss the keyboard (or scroll, which is blocked by the FlatList).
  On iPhone SE (375×667pt) with 2+ stops this issue is more pronounced.
  Compare: login.tsx, otp.tsx, profile-setup.tsx all correctly use KAV.
Recommendation: Wrap the screen in KeyboardAvoidingView with
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'} so the content shifts
  above the keyboard and the Search Ride button remains visible.

[05-3] MEDIUM | activity.tsx — API Error Shows Misleading "No rides yet" Empty State
File: rider-app/app/(tabs)/activity.tsx:46–64
Finding: fetchData() wraps both API calls with individual .catch(() => ({ data: [] }))
  (lines 48–49). When the network is unavailable or /rides/history returns an
  error, rides is set to [] and the "No rides yet" empty state (line 204–212) is
  displayed. A rider who has completed 20 rides will see "Your ride history will
  appear here once you complete your first trip." after a network error — a false
  statement that can confuse and erode trust. No error state is differentiated
  from a genuinely empty list.
Recommendation: Track API error separately (useState for error); when error is
  true, show "Could not load rides. Pull to refresh." instead of the empty state.
  Note: R-P2-6 already calls for this fix.

[05-4] MEDIUM | ride-options.tsx — fetchEstimates Failure Shows Empty List, No Error/Retry
File: rider-app/app/ride-options.tsx:74, 406–411
Finding: If fetchEstimates() fails (network error, backend down), estimates stays
  empty. The isLoading spinner correctly disappears, and allUnavailable is false
  (because that check requires estimates.length > 0). The vehicle options list
  renders nothing — no error message, no retry button. The user sees a blank
  vehicle options area with no indication that anything went wrong.
Recommendation: Track fetch error state; when estimates.length === 0 && !isLoading
  && error, show "Could not load fares. Tap to retry." with a retry button.
  This is the fix called for in R-P2-6.

[05-5] MEDIUM | wallet.tsx — Balance Load Failure Shows $0.00 Instead of Error
File: rider-app/app/wallet.tsx:119–124
Finding: The balance card renders `${(wallet?.balance ?? 0).toFixed(2)}` — when
  fetchWallet() fails and wallet is null, the balance shows "$0.00". The rider
  cannot distinguish between a genuine zero balance and a load failure. No error
  indicator or "Balance unavailable" label is shown.
Recommendation: Show "Balance unavailable" and a retry button when wallet is
  null after loading completes. This is tracked in R-P2-6.

[05-6] MEDIUM | ride-in-progress.tsx — No Error State When Ride Fails to Load
File: rider-app/app/ride-in-progress.tsx:306–309
Finding: When currentRide is null (fetchRide failed or returned null), the screen
  shows:
    <View style={styles.mapPlaceholder}>
      <Text>Loading Map...</Text>
    </View>
  This text is shown indefinitely if the API is unavailable — not just during
  loading. There is no error state, no retry button, and no way for the rider
  to recover. Compare: driver-arriving.tsx has a proper error state with a retry
  button (line 295–302).
Recommendation: Track isLoading and error separately; when error and no
  currentRide, show an error state with retry. Use the pattern from
  driver-arriving.tsx (lines 291–303).

[05-7] MEDIUM | driver-arrived.tsx — Blank Screen When currentRide is Null
File: rider-app/app/driver-arrived.tsx:126–206
Finding: The full-screen map is rendered as `{currentRide ? (<MapView ...>) : null}`.
  When currentRide is null (API error or store not hydrated), the map area is
  completely blank — no loading indicator, no error message, no retry. The
  bottom sheet still renders (with placeholder addresses) but the rider has no
  way to know what happened or recover. The header is shown but the core
  screen content is invisible.
Recommendation: Add an else clause that renders an error/loading view with a
  retry button, consistent with the driver-arriving error state.

[05-8] MEDIUM | allowFontScaling Not Set on Text Components (All 36 Screens)
File: rider-app/app/ (all screens) · shared/components/
Finding: Zero usages of `allowFontScaling` found in rider-app/app/ or
  shared/components/. React Native's default is `allowFontScaling={true}`,
  meaning system accessibility font sizes (up to 200%+ on iOS) automatically
  scale all Text. Fixed-height containers (status pills, bottom sheet handle,
  map buttons, driver card rows) will overflow or clip text at large font sizes.
  Critical safety-relevant labels (ETA, driver name, SOS) are affected.
  Per D05 severity guide: "allowFontScaling not set — layout breaks at 200% font → MEDIUM."
Recommendation: Audit fixed-height containers and set
  `allowFontScaling={false}` on Text inside them. For free-flow content,
  ensure sufficient flex or minHeight to handle scaled text.

[05-9] MEDIUM | Home Screen Map Zoom Buttons Below 44pt Touch Target (40×40pt)
File: rider-app/app/(tabs)/index.tsx — mapControlButton style (line 509–513)
Finding:
  mapControlButton: { width: 40, height: 40, justifyContent: 'center', alignItems: 'center' }
  The zoom-in (+) and zoom-out (−) buttons are 40×40pt with no `hitSlop`. iOS
  HIG mandates a minimum 44×44pt touch target. At 40pt these buttons are
  tappable but unreliable, especially with gloves or cold fingers (relevant for
  Saskatchewan winters).
Recommendation: Increase to `width: 44, height: 44` or add
  `hitSlop={{ top: 2, bottom: 2, left: 2, right: 2 }}`.

────────────────────────────────────────────────────────────────────────────────
PASSES
────────────────────────────────────────────────────────────────────────────────

[05-10] PASS | OfflineBanner Uses useSafeAreaInsets — Correctly Above Notch (R-P0-2)
File: shared/components/OfflineBanner.tsx:36–37, 170–171
Finding: `const insets = useSafeAreaInsets()` and
  `container: { top: topInset, ... }` confirmed. The banner is rendered at the
  safe area top, never behind the notch or Dynamic Island. P0-2 fix verified
  as already applied.

[05-11] PASS | driver-arriving.tsx BackHandler Blocks Hardware Back with Cancel Dialog
File: rider-app/app/driver-arriving.tsx:192–198
Finding: BackHandler.addEventListener('hardwareBackPress', () => { handleBack(); return true; })
  The handler shows a status-aware dialog (free cancel vs. fee vs. in-progress
  full fare). Correctly returns true to prevent default back navigation.

[05-12] PASS | driver-arrived.tsx BackHandler Blocks Hardware Back
File: rider-app/app/driver-arrived.tsx:67–70
Finding: BackHandler.addEventListener with handleCancelPress shows cancellation
  fee warning. Returns true to block default navigation. Dep on
  currentRide?.total_fare keeps the dialog accurate.

[05-13] PASS | ride-completed.tsx BackHandler Blocks Back (Cannot Exit Before Rating)
File: rider-app/app/ride-completed.tsx:65–68
Finding: BackHandler.addEventListener returns true unconditionally, preventing
  the rider from leaving the completion screen without rating and paying.

[05-14] PASS | ride-completed.tsx and payment-confirm.tsx Use KeyboardAvoidingView
File: rider-app/app/ride-completed.tsx:161; rider-app/app/payment-confirm.tsx:104
Finding: Both screens use `behavior={Platform.OS === 'ios' ? 'padding' : 'height'}`
  — submit buttons remain visible when keyboard is open.

[05-15] PASS | login.tsx / otp.tsx / profile-setup.tsx Use KeyboardAvoidingView
File: rider-app/app/login.tsx:109; otp.tsx:202; profile-setup.tsx:100
Finding: All three auth screens correctly wrap content in KAV with
  platform-specific behavior. Phone input and OTP fields remain visible above
  keyboard.

[05-16] PASS | Platform-Specific Map Provider (Android: Google, iOS: Default)
File: rider-app/app/driver-arriving.tsx:306; driver-arrived.tsx:21;
       ride-in-progress.tsx:211; shared/components/AppMap.tsx:9
Finding: All map screens use `PROVIDER_GOOGLE` on Android and `undefined` on iOS,
  matching the "Google Maps on Android, Apple Maps on iOS" checklist requirement.

[05-17] PASS | edgeToEdgeEnabled: true in app.config.ts
File: rider-app/app.config.ts:46
Finding: Android edge-to-edge display is explicitly enabled.

[05-18] PASS | SafeAreaView edges={['top']} Guards Header Overlays on Map Screens
File: rider-app/app/driver-arriving.tsx:269; ride-in-progress.tsx:195, 203;
       driver-arrived.tsx:209; (tabs)/index.tsx:141
Finding: All map screen header overlays use SafeAreaView with edges={['top']}.
  Content is never rendered behind the notch, Dynamic Island, or status bar.

[05-19] PASS | activity.tsx Has "No rides yet" Empty State
File: rider-app/app/(tabs)/activity.tsx:203–212
Finding: Empty state renders when rides.length === 0 && !loading, with icon,
  title, and explanatory text. Pull-to-refresh is also wired (RefreshControl).

[05-20] PASS | saved-places.tsx Has "No saved places yet" Empty State
File: rider-app/app/saved-places.tsx:182–186
Finding: FlatList ListEmptyComponent renders icon, title, and subtitle with
  call to action.

[05-21] PASS | notifications.tsx Has "No notifications" Empty State
File: rider-app/app/notifications.tsx:183–187
Finding: ListEmptyComponent shows notifications-off icon with "No notifications"
  and "You're all caught up!" message.

[05-22] PASS | driver-arriving.tsx Has Error State with Retry for Map Load Failure
File: rider-app/app/driver-arriving.tsx:295–302
Finding: When error is truthy: shows alert icon, error text, and a "Retry"
  TouchableOpacity that calls fetchRide(). This is the most complete error state
  of all ride-flow screens.

────────────────────────────────────────────────────────────────────────────────
LOW
────────────────────────────────────────────────────────────────────────────────

[05-23] LOW | Star Rating Buttons at iOS HIG Minimum (44pt) — Below Android 48dp
File: rider-app/app/ride-completed.tsx — starBtn style (line 514)
Finding: `starBtn: { padding: 4 }` with 36pt icon = 44×44pt touch target.
  This meets the iOS HIG 44pt minimum exactly. Android Material Design requires
  48×48dp minimum. No hitSlop is set to extend the touch area further.
  On a device running at 3× density (Pixel 7), 44pt ≈ 132dp — well above 48dp,
  so in practice this passes on Android too. The risk is low but worth a note.
Recommendation: Add `hitSlop={{ top: 4, bottom: 4, left: 4, right: 4 }}` for
  comfortable tapping. Already tracked for accessibility in R-P1-9.

────────────────────────────────────────────────────────────────────────────────
TASK 05 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     1 |
| MEDIUM          |     8 |
| LOW             |     1 |
| PASS            |    13 |
| RECOMMENDATION  |     0 |
| TOTAL           |    23 |

================================================================================
TASK 06 — REAL-TIME FEATURES (WebSocket & GPS) (Dimension 06)
================================================================================
Scope    : rider-app/hooks/useRiderSocket.ts · rider-app/store/rideStore.ts ·
           rider-app/app/driver-arriving.tsx · rider-app/app/ride-in-progress.tsx ·
           rider-app/app/chat-driver.tsx · rider-app/app/(tabs)/index.tsx
Framework: audit-framework/dimensions/06-real-time.md
Checks   : WS auth order · exponential backoff · poll/WS race condition ·
           driver_timeout UI · chat delivery mechanism · GPS permission flow ·
           WS cleanup · heartbeat · background/foreground reconnect

────────────────────────────────────────────────────────────────────────────────

[06-1] PASS | WS Auth Sent as First Message Before Any Data Consumed
File: rider-app/hooks/useRiderSocket.ts:160–168
Finding: In ws.onopen, the very first action is to send the auth message:
    ws.send(JSON.stringify({ type: 'auth', token, client_type: 'rider' }))
  The token is retrieved from useAuthStore.getState().token immediately before
  the send. No data subscription or store-read happens before this call.
  The hook guards against a missing token before opening the connection (lines
  149–153), so the socket is never opened without an auth token available.
  The connect() function itself refuses to proceed if userId, rideId, or token
  is absent (lines 145–153). Authentication is therefore enforced as the first
  message in every session.

[06-2] PASS | Exponential Backoff with Jitter Implemented
File: rider-app/hooks/useRiderSocket.ts:186–196
Finding: RECONNECT_DELAYS = [1000, 2000, 5000, 10000, 30000] (line 36).
  On ws.onclose the next delay is selected as:
    const baseDelay = RECONNECT_DELAYS[Math.min(attempt, length - 1)]
    const jitter = Math.random() * 1000 - 500;   // ±500 ms
    const delay = Math.max(500, baseDelay + jitter);
  The attempt counter is incremented before each recursive connect() call
  (line 195). The delay sequence is [~1s, ~2s, ~5s, ~10s, ~30s] capped at the
  last bucket — correct exponential backoff with jitter. The reconnect loop is
  only entered when userId and rideId are still present (lines 185–186),
  preventing ghost reconnects after ride completion.

[06-3] MEDIUM | Poll Result Can Overwrite More-Recent WS Driver Position
File: rider-app/store/rideStore.ts:355–371 (fetchRide) ·
      rider-app/app/driver-arriving.tsx:81–88 · rider-app/app/ride-in-progress.tsx:78–83
Finding: fetchRide() at rideStore.ts:364 unconditionally overwrites both
  currentRide and currentDriver with whatever the REST response returns:
    set({ currentRide: ride, currentDriver: driver, isLoading: false });
  updateDriverLocation() (called from the WS path) updates currentDriver.lat/lng
  in real time with sub-100 ms latency. However, when a poll fires (every 3 s on
  driver-arriving, 15 s on ride-in-progress), the REST API response's driver
  object contains the driver's GPS coordinates as of the last backend persist
  cycle. If the poll response arrives AFTER a WS driver_location_update, the
  poll sets currentDriver.lat/lng to older coordinates, causing the map marker
  to jump backwards.
  Example sequence:
    t=0   WS: driver at (52.1300, -106.6700) → marker updates
    t=0.5 REST /rides/{id} responds with driver at (52.1285, -106.6712)
            → fetchRide sets currentDriver → marker jumps back 150 m
  The driver-arriving screen's 3 s poll interval makes this visible on the map
  at typical city driving speeds (50 km/h ≈ 14 m/s → 42 m/interval).
Fix: In fetchRide(), preserve the WS-updated lat/lng if they are fresher than
  the REST response. One approach — record the timestamp of the last WS
  location update in the store and only overwrite lat/lng from REST if the WS
  timestamp is absent or older than the response. A simpler approach: in
  fetchRide(), merge the REST driver object but skip lat/lng fields when
  currentDriver already has coordinates (since WS is always newer). Effort: 1h.

[06-4] MEDIUM | driver_timeout Fires fetchRide() But UI Has No "Searching Again" Indicator
File: rider-app/hooks/useRiderSocket.ts:113–115
Finding: The driver_timeout case calls fetchRide(rideId) which will reload the
  ride object. If the backend re-dispatches and the status returns to 'searching',
  driver-arriving.tsx's status check at lines 85–87 will re-enter fastPoll (3 s).
  However, there is no transient UI element that explicitly tells the rider
  "Your driver didn't respond — finding you a new one." The screen will show the
  existing driver card (or the "Finding your driver" spinner, depending on the
  returned status) but there is no timeout-specific message or animation.
  A rider watching the screen may be confused why the driver info disappeared
  without explanation. The comment in the code (line 111) acknowledges the intent
  but the UI does not surface it.
Fix: On receiving driver_timeout, before calling fetchRide, set a transient
  store flag (e.g. driverTimedOut: true) or dispatch a local notification/alert:
  Alert.alert('Driver Not Available', 'Looking for a new driver…'). Clear the
  flag after the next fetchRide resolves. Effort: 1h.

[06-5] PASS | Chat Delivers New Messages via WebSocket (Not Polling)
File: rider-app/hooks/useRiderSocket.ts:127–129 ·
      rider-app/store/rideStore.ts:482–487 · rider-app/app/chat-driver.tsx:38–57
Finding: The chat_message WS case in useRiderSocket (line 127–129) calls
  useRideStore.getState().addChatMessage(data), which deduplicates by id
  (rideStore.ts:484–486). chat-driver.tsx has NO polling interval — there is
  no setInterval anywhere in the file. On mount it loads history once via
  GET /rides/{id}/messages (lines 39–52), then relies entirely on the WS push
  for subsequent messages. New messages arrive in real-time via the hook mounted
  in _layout.tsx (line 103). No backgrounding concern exists for the WS
  transport because useRiderSocket reconnects on AppState 'active' (lines
  226–246). This supersedes the concern raised in R-P1-2 — the handler IS
  present and functional.

[06-6] MEDIUM | GPS Permission: No Re-Permission Flow When User Denies Location
File: rider-app/app/(tabs)/index.tsx:82–83
Finding: index.tsx requests foreground permission correctly as the first step
  (line 82: Location.requestForegroundPermissionsAsync()). If the user denies,
  the function early-returns at line 83 (`if (status !== 'granted') return`).
  No alert, no explanation, no deep-link to Settings is shown. The map
  renders the "Locating..." placeholder indefinitely. The rider has no feedback
  that their location was denied and no prompt to re-enable it.
  Note: Background location permission is not requested at all in this screen —
  this is acceptable for the rider-side (the rider does not need background GPS;
  only the driver does). The foreground-before-background order is therefore
  satisfied by omission.
Fix: When status !== 'granted', show an alert:
  "Location access required. Spinr needs your location to show nearby drivers
   and confirm your pickup. Please enable location in Settings."
  with an "Open Settings" button that calls Linking.openSettings(). Effort: 30m.

[06-7] PASS | WS Socket Closed on Ride Completion and on Logout
File: rider-app/hooks/useRiderSocket.ts:215–223 · rider-app/store/rideStore.ts:497–499
Finding: The lifecycle effect (lines 215–223) calls disconnect() when
  currentRide?.id becomes falsy. clearRide() in rideStore.ts sets
  currentRide: null (line 498), which triggers the effect and calls disconnect().
  clearRide() is called from ride_cancelled WS handler (line 108), the cancel
  button flows in driver-arriving.tsx, and the ride-completed screen.
  For logout: useAuthStore.logout() clears the user object, causing user?.id
  to become null, which also fires the lifecycle effect's disconnect() path.
  Both ride completion and logout correctly close and null the WebSocket reference.

[06-8] PASS | Heartbeat — useRiderSocket Replies to Server ping with pong
File: rider-app/hooks/useRiderSocket.ts:62–65
Finding: The 'ping' case in handleMessage checks ws.readyState === WebSocket.OPEN
  before sending. The pong reply is sent immediately in the same message handler,
  with no setTimeout delay. This satisfies the 30-second ping-pong keepalive
  requirement from the checklist. The pong message is { type: 'pong' } matching
  the expected server format.

[06-9] PASS | WS Reconnects When App Returns to Foreground
File: rider-app/hooks/useRiderSocket.ts:226–246
Finding: AppState.addEventListener('change') is registered on mount (line 227).
  When nextState === 'active' and both userId and rideId are set, the code
  checks if the socket is CLOSED or CLOSING (line 234) and calls connect() with
  the attempt counter reset to 0 (line 240 — fast reconnect on foreground).
  Any pending reconnect timeout is cancelled before reconnecting (lines 237–239).
  The subscription is correctly removed on unmount (line 245: sub.remove()).
  This covers the scenario where the phone was locked for >30 s (heartbeat missed)
  and the server silently closed the connection — foreground triggers a clean
  reconnect rather than waiting for the backoff timer.

────────────────────────────────────────────────────────────────────────────────
ADDITIONAL CHECKS (from D06 checklist not in the 9 specific checks)
────────────────────────────────────────────────────────────────────────────────

[06-10] LOW | GPS Accuracy Uses Location.Accuracy.Balanced on Home Screen — Not BestForNavigation
File: rider-app/app/(tabs)/index.tsx:96
Finding: index.tsx calls getCurrentPositionAsync with accuracy: Location.Accuracy.Balanced.
  Per the D06 checklist, active rides should use ACCURACY.BestForNavigation.
  The home screen is not an active-ride screen, so Balanced is appropriate there.
  However, no GPS subscription is set up for the rider during the ride flow — the
  rider's position is not tracked for navigation purposes (only the driver's
  position is). Therefore BestForNavigation is not applicable for rider GPS.
  This is flagged LOW as a documentation gap: the accuracy choice is correct but
  should be annotated in comments to explain why BestForNavigation is absent.

[06-11] RECOMMENDATION | Polling Not Suspended When WebSocket Is Connected
File: rider-app/app/driver-arriving.tsx:81–88 · rider-app/app/ride-in-progress.tsx:78–83
Finding: driver-arriving.tsx polls every 3 s (fast) or 15 s (slow) regardless of
  WS connection state. ride-in-progress.tsx polls every 15 s unconditionally.
  When WS is healthy, these polls are redundant and waste battery/bandwidth.
  This was already documented as R-P2-5 (before this audit). Confirming the gap
  is still present. The connectionState from useRiderSocket is available in
  _layout.tsx (line 103) but is not exposed to child screens.
  This is a RECOMMENDATION, not a new finding — see R-P2-5 for fix guidance.

────────────────────────────────────────────────────────────────────────────────
TASK 06 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     0 |
| MEDIUM          |     3 |
| LOW             |     1 |
| PASS            |     6 |
| RECOMMENDATION  |     1 |
| TOTAL           |    11 |

================================================================================
TASK 07 — STATE MACHINE & DISPATCH (Dimension 07)
================================================================================
Scope    : rider-app/store/rideStore.ts · rider-app/app/driver-arriving.tsx ·
           rider-app/components/FreeCancelTimer.tsx ·
           rider-app/hooks/useRiderSocket.ts · backend/routes/rides.py
Framework: audit-framework/dimensions/07-state-machine.md
Checks   : Cancel fee after driver_arrived · /start and /complete role guards ·
           double booking · cold-start hydration · FreeCancelTimer expiry ·
           driver_timeout UI reset · magic strings

State machine: searching → driver_assigned → driver_accepted →
               driver_arrived → in_progress → completed / cancelled

────────────────────────────────────────────────────────────────────────────────

[07-1] HIGH | Rider Can Call /rides/{id}/start Directly — Bypasses OTP Verification
File: backend/routes/rides.py:1876–1890
Finding: The endpoint rider_start_ride (POST /rides/{id}/start) is reachable by
  any authenticated rider, not just the assigned driver. It checks only that
  the caller is the ride's rider_id, then unconditionally flips the ride to
  'in_progress' and sets ride_started_at — no OTP verification, no driver role
  check, no requirement that the driver have entered the OTP on their device.
  The driver-app's start-ride flow (POST /drivers/rides/{id}/start) is the
  intended path: that endpoint verifies the 4-digit OTP before transitioning.
  This endpoint creates a completely parallel bypass path:
    POST /rides/{rideId}/start   (rider token)  →  status = 'in_progress'
  A rider can start their own trip before the driver arrives (even from
  'driver_assigned' state, because the guard only checks
  `ride.status not in ["driver_arrived"]` — meaning driver_arrived is the
  only allowed state, but the endpoint doesn't require the driver to have
  verified anything). The entire OTP pickup-verification flow is skipped.
  An adversarial rider could also use this to remove a driver from a ride
  they don't want (start the ride, driver sees it as started but never
  verified OTP, causing confusion and a potential dispute).
  The endpoint's docstring "when OTP already verified or used together with
  driver" acknowledges the OTP bypass but provides no enforcement.
Fix: Remove rider_start_ride entirely, or restrict it to driver role:
    driver = await db.find_one("drivers", {"user_id": current_user["id"]})
    if not driver or ride.get("driver_id") != driver["id"]:
        raise HTTPException(status_code=403, detail="Only the assigned driver can start the ride")
  Require OTP verification in the same guard. Rides must only transition to
  in_progress via the driver-side /drivers/rides/{id}/start endpoint.
  Effort: 1 hour.

[07-2] HIGH | cancel_ride Uses Wrong Status Guard — In-Progress Rides Can Be Cancelled Free
File: backend/routes/rides.py:1341
Finding: The cancel_ride_rider endpoint checks:
    if ride.get("status") == 'trip_in_progress':
        raise RideStateError("Cannot cancel after trip has started")
  The actual status string used throughout the codebase is 'in_progress', not
  'trip_in_progress'. This string is never assigned by any endpoint. The guard
  therefore NEVER fires. A rider whose ride is in status 'in_progress' (trip
  actively under way) can successfully call POST /rides/{id}/cancel and will:
  (a) cancel a live trip mid-journey,
  (b) receive no cancellation fee (charged_driver stays 0 because neither the
      driver_arrived branch nor the time-based branch triggers for in_progress),
  (c) leave the driver mid-trip with no payment and no notification.
  The driver earnings for the partial trip are forfeit.
  Reproduction: ride.status = 'in_progress' → POST /rides/{id}/cancel →
  returns {success: true, cancellation_fee: 0}.
Fix: Change the guard to use the correct status string:
    if ride.get("status") == 'in_progress':
        raise RideStateError("Cannot cancel after trip has started")
  Additionally, add a comprehensive guard covering all non-cancellable states:
    NON_CANCELLABLE = {'in_progress', 'completed', 'cancelled'}
    if ride.get('status') in NON_CANCELLABLE:
        raise HTTPException(400, f"Cannot cancel ride in state: {ride.get('status')}")
  Effort: 30 minutes.

[07-3] HIGH | Double Booking — No Active Ride Guard on Client or Server
File: rider-app/store/rideStore.ts:319–353 · backend/routes/rides.py:515–739
Finding: createRide() in rideStore.ts does not check whether currentRide is
  already set before calling POST /rides. The backend create_ride handler
  performs no query to verify whether the rider already has an active ride in
  states ('searching', 'driver_assigned', 'driver_accepted', 'driver_arrived',
  'in_progress'). If the rider double-taps the Book button or navigates back
  and re-submits payment-confirm.tsx before the first request returns, two
  simultaneous rides are created and two drivers are dispatched.
  payment-confirm.tsx uses a `isSubmitting` ref (line 51-53) that prevents
  UI-level double-taps within the same screen mount, but this guard is reset
  on each navigation and is bypassed by page-reload or navigation back.
  This was pre-populated as R-P0-5; the audit confirms neither the store nor
  the backend has the guard. The isSubmitting ref only protects against the
  fastest double-taps, not against navigating back and re-submitting.
Fix: (a) Client — in createRide(), before the API call:
    if (get().currentRide) throw new Error('A ride is already active');
  (b) Server — in create_ride(), after the ban check:
    existing = await db.find_one('rides', {'rider_id': current_user['id'],
      'status': {'$in': ['searching', 'driver_assigned', 'driver_accepted',
                          'driver_arrived', 'in_progress']}})
    if existing:
        raise HTTPException(409, 'You already have an active ride')
  Effort: 2–3 hours (P0-5 already tracked).

[07-4] MEDIUM | hydrateActiveRide() Restores Stale Ride Without Backend Validation
File: rider-app/store/rideStore.ts:584–602
Finding: hydrateActiveRide() reads the last known ride from AsyncStorage and,
  if status is not in TERMINAL_STATUSES, immediately populates currentRide
  and currentDriver from the stale local copy — without calling /rides/active
  to confirm the ride is still live. If the app was closed while a ride was
  in_progress and the ride completed (or was cancelled) while the app was
  shut down, the next cold start will restore the stale 'in_progress' ride
  and route the rider to ride-in-progress.tsx with ghost data. The rider sees
  a live-looking ride screen for a trip that already ended.
  The subsequent fetchActiveRide() call (invoked separately in _layout.tsx)
  will eventually correct this, but there is a visible window of incorrect
  state between hydrateActiveRide() resolving and fetchActiveRide() returning.
  If fetchActiveRide() fails (network unavailable), the ghost state persists
  indefinitely and the rider cannot book a new ride.
  This was pre-populated as R-P2-2; the audit confirms the validation call
  (GET /rides/active) is absent from hydrateActiveRide().
Fix: Validate the stored ride against the backend before setting state:
    hydrateActiveRide: async () => {
      const raw = await AsyncStorage.getItem(ACTIVE_RIDE_KEY);
      if (!raw) return;
      const { currentRide } = JSON.parse(raw);
      if (!currentRide || TERMINAL_STATUSES.has(currentRide.status)) {
        await AsyncStorage.removeItem(ACTIVE_RIDE_KEY);
        return;
      }
      try {
        const live = await api.get('/rides/active');
        if (!live.data?.active || live.data.ride?.id !== currentRide.id) {
          await AsyncStorage.removeItem(ACTIVE_RIDE_KEY);
          return; // stale — do not restore
        }
        if (!get().currentRide) {
          set({ currentRide: live.data.ride, currentDriver: live.data.ride.driver || null });
        }
      } catch {
        await AsyncStorage.removeItem(ACTIVE_RIDE_KEY);
      }
    }
  Effort: 2 hours (R-P2-2 already tracked).

[07-5] MEDIUM | FreeCancelTimer Shows Expired But Does Not Disable Cancel Button
File: rider-app/components/FreeCancelTimer.tsx:54–90 ·
      rider-app/app/driver-arriving.tsx:537–547
Finding: FreeCancelTimer correctly counts down the free-cancel window and
  displays "Cancellation fee: $X" in red when the window expires. However,
  the component is display-only — it emits no callback when the timer reaches
  zero, and the Cancel button in driver-arriving.tsx remains fully enabled and
  shows a fee warning but does NOT visually communicate that the fee will be
  charged. A rider who sees the "Cancellation fee: $3.00" label on the timer
  may still not associate the timer expiry with the Cancel button becoming
  a paid action.
  More critically: there is no onExpire callback from FreeCancelTimer to the
  parent screen. The Cancel button is not disabled; no toast appears saying
  "Free cancel window expired." The only signal is the passive label change
  inside the timer card. A rider who glances at the screen without reading
  the timer text may tap Cancel thinking it is still free.
  Additionally, the handleBack() function in driver-arriving.tsx correctly
  shows "A cancellation fee of $X will be charged" when status=driver_arrived,
  but the fee dialog is NOT triggered before the timer expires for
  status=driver_accepted (the dialog says "free cancel" even when the timer
  has already expired at that status).
Fix:
  (a) Add an onExpire?: () => void prop to FreeCancelTimer. When the timer
      hits zero, call onExpire(). In driver-arriving.tsx, on onExpire, show
      a toast or banner: "Free cancel window expired — cancellation fee now
      applies."
  (b) In handleBack(), when status is driver_accepted AND the free cancel
      window has expired (free_cancel_seconds_remaining === 0), switch to
      the fee-warning dialog instead of the free cancel dialog.
  Effort: 1–2 hours.

[07-6] MEDIUM | driver_timeout Does Not Reset UI to "Searching" State
File: rider-app/hooks/useRiderSocket.ts:111–115
Finding: The driver_timeout WebSocket case at line 113–115 only calls
  fetchRide(rideId). It does NOT call applyRideStatusFromWS to immediately
  set currentRide.status back to 'searching' in the store. The UI continues
  to display the driver card, driver name, and route with the previous
  driver's details while the fetchRide response is in flight. At the 3 s
  poll interval this is a brief inconsistency; but if the poll is in slow
  mode (15 s) or if the network is slow, the rider sees the previous driver's
  details for up to 15 s after the driver timed out. There is also no Alert
  or notification to tell the rider what happened. This was documented in
  finding [06-4] and R-P1-16; re-confirmed here as part of the D07 state
  machine audit.
Fix: In the driver_timeout case, immediately apply the status transition and
  show an alert before fetching:
    case 'driver_timeout':
      if (rideId) {
        applyRideStatusFromWS(rideId, 'searching');  // instant UI update
        Alert.alert('Driver Not Available',
          'Your driver didn\'t respond. Finding another driver…');
        fetchRide(rideId);
      }
      break;
  Effort: 30 minutes (R-P1-16 already tracked).

[07-7] MEDIUM | Ride Status Magic Strings Scattered Across 36 Screens — No Constants
File: rider-app/store/rideStore.ts · rider-app/hooks/useRiderSocket.ts ·
      rider-app/app/ (all 36 screens)
Finding: Status strings 'searching', 'driver_assigned', 'driver_accepted',
  'driver_arrived', 'in_progress', 'completed', 'cancelled' appear as inline
  literals across the entire codebase. The pre-audit baseline noted 78 magic
  ride-status strings. The wrong-status bug in [07-2] ('trip_in_progress'
  instead of 'in_progress') is a direct consequence of this pattern — a typo
  in a string literal created a permanently dead guard. The client state
  machine string 'driver_accepted' matches the backend string; however, no
  TypeScript type enforces this correspondence. A future rename or addition
  of a new status would require a codebase-wide grep to catch all usages.
  This was pre-populated as R-P2-3; confirmed present in every ride-flow file.
Fix: Create rider-app/constants/rideStatus.ts:
    export const RideStatus = {
      SEARCHING: 'searching',
      DRIVER_ASSIGNED: 'driver_assigned',
      DRIVER_ACCEPTED: 'driver_accepted',
      DRIVER_ARRIVED: 'driver_arrived',
      IN_PROGRESS: 'in_progress',
      COMPLETED: 'completed',
      CANCELLED: 'cancelled',
    } as const;
  Replace all inline literals with RideStatus.X references. On the backend,
  define VALID_STATUSES and CANCEL_ALLOWED_STATES as module-level frozensets.
  Effort: 3–4 hours (R-P2-3 already tracked).

────────────────────────────────────────────────────────────────────────────────
PASSES
────────────────────────────────────────────────────────────────────────────────

[07-8] PASS | Cancellation Fee Correctly Applied and Paid Out for driver_arrived
File: backend/routes/rides.py:1356–1412
Finding: cancel_ride_rider correctly detects ride.status == 'driver_arrived'
  (line 1356) and charges a flat $5.00 driver cancellation fee (charged_driver
  = Decimal("5.00")) plus the admin portion. The fee is immediately credited
  to the driver's wallet (lines 1383–1405) with a wallet_transactions record
  and a push notification to the driver. The cancel button in
  driver-arriving.tsx (handleBack, lines 131–148) correctly shows a fee
  warning dialog before proceeding. The fee is calculated server-side from
  app_settings — not hardcoded on the client. The $5.00 driver fee and
  $0.50 admin fee are independent of the client-supplied values.

[07-9] PASS | /rides/{id}/complete Is a Read-Only Endpoint (No State Mutation)
File: backend/routes/rides.py:1893–1902
Finding: rider_complete_ride (POST /rides/{id}/complete) does NOT transition
  the ride to 'completed'. It simply calls get_ride(ride_id) and returns the
  current ride dict. The ride is completed exclusively by the driver-side
  endpoint (/drivers/rides/{id}/complete), which sets status='completed',
  stores the final distance, and processes driver earnings. The rider's
  /complete endpoint is a convenience fetch for the ride-completed.tsx screen.
  No state bypass is possible through this endpoint.

[07-10] PASS | Atomic Driver Claim Prevents Double-Accept Race
File: backend/routes/rides.py:189–201
Finding: match_driver_to_ride uses db_supabase.claim_driver_atomic() to
  atomically claim a driver (sets is_available=False) before assigning them
  to a ride. If two concurrent dispatch calls race, the second claim returns
  None and triggers a fallback loop through remaining candidates. The ride
  assignment UPDATE is only performed after a successful atomic claim.
  This addresses the "two drivers both accept the same ride" CRITICAL pattern
  in the D07 severity guide.

[07-11] PASS | 5-Minute Auto-Cancel Implemented for "searching" Timeout
File: backend/routes/rides.py:698–737
Finding: After ride creation, if the ride returns to 'searching' status,
  asyncio.create_task(ride_search_timeout(ride.id, 300)) is launched. After
  300 s it re-checks the ride; if still 'searching', it cancels the ride,
  notifies the rider via WebSocket and push notification, and logs the event.
  This satisfies the D07 checklist item "5-minute auto-cancel if no driver
  found — notification sent to rider."

[07-12] PASS | Re-Dispatch on Driver Decline Is Handled (Not Fire-and-Forget)
File: backend/routes/rides.py:340–411 (_offer_timeout_handler)
Finding: _offer_timeout_handler waits for the driver to respond. If the ride
  is still in 'driver_assigned' state after timeout_seconds (offer_timeout + 15),
  it releases the driver (sets is_available=True), resets the ride to
  'searching', notifies the rider via WebSocket, and calls
  match_driver_to_ride(ride_id) again. Re-dispatch is fully implemented and
  server-authoritative (fires even if the driver's device crashes or loses
  connectivity). The function handles errors with a try/except and logs
  failures via logger.warning — no silent failure on exception.

[07-13] PASS | Driver Card Hidden Until driver_accepted State — No Pre-Accept Leak
File: rider-app/app/driver-arriving.tsx:450–453
Finding: The driver details card is only rendered when
  currentRide.status === 'driver_accepted' || 'driver_arrived' || 'in_progress'.
  While the ride is in 'driver_assigned' (dispatched but driver hasn't tapped
  Accept), the screen shows a "Finding your driver" ActivityIndicator with no
  driver name, vehicle, or plate. This prevents showing a driver's identity
  before they have committed to the ride.

[07-14] PASS | Cancellation Fee Calculation Uses Server-Side app_settings
File: rider-app/app/driver-arriving.tsx:105–106 · backend/routes/rides.py:898–937
Finding: The cancel fee and free-cancel window are read from
  (currentRide as any)?.cancellation_fee and free_cancel_window_seconds, which
  are injected into the GET /rides/{id} response from app_settings at
  lines 908–937. The old client-side formula (Math.min(5, fare * 0.2)) has
  been replaced. Fee values are server-authoritative.

────────────────────────────────────────────────────────────────────────────────
TASK 07 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     3 |
| MEDIUM          |     4 |
| LOW             |     0 |
| PASS            |     7 |
| RECOMMENDATION  |     0 |
| TOTAL           |    14 |

================================================================================
TASK 08 — PAYMENTS & PCI COMPLIANCE (Dimension 08)
================================================================================
Scope    : rider-app/app/payment-confirm.tsx · rider-app/app/ride-completed.tsx ·
           rider-app/app/manage-cards.tsx · rider-app/store/walletStore.ts ·
           rider-app/store/rideStore.ts · backend/routes/payments.py ·
           backend/routes/rides.py · backend/routes/promo.py ·
           backend/routes/wallet.py · backend/routes/webhooks.py ·
           backend/routes/fare_split.py
Framework: audit-framework/dimensions/08-payments.md
Checks   : Idempotency key · double-tap guard · wallet balance server-side ·
           tip idempotency · SetupIntent / customer_id · promo fare validation ·
           fare-split idempotency · webhook HMAC · raw card data

────────────────────────────────────────────────────────────────────────────────

[08-1] HIGH | /wallet/pay Does Not Validate Amount Against Stored Ride Fare
File: backend/routes/wallet.py:89–91, 146–188
Finding: WalletPayRequest accepts any amount > 0 from the client:
    class WalletPayRequest(BaseModel):
        ride_id: str
        amount: float = Field(..., gt=0)   # no upper bound, no fare cross-check
  The wallet_pay handler checks the rider has sufficient balance but never
  fetches the ride record to confirm the requested debit matches the stored
  ride fare. A client can POST /wallet/pay with ride_id="<valid_id>" and
  amount=0.01 (or any arbitrary amount), and the handler will:
  (a) debit only $0.01 from the wallet instead of the full fare,
  (b) set the ride's payment_status to "paid",
  (c) return success.
  The rider effectively pays a fraction of the fare while the ride is marked
  fully settled. The driver's earnings are credited based on ride creation
  (not on this wallet-pay event), but the operator's ledger shows a fraudulent
  "paid" status at the manipulated amount.
  Contrast: POST /payments/create-intent correctly cross-checks body.amount
  against the stored ride fare (payments.py:72–85).
Fix: Fetch the ride record in wallet_pay and enforce the debit equals the fare:
    ride = await db.find_one("rides", {"id": req.ride_id})
    if not ride or ride.get("rider_id") != current_user["id"]:
        raise HTTPException(404, "Ride not found")
    fare = _d(ride.get("total_fare", 0))
    if _d(req.amount) != fare:
        raise HTTPException(400,
            f"Payment amount {req.amount} does not match ride fare {fare}")
  Additionally add an upper bound consistent with the system maximum fare
  (e.g. le=500) and change the type from float to Decimal at the schema level
  (see also R-P2-23 for the float-to-Decimal migration).
  Effort: 1 hour.

[08-2] HIGH | Tip Is Additive — Rider Can Submit Multiple Tips for the Same Ride
File: backend/routes/rides.py:945–969
Finding: POST /rides/{ride_id}/tip accumulates tip amounts rather than
  setting a one-time tip:
    new_tip = ride.get("tip_amount", 0) + tip_amount
    await db_supabase.update_ride(ride_id, {"tip_amount": new_tip, ...})
  There is no flag or check to prevent a rider from calling this endpoint
  multiple times. Each call adds to the running total, so:
    POST /rides/{id}/tip  { "amount": 5 }  → tip_amount = 5, driver +$5
    POST /rides/{id}/tip  { "amount": 5 }  → tip_amount = 10, driver +$5 again
  ride-completed.tsx does set a local tipSent flag (line 109: if tipSent) that
  prevents re-sending within a single screen session, but this guard is in
  client state only — it is lost on navigation, app backgrounding, or any API
  call made outside the screen. A malicious or buggy client can call the
  endpoint N times and over-tip, generating unbounded driver_earnings credits.
  No wallet debit is made by /tip (the endpoint only updates the ride record);
  the monetary impact depends on whether the driver's payout flow reads
  tip_amount from the ride record. If it does, driver earnings are inflated.
Fix: Add a one-time-tip guard:
  Option A — status field: add a `tip_paid: bool` column; set it to True on
    first tip call and return 409 on subsequent calls.
  Option B — check existing value: if ride.get("tip_amount", 0) > 0, return
    409 "Tip already submitted for this ride."
  Also consider whether /tip and /process-payment's tip_amount path should
  be unified — currently a rider can tip via both routes independently.
  Effort: 30 minutes.

[08-3] HIGH | createRide() Does Not Pass an Idempotency Key — Double Charge on Retry
File: rider-app/store/rideStore.ts:319–353 · backend/routes/rides.py:515–739
Finding: createRide() in rideStore.ts calls POST /rides with no Idempotency-Key
  header. If the network drops between the client sending the request and
  receiving the response, the automatic retry (api client interceptor or user
  re-tap) will create a second ride and initiate a second payment intent. The
  backend create_ride handler has no idempotency cache on the ride-creation
  path — each POST /rides creates a new ride unconditionally.
  payment-confirm.tsx uses an isSubmitting ref (line 51–53) that prevents
  UI-level double-taps within the same screen session, but the Book button's
  disabled prop is tied to the store's isLoading state, not isSubmitting.
  The disabled={isLoading} guard (line 322) is set during the API call but
  resets to false after createRide resolves or rejects — and importantly,
  isLoading (a Zustand state value) does NOT visually disable the button
  during the navigation transition from the error alert back to the screen.
  A network timeout → error alert → user re-taps Book creates two rides.
  This issue was pre-identified as R-P1-7; the audit confirms neither the
  client Idempotency-Key header nor the server-side idempotency cache is
  implemented.
Fix: See R-P1-7 for the full fix prescription. Core fix:
  (a) Client — generate a UUID key before the call and set
      headers: { 'Idempotency-Key': key } on the api.post('/rides', ...) call.
  (b) Backend — store (idempotency_key, user_id, response) in a short-lived
      cache (Redis or DB) and return the cached response for duplicates.
  Effort: 2–3 hours.

[08-4] MEDIUM | Book Button — disabled Prop Tied to isLoading, Not isSubmitting Ref
File: rider-app/app/payment-confirm.tsx:51–54, 319–336
Finding: The Book button uses disabled={isLoading} (line 322), where isLoading
  comes from the rideStore Zustand state (line 30). The isSubmitting ref (line
  51) is the correct guard against double-taps, but it is NOT reflected in the
  button's disabled prop. The button remains visually tappable and does not show
  a spinner when isSubmitting.current is true (only when isLoading is true).
  Between the moment the rider taps "Book" and the moment createRide() sets
  isLoading: true in the store (line 326), there is a brief window (the
  async overhead before the API call begins) during which neither guard blocks
  a second tap. In practice, isSubmitting.current = true runs synchronously
  before await, so the ref guard catches most double-taps. But:
  (a) The button gives no visual feedback (no spinner, no disabled state)
      while the API call is in-flight with isLoading still false.
  (b) A user who navigates away and back gets a fresh screen mount with
      isSubmitting.current = false, bypassing the guard.
Fix: Add local isSubmitting state (useState, not useRef) and use it to disable
  the button and show a spinner:
    const [isSubmitting, setIsSubmitting] = useState(false);
    // In handleBookRide: setIsSubmitting(true) before, setIsSubmitting(false) in finally
    <TouchableOpacity disabled={isSubmitting || isLoading} ...>
  This provides immediate visual feedback and correctly blocks the button until
  the API completes. Effort: 30 minutes.

[08-5] MEDIUM | Promo Discount Calculated Against Client-Supplied Fare — No Server Validation
File: rider-app/app/payment-confirm.tsx:76–77 · backend/routes/promotions.py:60–195
Finding: The promo validation flow:
  1. Client calls: POST /promo/validate  { code, ride_fare: selectedEstimate.total_fare }
  2. Backend calculates discount as a fraction of ride_fare (the client value)
  3. discount_amount is returned and shown on the UI
  The backend never cross-checks ride_fare against a stored ride record. A
  client can supply ride_fare=0.01 when validating, receive a negligible
  discount amount, but then when ride_fare is large, the stored promo usage is
  already recorded. More critically: when ride_fare=0 or is manipulated to be
  very small, a percentage discount yields a near-zero discount_amount that
  inflates the client's displayed "total" less than expected.
  The inverse is also true: submitting ride_fare=10000 inflates the percentage
  discount calculation well above what the actual fare warrants.
  Crucially, the discount is only shown in the UI — the actual ride fare
  deduction must happen during ride creation, but createRide() does not pass
  the promo_code or applied discount to the backend (rideStore.ts:319–353).
  The backend create_ride handler has no promo application logic.
  This means the discount is display-only: the rider is charged the full fare
  regardless of the promo code applied, which is a different severity class
  (broken feature, not a fraud vector). However, the client-controlled
  ride_fare in /promo/validate does mean the minimum-fare eligibility check
  (line 111: if req.ride_fare < min_fare) can be bypassed by supplying a high
  fake fare value.
  This is the issue pre-identified as R-P1-8; confirmed present.
Fix: Two parts:
  (a) validate_promo must accept only a promo code — not a ride_fare. Fare
      should be fetched from the ride record or estimate by ID:
      POST /promo/validate  { code, ride_id }  — backend fetches fare.
  (b) createRide() must pass the applied promo_id to the backend, which
      deducts the discount from the server-side fare during ride creation.
  Effort: 2–3 hours (frontend + backend).

[08-6] MEDIUM | Fare-Split Participant Payment Is Not Idempotent
File: backend/routes/fare_split.py:257–329
Finding: The pay_split_share endpoint checks participant["status"] == "paid"
  before processing (line 271–272: if participant["status"] == "paid": raise 400).
  This is a correct guard. However, there is a TOCTOU (time-of-check/time-of-use)
  race condition: two concurrent requests from the same participant can both
  read status="accepted", pass the check simultaneously, then both update
  the wallet and both mark the status as "paid". Under normal single-device
  usage this race is unlikely, but:
  (a) No database-level atomic guard prevents it (no SELECT...FOR UPDATE or
      conditional update pattern equivalent to the atomic guard in
      process_payment at rides.py:993–1001).
  (b) The wallet deduction (lines 291–304) uses a two-step read-then-write
      without optimistic concurrency: two parallel calls read the same
      balance, both deduct the share, both write back a new balance, resulting
      in one deduction actually taking effect and the other being silently lost.
Fix: Apply the same atomic guard pattern used in process_payment:
    guard = await db.update_one(
        "fare_split_participants",
        {"id": participant_id, "status": "accepted"},   # condition: still accepted
        {"$set": {"status": "processing"}},
    )
    if not guard.modified_count:                         # already paid or processing
        return {"status": "paid", "already_paid": True}
  Then proceed with the wallet deduction and final "paid" status update.
  Effort: 1 hour.

[08-7] PASS | Stripe Webhook Verifies HMAC Signature via construct_event()
File: backend/routes/webhooks.py:25–55
Finding: The stripe_webhook handler:
  (a) Reads stripe_webhook_secret from app_settings (not hardcoded).
  (b) Requires the secret to be present — returns 500 with an error message
      if stripe_webhook_secret is not configured (lines 35–40), rejecting
      all unverified events rather than processing them blindly.
  (c) Calls stripe.Webhook.construct_event(payload, sig_header, webhook_secret)
      (line 50) — this verifies the Stripe-Signature header's HMAC-SHA256 sig.
  (d) Invalid payload → 400 "Invalid payload"; invalid signature → 400
      "Invalid signature". No event is processed without a valid HMAC.
  The HMAC secret is loaded at runtime from the database settings store, not
  from a hardcoded string. Per framework checklist: PASS.

[08-8] PASS | Webhook Event Idempotency via stripe_events Dedup Table
File: backend/routes/webhooks.py:67–88 · backend/db_supabase.py:1084–1100
Finding: claim_stripe_event() is called before any business logic (line 81).
  It inserts (event_id, event_type, processed) atomically using the event_id
  as PRIMARY KEY. A duplicate event_id (Stripe retry) causes a unique-key
  violation, claim_stripe_event returns False, and the handler returns 200
  with {"duplicate": True} without repeating any side effects (line 87–88).
  mark_stripe_event_processed() stamps processed_at after successful handling
  (line 223). Events that fail processing (exception before mark_processed)
  have processed_at=NULL, enabling nightly reconciliation replay.

[08-9] PASS | PaymentIntent Creation Has Idempotency Key
File: backend/routes/payments.py:105–116
Finding: create_payment_intent() constructs an idempotency key:
    idempotency_key = f"ride-{body.ride_id}-{current_user['id']}"  (when ride_id present)
    fallback: f"intent-{current_user['id']}-{amount}"
  The key is passed to stripe.PaymentIntent.create(..., idempotency_key=...).
  A network retry that re-POSTs to /payments/create-intent with the same
  ride_id and user_id will receive the same PaymentIntent from Stripe without
  creating a second charge. Per framework checklist: PASS.

[08-10] PASS | PaymentIntent Amount Cross-Checked Against Stored Ride Fare
File: backend/routes/payments.py:72–85
Finding: When body.ride_id is present, the handler fetches the ride record and
  compares Decimal(str(body.amount)) with Decimal(str(ride.total_fare)). A
  mismatch raises HTTP 400. The comparison uses Decimal on both sides to avoid
  float equality pitfalls. Client cannot inflate/deflate the Stripe charge by
  manipulating the amount field.

[08-11] PASS | Card Add Flow Uses Stripe SetupIntent — Customer ID Server-Derived
File: backend/routes/payments.py:156–185, 353–392 · rider-app/app/manage-cards.tsx:88–113
Finding: On the client, manage-cards.tsx calls Stripe's createPaymentMethod()
  (line 92) using @stripe/stripe-react-native's CardField. Raw PAN/CVC/expiry
  remain inside the native Stripe view and never appear in JavaScript.
  The tokenized payment_method_id is the only value posted to /payments/cards.
  The backend's add_card handler calls get_or_create_stripe_customer() (line
  354) to resolve the customer_id server-side from the authenticated user's
  database record. The client cannot inject a foreign customer_id.
  The endpoint also creates a SetupIntent (line 361–368: stripe.SetupIntent.create
  with confirm=True) to save the card for future off-session use.
  SetupIntent customer ID is derived server-side from the DB lookup — not
  client-supplied. Per framework checklist: PASS.

[08-12] PASS | PCI Guard Rejects Raw Card Fields — Including camelCase Variants
File: backend/routes/payments.py:280–327
Finding: The _RAW_CARD_FIELDS set (lines 280–284) covers all variants required
  by the framework checklist:
    snake_case: card_number, cvv, cvv2, cvc, cvc2, security_code,
                card_security_code, expiry, expiration, expiration_date,
                exp_month, exp_year, card_no
    camelCase:  cardNumber, pan, primary_account_number
  The guard fires before Pydantic model validation (before any logging) and
  raises HTTP 400 without echoing the offending values back. The warning log
  records only the field names, not their values. All required snake_case and
  camelCase variants from the D08 checklist are present.

[08-13] PASS | No Raw Card Data Collected by Any Mobile Screen
File: rider-app/app/manage-cards.tsx · rider-app/app/payment-confirm.tsx ·
      rider-app/app/wallet.tsx
Finding: manage-cards.tsx is the only screen that collects card information.
  It uses Stripe's <CardField> component (line 226) — a native Stripe-hosted
  view. Card number, expiry, and CVC are entered directly into the Stripe
  native layer and are never exposed to JavaScript or React state. The screen
  has no TextInput fields for card_number, cvv, exp_month, or exp_year. The
  cardholder name is the only free-text field (line 244), which is not a PCI
  sensitive value. payment-confirm.tsx and wallet.tsx contain no card-entry
  UI at all. PCI SAQ-A scope is maintained.

[08-14] PASS | Process-Payment Has Atomic Double-Charge Guard
File: backend/routes/rides.py:984–1001
Finding: Before executing any payment logic, process_payment checks if
  ride.payment_status is already "paid" or "processing" (line 985) and returns
  immediately if so (idempotent early return). It then performs an atomic
  conditional update: SET payment_status="processing" WHERE payment_status NOT
  IN ("paid", "processing"). If modified_count == 0, another concurrent request
  won the race and the current call returns safely. The wallet debit proceeds
  only after this guard succeeds.

[08-15] PASS | Wallet Pay Validates Sufficient Balance Server-Side
File: backend/routes/wallet.py:146–188
Finding: wallet_pay fetches the wallet balance with Decimal arithmetic
  (lines 154–158: old_balance = _d(wallet.balance); if old_balance < debit_amount:
  raise 400 "Insufficient wallet balance"). The check runs server-side in
  Python with ROUND_HALF_UP Decimal before any debit is applied. The client
  balance display is not trusted. Balance cannot go negative.

[08-16] PASS | Fare-Split Total Derived from Server-Side Ride Fare
File: backend/routes/fare_split.py:56–120
Finding: create_fare_split fetches total_fare from the ride record in the DB:
    total_fare = float(ride.get("grand_total") or ride.get("total_fare", 0))
  The split amount is calculated as total_fare / split_count server-side.
  The client does not supply the fare to be split — only the ride_id and
  participant phone numbers. A rider cannot manipulate their split amount by
  crafting the request payload.

[08-17] LOW | Stripe Test Key sk_test_* Likely Present in App Settings (Dev Mode)
File: backend/routes/payments.py:61 · backend/routes/webhooks.py:32
Finding: Both payment and webhook handlers read stripe_secret_key from
  app_settings at runtime. No sk_test_* value is committed in source files
  (audit previously confirmed — [03-17] PASS). However, the development
  app_settings in the database will carry a sk_test_* key during testing.
  Per audit ground rules: Stripe test keys (sk_test_*) → LOW severity only.
  Production-readiness gate: ensure app_settings.stripe_secret_key and
  stripe_webhook_secret are set to live values (sk_live_*, whsec_*) before
  production launch via an env-gated settings migration.
  Effort: Production config task — 30 minutes.

────────────────────────────────────────────────────────────────────────────────
TASK 08 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     3 |
| MEDIUM          |     3 |
| LOW             |     1 |
| PASS            |     9 |
| RECOMMENDATION  |     0 |
| TOTAL           |    16 |

================================================================================
TASK 09 — TEST COVERAGE (Dimension 09)
================================================================================

[09-1] HIGH | No Jest Coverage Threshold — Coverage Can Drop to Zero Undetected
File: rider-app/jest.config.js
Finding: jest.config.js sets `preset: 'jest-expo'` and configures
  `setupFilesAfterEnv`, `testPathIgnorePatterns`, `transformIgnorePatterns`, and
  `moduleNameMapper` — but defines no `coverageThreshold` block whatsoever.
  The CI job runs `yarn test --ci --coverage` (ci.yml:201) and uploads the report
  to Codecov. However, because no threshold is configured in jest.config.js, Jest
  will always exit 0 regardless of coverage percentage. A PR that deletes all tests
  or introduces dead paths would pass CI silently.
  The D09 framework checklist requires: `lines ≥ 70, functions ≥ 60`.
  The backend pytest job correctly enforces `--cov-fail-under=70` (ci.yml:79);
  the rider-app Jest job has no equivalent gate.
Fix: Add a coverageThreshold block to rider-app/jest.config.js:
    module.exports = {
      // ...existing config...
      coverageThreshold: {
        global: {
          lines: 70,
          functions: 60,
          branches: 60,
          statements: 70,
        },
      },
    };
  Effort: 15 minutes.

[09-2] HIGH | rideStore.test.ts Missing: hydrateActiveRide, Double-Booking Guard,
             Cancel-After-driver_arrived
File: rider-app/store/__tests__/rideStore.test.ts
Finding: The test file covers createRide (happy path + missing-details throw),
  cancelRide (happy path only), startRide, completeRide, clearRide, and recent-
  searches helpers. Three critical paths are entirely absent:

  (a) hydrateActiveRide — the cold-start path that reads AsyncStorage, hits
      /rides/active, and routes the user into an active-ride screen. A bug here
      (R-P2-2: stale ride not revalidated) is confirmed unfixed and untested.

  (b) Double-booking guard — createRide() has no idempotency key and no
      optimistic dedup guard (R-P1-21). There is no test that calls createRide()
      twice concurrently or verifies that a second in-flight call is rejected
      while the first is pending (isLoading flag).

  (c) cancelRide called when currentRide.status === 'driver_arrived' — the
      backend guard uses the wrong status string ('trip_in_progress' instead of
      'in_progress', R-P1-18) and the frontend has no cancel-fee warning. The
      existing cancelRide test only covers `driver_accepted` status; there is no
      test asserting that a cancel during driver_arrived shows a fee warning or
      is blocked client-side.

  Together these gaps mean the two highest-severity store bugs confirmed in this
  audit (R-P1-18, R-P1-21) have no regression coverage.
Fix: Add three describe blocks:
  • 'hydrateActiveRide' — test: valid live ride → sets currentRide; stale ride
    (backend returns different id) → clears AsyncStorage; network error → clears.
  • 'createRide double-submit guard' — set isLoading: true, verify createRide()
    returns without calling api.post a second time.
  • 'cancelRide from driver_arrived' — mock status 'driver_arrived', verify
    that the cancellation flow either shows a fee alert or is rejected.
  Effort: 3–4 hours.

[09-3] HIGH | rideStore.ws.test.ts Missing: driver_timeout Re-dispatch, ride_cancelled
             Event, WS/Poll Race Condition
File: rider-app/store/__tests__/rideStore.ws.test.ts
Finding: The file tests updateDriverLocation, applyRideStatusFromWS, addChatMessage,
  and clearRide. Three WebSocket scenarios that directly correspond to known bugs
  or UX gaps are absent:

  (a) driver_timeout re-dispatch — useRiderSocket's driver_timeout case calls
      fetchRide() (R-P1-16). The store has no test asserting that when
      applyRideStatusFromWS is called with a driver_timeout payload, the store
      transitions back to 'searching' status and the currentDriver is cleared.

  (b) ride_cancelled by backend — if the backend cancels a ride (driver no-show,
      system timeout), a WS message is pushed. There is no test verifying that
      the ride store handles a 'ride_cancelled' WS event by clearing currentRide
      and currentDriver.

  (c) WS update vs. poll race — R-P2-29 documents a confirmed race where a stale
      poll response overwrites a fresher WS driver position. No test exists that
      calls applyRideStatusFromWS (updating driver lat/lng) followed immediately
      by a fetchRide() mock returning older coordinates, and asserts that the
      fresher WS coordinates are preserved.

  The driver_timeout and ride_cancelled gaps are especially high-impact: they are
  paths that real users hit when a driver fails to respond or cancels, and they
  have zero automated coverage.
Fix: Add three test cases:
  • driver_timeout: applyRideStatusFromWS('ride_1', 'searching') after a
    driver_timeout WS payload → currentDriver becomes null.
  • ride_cancelled: applyRideStatusFromWS('ride_1', 'cancelled') → clearRide
    called (currentRide null, currentDriver null).
  • Race: updateDriverLocation(51.5, -105.5); then fetchRide() mock returns
    {lat: 51.0, lng: -105.0}; assert final driver lat === 51.5 (WS wins).
  Effort: 2–3 hours.

[09-4] HIGH | walletStore.test.ts Missing: Insufficient Balance Rejection,
             Tip Idempotency
File: rider-app/store/__tests__/walletStore.test.ts
Finding: The file tests fetchWallet (success + error), topUp (success + error),
  fetchTransactions (two variants), createFareSplit (success), cancelFareSplit,
  and clearError. Two safety-critical wallet paths are absent:

  (a) Insufficient balance rejection — payWithWallet (which maps to POST
      /wallet/pay) is listed in the file's header comment ("Covers: fetchWallet,
      topUp, payWithWallet, ...") but no describe('payWithWallet') block exists.
      In particular, there is no test that mocks a 400 response with "Insufficient
      wallet balance" and verifies the store surfaces this error to the UI.
      R-P1-19 (wallet pay does not validate amount vs. fare) is a confirmed HIGH
      finding; without a payWithWallet test the regression surface is invisible.

  (b) Tip idempotency — rateRide() and addTip() store actions have no tests at
      all. R-P1-20 (additive tip accumulation) is a confirmed HIGH finding. There
      is no test verifying that a second tip submission is rejected client-side
      (e.g., tipSent flag) or that the store surfaces a 409 conflict from the
      backend correctly.

Fix: Add two describe blocks:
  • 'payWithWallet' — success path (balance deducted); 400 insufficient balance
    (error surfaced); 400 amount mismatch (error surfaced).
  • 'addTip' — first submission succeeds; second submission with same rideId
    receives 409 → error state set; verify store does not double-count the tip.
  Effort: 2 hours.

[09-5] MEDIUM | Fare Split, Scheduled Rides, Promo Validation, and Offline Queue
              Not Covered in Any Unit Test
File: rider-app/store/__tests__/ (all three files)
Finding: Four store subsystems have zero unit-test coverage:

  (a) Fare split lifecycle — walletStore.test.ts tests createFareSplit and
      cancelFareSplit (both happy paths), but there is no test for: participant
      payment confirmation (pay_split_share), split expiry, or the R-P2-34
      double-payment TOCTOU race scenario.

  (b) Scheduled rides — rideStore has fetchScheduledRides() and cancelScheduledRide()
      actions (rideStore.ts:532, 542). Neither is tested. The scheduled-ride state
      (scheduledRides array) is zeroed in resetStore() but never exercised.

  (c) Promo validation — rideStore has applyPromo() and fetchAvailablePromos()
      actions. The store's appliedPromo field is zeroed in resetStore() but
      applyPromo() is not tested. Given R-P1-8 (promo discount not validated
      server-side) and R-P2-33 (minimum fare bypass), client-side promo handling
      should have explicit coverage.

  (d) Offline queue — rideStore.ts:280 defines syncOfflineRequests() which replays
      queued create_ride calls on reconnect. R-P2-1 identifies this as covering
      only ride creation (cancel, rate, tip are dropped). There is no test for
      syncOfflineRequests() at all — neither the current narrow behaviour nor the
      gap.

  Severity: MEDIUM (not individually critical, but collectively mean that four
  significant store subsystems have zero regression safety net before beta).
Fix: Add test coverage for:
  • fetchScheduledRides + cancelScheduledRide (rideStore)
  • applyPromo / fetchAvailablePromos (rideStore)
  • syncOfflineRequests — verify pending create_ride is replayed; verify that
    cancel/rate/tip queue items are noted as not-replayed (or add the replay).
  • fareSplit payment confirmation path (walletStore)
  Effort: 3–4 hours.

[09-6] HIGH | E2E ride-booking.spec.ts Does Not Cover: Rating, Tip, or Explicit
             Screen Assertions
File: rider-app/e2e/ride-booking.spec.ts
Finding: The spec walks five ride-status stages (searching → driver_assigned →
  driver_arrived → in_progress → completed) by seeding localStorage with auth
  and mocking /rides/active. However:

  (a) Rating and tip steps are absent — the D09 checklist requires the E2E flow
      to cover: book → driver arriving → in-progress → completed → rate → tip.
      After 'completed' the test ends. Neither rate-ride nor the rating section
      inside ride-completed.tsx is exercised in E2E.

  (b) The assertion after each stage is only `expect(page.locator('body'))
      .toBeVisible()` — this confirms the page has not crashed but not that the
      correct screen or UI elements are rendered. No `assertVisible` equivalent
      (getByRole, getByText, getByTestId) is used in the ride-booking spec.

  (c) The test seeds auth and mocks backend once per stage iteration (five
      `seedAuthedSession` + `mockBackend` calls in a loop with page.goto('/')
      per iteration) rather than simulating a single ride session through all
      transitions. This means the test never validates that state transitions
      between screens work correctly in a continuous session.

  Severity: HIGH per D09 framework ("E2E covers only login — no ride flow" maps
  closest, since the flow is present but shallow: no UI assertions and no
  rating/tip step).
Fix:
  • Add a sixth stage for 'rate' — mock /rides/{id}/rate endpoint, assert
    that a rating UI element is visible (e.g., page.getByTestId('star-rating')).
  • Add a seventh stage for 'tip' — verify tip button is visible and tappable.
  • Replace bare `body.toBeVisible()` with at least one screen-specific
    assertion per stage (e.g., getByText('Finding your driver') for searching,
    getByText("Your driver is on the way") for driver_assigned, etc.).
  • Convert the loop-per-stage pattern into a sequential single-session test that
    uses mockBackend({ rideStatusSequence: stages }) to simulate real transitions.
  Effort: 4–6 hours.

[09-7] PASS | All Unit Test Files Mock the API Client — No Real HTTP Calls
File: rider-app/store/__tests__/rideStore.test.ts,
      rider-app/store/__tests__/rideStore.ws.test.ts,
      rider-app/store/__tests__/walletStore.test.ts
Finding: All three unit-test files open with `jest.mock('@shared/api/client', ...)`
  before importing any store. The mock replaces the shared Axios client with
  Jest spy functions (post, get, put, patch, delete). No real HTTP calls are
  possible in Jest context. AsyncStorage is similarly mocked in rideStore tests.
  Supabase and Firebase are not directly imported by the store test files; no
  mock is needed. No test file references a live backend URL or reads
  EXPO_PUBLIC_BACKEND_URL at test time.

[09-8] PASS | E2E Tests Use Playwright page.route() to Intercept All Backend Calls
File: rider-app/e2e/fixtures.ts, rider-app/playwright.config.ts
Finding: mockBackend() registers a page.route('**/api/v1/**', ...) handler that
  intercepts every backend request and returns canned JSON. Google Maps and Firebase
  are stubbed via page.addInitScript before any app script runs. The Playwright
  config confirms no external base URL is mandated (defaults to localhost:3002).
  The route handler returns a default `{}` for any unrecognised path, preventing
  network leakage. CI retries (2 retries on failures) are configured but flaky
  tests are not silently suppressed — they are retried and reported in the GitHub
  Actions reporter.

[09-9] MEDIUM | No Component Tests Exist — Zero Coverage for Critical UI Components
File: rider-app/ (no __tests__ directory outside store/)
Finding: The D09 checklist requires component tests for critical UI:
  RideOfferPanel, ActiveRidePanel, TripCompletedPanel. Searching the repository
  finds only three test files, all in rider-app/store/__tests__/. No component-
  level test files exist anywhere in rider-app/app/, rider-app/components/, or
  rider-app/hooks/. The useRiderSocket hook — which dispatches every WebSocket
  event — is not tested at the hook level. This means a regression in how a
  WebSocket event is translated into a store dispatch would be invisible until
  a manual E2E run or production failure.
Fix: Add @testing-library/react-native component tests for at minimum:
  • RideStatusPanel / ride-searching state (renders "Finding your driver" text)
  • ActiveRidePanel (renders driver name, ETA, cancel button)
  • TripCompletedPanel (renders fare, rating stars, tip buttons)
  • useRiderSocket hook test using a fake WebSocket — verify that
    driver_location_update, driver_timeout, ride_cancelled each correctly
    dispatch to the store.
  Effort: 1–2 days.

[09-10] MEDIUM | No Snapshot Tests — But Also No Explicit Rationale to Exclude Them
File: rider-app/jest.config.js, rider-app/store/__tests__/
Finding: The D09 checklist flags "snapshot tests without a clear rationale" as
  a concern (snapshots rot quickly). The rider-app has zero snapshot tests, which
  avoids the snapshot-rot problem but also means there is no golden-render check
  for any screen. This is not a bug, but combined with the absence of component
  tests ([09-9]) it means no automated check exists for any rendered UI state.
  Note: this finding is MEDIUM only in conjunction with [09-9] — if component
  tests are added, snapshot-free is perfectly acceptable.

[09-11] LOW | CI Does Not Run E2E Tests in the Rider-App Test Job
File: .github/workflows/ci.yml:199–209
Finding: The `rider-app-test` CI job runs `yarn test --ci --coverage` (Jest only).
  E2E Playwright tests (`yarn test:e2e`) are run in a separate job
  (`playwright-smoke`, ci.yml:384) that depends on `rider-app-test` and uses an
  `expo export` build. E2E tests are therefore not blocking — they run in a
  downstream job and a Playwright failure does not prevent the `rider-app-test`
  job from marking green. Additionally, ci.yml:2 (`retries: process.env.CI ? 2 : 0`
  in playwright.config.ts) means Playwright retries twice before reporting failure,
  which can mask genuine flakiness in the smoke suite.
  Severity: LOW (E2E do run on every PR; this is a depth/visibility gap, not a
  missing gate).

────────────────────────────────────────────────────────────────────────────────
TASK 09 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     4 |
| MEDIUM          |     3 |
| LOW             |     1 |
| PASS            |     2 |
| RECOMMENDATION  |     0 |
| TOTAL           |    10 |

================================================================================
TASK 10 — ERROR HANDLING & RESILIENCE (Dimension 10)
================================================================================

[10-1] MEDIUM | fetchEstimates() Failure: Empty Screen, No Retry UI
File: rider-app/app/ride-options.tsx:71–84, 406–495
      rider-app/store/rideStore.ts:222–242
Finding: When fetchEstimates() fails, rideStore sets isLoading=false and
  error=error.message (rideStore.ts:240–241) but the ride-options.tsx screen
  never reads the error field from the store. The render logic is:
    • isLoading=true  → ActivityIndicator + "Finding best rides..."
    • isLoading=false, estimates=[]  → empty ScrollView (nothing rendered)
  The estimates array stays empty, the footer "Confirm" button is hidden
  (line 498: !isLoading && !allUnavailable && estimates.length > 0), and no
  error message or retry button is shown. The user sees a blank white panel
  with no explanation and no way to recover except pressing the back button
  and trying again from scratch.
  The error state in the store (rideStore.error) is also never cleared between
  navigations, so a second fetch on the same mount doesn't reset it either.
Fix: Read useRideStore().error and, when estimates.length===0 && !isLoading &&
  error, render an error card with message and a "Retry" button that calls
  fetchEstimates(). Clear the error on mount with clearError().
  Effort: 1 hour.

[10-2] PASS | createRide() Network Failure: Button Disables and Error Displays
File: rider-app/app/payment-confirm.tsx:51–68, 319–336
Finding: payment-confirm.tsx uses an isSubmitting ref (line 51) as a guard —
  handleBookRide() returns immediately if isSubmitting.current is true.
  On success the flow navigates away; on error the catch block (line 63–65)
  calls setAlertState({ variant: 'danger', ... }) which renders a CustomAlert
  with the error message. The finally block (line 65–67) clears the ref so the
  button becomes active again.
  The book button shows an ActivityIndicator when isLoading=true (line 325) and
  the button is disabled={isLoading} (line 321). This means: (a) spinner shows
  during the in-flight request, (b) error message appears after failure, and
  (c) the button re-enables so the user can retry.
  One gap: the button uses disabled={isLoading} (store-level flag) rather than
  disabled={isSubmitting.current}. The isSubmitting ref prevents re-entry but
  the button is not visually disabled while isSubmitting is true and isLoading
  is still false (store sets isLoading before the API call, so this is usually
  fine, but there is a brief window between the guard check and set({isLoading:
  true}) where a rapid double-tap could slip through). This exact gap was
  already flagged as R-P2-32. No new finding raised here.
  Overall verdict: PASS — error is shown and button re-enables after failure.

[10-3] HIGH | triggerEmergency() Swallows Errors — Rider Not Notified on Failure
File: rider-app/store/rideStore.ts:442–453
Finding: The triggerEmergency action catches all errors and logs them to the
  console but deliberately does not notify the user (the comment reads "Even if
  API fails, we don't throw to not block the local 911 UI flow"). This design
  choice means a rider whose SOS call fails — due to a network outage, timeout,
  or backend error — receives no indication that emergency services were NOT
  notified through the app.
  The caller in ride-in-progress.tsx (line 111) calls triggerEmergency() then
  Linking.openURL('tel:911') immediately, so the phone-dial path does work
  independently. However, the backend emergency path also sends push
  notifications to emergency contacts, records the GPS coordinates, and flags
  the ride for live monitoring. All of that silently fails if the API call
  fails, and the rider is not told that only the direct 911 call went through.
  This is exactly the pre-populated finding R-P0-1 in rider-P0-critical-fix-
  now.md — confirming it is still present and unfixed.
  The correct fix (as documented in R-P0-1) is to add an Alert in the catch
  block: "Emergency alert may not have reached our team — please call 911
  directly" with a tel:911 button. The throw should then re-raise so the caller
  can clean up if needed.
Fix: Catch block must show Alert.alert() with 911 fallback link. See R-P0-1
  for the full code prescription.
  Effort: 1 hour.

[10-4] MEDIUM | Offline Queue Handles Only create_ride — cancel, rate, tip Silently Dropped
File: rider-app/store/rideStore.ts:280–317
Finding: syncOfflineRequests() only processes entries where request.type ===
  'create_ride' (line 294). All other types fall into the implicit else branch
  and do nothing. This was already identified in R-P2-1.
  Confirming with code inspection:
  • cancelRide() (line 373): calls api.post directly — no offline queue write
  • rateRide() (line 429): calls api.post directly — no offline queue write
  • completeRide() (line 415): calls api.post directly — no offline queue write
  • triggerEmergency() (line 442): calls api.post directly — no offline queue write
  None of these actions write to the 'offline_queue' AsyncStorage key before
  attempting the API call. They simply fail silently if the request throws
  (cancelRide sets error, rateRide re-throws but the caller in ride-completed
  catches and shows an alert, completeRide just sets error). So the queue
  restriction to create_ride is moot in practice — the other actions never
  get queued in the first place, making them silently lost when offline.
  A rider who taps "Cancel", "Rate", or "Submit Tip" while offline gets either
  an error alert (rateRide) or nothing (cancelRide, completeRide). The queue
  defined in the spec comment ("Add other request types here as needed") is
  a dead stub.
  Severity: MEDIUM (confirmed prior finding R-P2-1; not raising a new P2).
Fix: See R-P2-1 for the full fix prescription. Effort: 3–4 hours.

[10-5] MEDIUM | Cold-Start Stale Ride: hydrateActiveRide() Does Not Validate Against Backend
File: rider-app/store/rideStore.ts:584–602
      rider-app/app/_layout.tsx (cold-start flow)
Finding: hydrateActiveRide() (rideStore.ts:584) reads the cached ride from
  AsyncStorage and restores it to the store IF the status is not in
  TERMINAL_STATUSES (completed, cancelled, failed). It does NOT make an API
  call to validate that the ride is still active on the backend.
  If the backend has moved the ride to a terminal state while the app was
  closed (e.g. ride was cancelled by the driver, or timed out), the stale
  non-terminal status in AsyncStorage will cause the store to restore the
  phantom ride. The layout's route-guard or individual screens (driver-arriving,
  ride-in-progress) then render with stale data until the next poll interval
  (15 seconds) corrects the state. During that window the rider sees a ghost
  active-ride screen.
  There is no 404 handling either: _layout.tsx calls hydrateActiveRide() and
  fetchActiveRide() in sequence. fetchActiveRide() calls GET /rides/active and
  will correct the state if the ride is gone — but only after the layout has
  already rendered. The issue is the initial flash of the wrong screen, not a
  permanent stuck state.
  Note: this is confirmed prior finding R-P2-2. However the specific question
  was: "if /rides/active returns 404, does hydrateActiveRide() clear the stale
  state and route to home?" — the answer is that hydrateActiveRide() itself
  never calls the backend, so it cannot detect the 404. The correction only
  happens in the subsequent fetchActiveRide() call which returns null on any
  error. That null return does not explicitly clear the store state that was
  just populated by hydrateActiveRide() — the store retains currentRide until
  fetchActiveRide()'s success path overwrites it. So a 404 from /rides/active
  leaves currentRide set to the stale hydrated data indefinitely (there is no
  "clear if null returned" logic in fetchActiveRide).
  This is worse than previously documented: the stale ride persists until a
  screen-level status check transitions the UI.
Fix: In fetchActiveRide(), when the response indicates no active ride (or on
  404), explicitly call clearRide() to remove the stale hydrated state.
  Or add an API call inside hydrateActiveRide() before restoring state. See
  R-P2-2 for the prescribed fix.
  Effort: 2 hours.

[10-6] PASS | completeRide() Idempotency: Backend Handles Duplicate Gracefully
File: rider-app/store/rideStore.ts:415–427
      backend/routes/rides.py:1893–1902
Finding: POST /{ride_id}/complete is a read-only endpoint — it fetches and
  returns the current ride state (line 1901: "Return the current ride state").
  It does NOT perform a state mutation. The driver-side endpoint performs the
  actual status transition to 'completed'; the rider /complete path only reads
  the result. Repeated calls from the rider client will simply return the ride
  object each time without any side effects, duplicate charges, or conflicts.
  The client-side completeRide() action (rideStore.ts:415) calls this endpoint
  then calls AsyncStorage.removeItem(ACTIVE_RIDE_KEY). Multiple calls will each
  attempt the removeItem (idempotent) and set currentRide to the returned data.
  No spinner-forever risk: the catch block (line 424) sets isLoading to false
  via the error path... wait — there is no isLoading toggle in completeRide()
  at all. The action does not call set({isLoading: true}) before the request or
  set({isLoading: false}) after. The absence of isLoading management means the
  book button on payment-confirm (disabled={isLoading}) is not affected here.
  Idempotency verdict: PASS — the endpoint is safe to call multiple times.
  Minor note: completeRide() has no isLoading guard, so the calling screen
  must handle its own button-disable state. This is tracked separately.

[10-7] MEDIUM | ErrorBoundary at Root Only — Major Ride Screens Unprotected
File: rider-app/app/_layout.tsx:302, 329
      rider-app/app/driver-arriving.tsx (no ErrorBoundary)
      rider-app/app/ride-in-progress.tsx (no ErrorBoundary)
      rider-app/app/ride-completed.tsx (no ErrorBoundary assumed)
Finding: ErrorBoundary is applied only at the root layout level — once in the
  loading state shell (line 302) and once wrapping the full Stack in
  RootLayoutInner (line 329). No individual screen in the ride flow is wrapped
  in its own ErrorBoundary.
  The dimension checklist requires: "Every major screen is wrapped in an
  ErrorBoundary (not just root)." The driver-arriving and ride-in-progress
  screens are explicitly named in the audit brief.
  The root ErrorBoundary shows "Something went wrong" with a "Try Again" button
  (shared/components/ErrorBoundary.tsx:39) — this is a full-screen takeover
  that replaces the entire navigation stack, not just the failing screen. A
  JavaScript error thrown inside driver-arriving (e.g. from a failed map
  render, bad data from WS event, or null driver coordinates) will kill the
  entire app UI rather than showing a screen-level fallback that preserves
  navigation to other tabs.
  During an active ride, this means a single render error on the map puts the
  rider in a dead app state with no navigation to the home screen. The "Try
  Again" button resets the ErrorBoundary state which re-mounts the entire app
  — potentially losing ride context (though the store persists via Zustand).
  The ErrorBoundary does call captureException() (line 84), so crashes are
  logged to the error reporting service — that part is PASS.
Fix: Wrap driver-arriving, ride-in-progress, ride-completed, and driver-arrived
  individually with <ErrorBoundary> using a screen-specific fallback that shows
  a retry button AND a "Go Home" navigation option that calls clearRide() so
  the rider is not trapped in a stale ride state.
  Effort: 2 hours.

[10-8] PASS | 401 During Active Ride: Silent Token Refresh via Queued Refresh Deduplication
File: shared/api/client.ts:153–218
Finding: The API client implements a full token refresh flow with concurrent-
  request deduplication. On a 401 response, handleApiError() (line 153):
  1. Checks that _refreshCallback is registered and that a retry function is
     available (line 155).
  2. Deduplicates concurrent refresh calls via _refreshPromise (line 159–163)
     — multiple simultaneous requests that all hit 401 will share a single
     refresh attempt rather than flooding the auth server.
  3. If the refresh succeeds, the original request is retried automatically
     with the new in-memory token (line 165).
  4. Only if the refresh fails does the client fall through to log the 401 and
     call authStore.logout() which redirects to /login (lines 194–213).
  The 15-second request timeout is configured globally (REQUEST_TIMEOUT=15000,
  line 12), satisfying the dimension requirement.
  The error log ring buffer is bounded at 50 entries (MAX_ERROR_LOG=50, line
  139), satisfying the accumulation requirement. Entries store only message,
  status, method, url, request_id, and exception_type — not full response
  bodies (line 176–185). PASS on log contents.
  The 401 retry queue correctly avoids recursion: it checks url !== '/auth/
  refresh' (line 155) to prevent infinite retry loops on the refresh endpoint
  itself.
  4xx errors are not automatically retried — only 401 triggers the one-time
  refresh+retry pattern. Other 4xx errors throw immediately.
  Overall: the live ride map screen is not disrupted during a silent token
  refresh because the client retries the original request transparently. The
  rider sees no error UI unless the refresh itself fails (which triggers logout
  — the correct behavior for a fully expired session).

────────────────────────────────────────────────────────────────────────────────
TASK 10 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     1 |
| MEDIUM          |     4 |
| LOW             |     0 |
| PASS            |     3 |
| RECOMMENDATION  |     0 |
| TOTAL           |     8 |

================================================================================
TASK 11 — SECURITY HEADERS, CORS & RATE LIMITING (Dimension 11)
================================================================================
Scope    : backend/core/middleware.py · backend/core/config.py ·
           backend/utils/rate_limiter.py · backend/routes/rides.py ·
           backend/routes/promotions.py · rider-app/app.config.ts ·
           rider-app/app/_layout.tsx
Framework: audit-framework/dimensions/11-security-headers-cors.md
Checks   : CORS allowlist · wildcard · security headers · rate limiting ·
           deep link validation · Firebase App Check · CORS on error responses

────────────────────────────────────────────────────────────────────────────────

[11-1] HIGH | spinr.app Origin Not in CORS Allowlist — Rider Web App Cannot Call API
File: backend/core/config.py:47
      backend/core/middleware.py:307–313
Finding: The ALLOWED_ORIGINS default and the hardcoded always_allowed list
  together define the full CORS origin set. The hardcoded list is:
    ["https://spinr-admin.vercel.app", "http://localhost:3000", "http://localhost:3001"]
  The default ALLOWED_ORIGINS env var is:
    "http://localhost:3000,http://localhost:8081,http://localhost:19006"
  Neither list includes https://spinr.app or https://spinr-track.app.
  The rider mobile app (React Native, not web) is not affected because it does
  not use browser CORS. However, the app.config.ts intentionally registers
  applinks:spinr.app and applinks:spinr-track.app as associated domains
  (app.config.ts:37–39) — meaning spinr.app hosts a web landing page or
  web app that links into the native app. Any web component on spinr.app
  that calls the backend API (e.g. the live-tracking page at
  spinr-track.app/{token} or a web booking flow) will be rejected with a
  CORS error.
  The backend serves a public unauthenticated /track/{share_token} endpoint
  (rides.py:1198). If spinr-track.app hosts a web page that calls this
  endpoint in the browser, every request will fail with "CORS policy: No
  'Access-Control-Allow-Origin' header is present."
Fix: Add https://spinr.app and https://spinr-track.app to the ALLOWED_ORIGINS
  environment variable for the production deployment. If these domains host no
  web-to-backend calls today, document the gap and add them before any web
  component is shipped.
  Additionally, document the complete expected production allowlist in the
  .env.example file so operators know what to set:
    ALLOWED_ORIGINS=https://spinr.app,https://spinr-track.app,https://spinr-admin.vercel.app
  Effort: 30 minutes.

[11-2] MEDIUM | /rides/{id}/cancel Missing Rate-Limit Decorator
File: backend/routes/rides.py:1314–1315
Finding: POST /rides/{ride_id}/cancel has no SlowAPI rate-limit decorator.
  The create_ride endpoint (line 515–516) correctly applies
  @ride_request_limit (10/minute). The cancel endpoint has no equivalent:
    @api_router.post("/{ride_id}/cancel")
    async def cancel_ride_rider(ride_id: str, current_user: dict = ...):
  An authenticated attacker (or a client bug that retries on network error)
  can flood the cancel endpoint without throttling, causing repeated
  cancellation-fee charge attempts or backend load from repeated DB reads,
  state machine evaluations, and potential Stripe API calls.
  Dimension 11 checklist: "Rate limits configured for all sensitive
  endpoints." Cancel is a financial-state endpoint.
Fix: Add @ride_request_limit (or a separate cancel_limit = default_limiter.limit("5/minute"))
  directly above the cancel endpoint, analogous to create_ride:
    @api_router.post("/{ride_id}/cancel")
    @ride_request_limit
    async def cancel_ride_rider(request: Request, ride_id: str, ...):
  (Note: SlowAPI requires the first parameter to be named `request` of type
  starlette.requests.Request when using a decorator — add `request: Request`
  as done in create_ride:517–524.)
  Effort: 30 minutes.

[11-3] MEDIUM | /promo/validate Missing Rate-Limit Decorator — Code Enumeration Possible
File: backend/routes/promotions.py:60–64
Finding: POST /promo/validate has no SlowAPI decorator:
    @api_router.post("/validate")
    async def validate_promo(req: ValidatePromoRequest, current_user: dict = ...):
  The endpoint performs a live database lookup for each submitted promo code and
  returns distinct error messages per failure case (invalid code → 404;
  expired → 400; usage limit → 400; user eligibility → 400). A malicious
  caller can submit sequential or dictionary guesses against this endpoint to
  enumerate valid promo codes (a 404 response means the code does not exist;
  a 400 means it does). With no rate limit, hundreds of guesses per second are
  possible. Short promo codes (e.g. "SUMMER25", "FALL10") are low-entropy and
  enumerable within minutes.
  The Dimension 11 checklist explicitly names promo validation as a target for
  rate limiting to prevent code enumeration.
Fix: Add a rate-limit decorator. A dedicated promo_limit at 10/minute per user
  (not per IP, since the user is authenticated) is appropriate:
    promo_validate_limit = default_limiter.limit("10/minute")
    @api_router.post("/validate")
    @promo_validate_limit
    async def validate_promo(request: Request, req: ValidatePromoRequest, ...):
  Additionally, consider returning a generic "Promo code not found or not
  applicable" message instead of distinct 404 vs 400 responses to make
  enumeration harder.
  Effort: 30 minutes.

[11-4] MEDIUM | Referrer-Policy Value Is Non-Standard — Violates D11 Checklist
File: backend/core/middleware.py:99
Finding: _BASE_SECURITY_HEADERS sets:
    "Referrer-Policy": "strict-origin-when-cross-origin"
  The Dimension 11 checklist requires:
    "Referrer-Policy: no-referrer"
  "strict-origin-when-cross-origin" sends the origin component of the URL
  on cross-origin requests (but drops the path). For an API backend that
  serves no HTML and whose clients are mobile apps, "no-referrer" is stricter
  and has no functional cost — there is no legitimate use case for the backend
  to emit a Referrer on responses, and doing so leaks origin information in
  server logs.
Fix: Change the Referrer-Policy value to "no-referrer":
    "Referrer-Policy": "no-referrer",
  Effort: 5 minutes.

[11-5] PASS | No Wildcard (*) in Production CORS
File: backend/core/middleware.py:315–323
Finding: init_middleware() explicitly detects if wildcard "*" is in the origins
  list and raises RuntimeError when ENV=production:
    if wildcard and is_production:
        raise RuntimeError("CORS is configured with wildcard '*' while ENV=production...")
  The server refuses to start with wildcard CORS in production. In development,
  wildcard is allowed but allow_credentials is disabled to comply with the CORS
  spec. PASS.

[11-6] PASS | CORS Allowlist Enforced via Environment Variable
File: backend/core/config.py:47; backend/core/middleware.py:307
Finding: origins are parsed from settings.ALLOWED_ORIGINS, which is a
  Pydantic BaseSettings field backed by the ALLOWED_ORIGINS environment
  variable. The server reads it at startup; no origin is hardcoded in
  production config. The always_allowed list hardcodes only admin and
  localhost origins, which is acceptable. PASS.

[11-7] PASS | Preflight OPTIONS Handled Correctly
File: backend/core/middleware.py:334–340
Finding: FastAPI's CORSMiddleware is registered with allow_methods=["*"] and
  allow_headers=["*"]. CORSMiddleware handles OPTIONS preflight requests
  automatically and returns the correct Access-Control-Allow-Methods and
  Access-Control-Allow-Headers headers. PASS.

[11-8] PASS | CORS Headers Present on 4xx/5xx Error Responses
File: backend/core/middleware.py:353–387
      backend/utils/error_handling.py:542–560
Finding: Two mechanisms ensure CORS headers reach error responses:
  (1) cors_exception_handler registered via @app.exception_handler(Exception)
  in init_middleware (line 354) inspects the origin header and adds
  Access-Control-Allow-Origin + Vary: Origin on all exception responses,
  including HTTP exceptions and unhandled 500s.
  (2) register_exception_handlers() (server.py:54) registers a second
  general_exception_handler from utils/error_handling.py that reads from the
  same resolved origins set (error_handling.py:538) and applies _cors_headers_for().
  Both paths resolve origins from the same settings.ALLOWED_ORIGINS variable
  so the two handlers stay in sync. Error responses carry security headers too
  (middleware.py:385: _apply_security_headers on exception response). PASS.

[11-9] PASS | All Required Security Headers Present on API Responses
File: backend/core/middleware.py:96–138
Finding: SecurityHeadersMiddleware applies all required headers on every
  response via _apply_security_headers():
    X-Content-Type-Options: nosniff            ✓ (line 97)
    X-Frame-Options: DENY                      ✓ (line 98)
    Referrer-Policy: strict-origin-when-cross-origin ✓ (line 99) [see 11-4]
    Permissions-Policy: geolocation=(),
      microphone=(), camera=(), payment=()     ✓ (line 100)
    Cross-Origin-Opener-Policy: same-origin    ✓ (line 101)
    Cross-Origin-Resource-Policy: same-site    ✓ (line 102)
    Content-Security-Policy: default-src 'none';
      frame-ancestors 'none'; base-uri 'none' ✓ (line 105/138)
    Strict-Transport-Security: max-age=31536000;
      includeSubDomains; preload              ✓ (line 133, production only)
  Security headers are also injected into exception responses (line 385).
  Minor: Referrer-Policy value should be "no-referrer" per checklist (see 11-4).
  Minor: Cross-Origin-Resource-Policy is "same-site" vs the checklist value of
  "same-origin" — for an API with mobile clients "same-site" is correct
  because mobile apps are not "same-origin" but requests are legitimate.
  No missing headers; PASS with the Referrer-Policy gap noted as 11-4.

[11-10] PASS | Deep Link ride_id Validated Against Authenticated User (No IDOR)
File: backend/routes/rides.py:847–864
      rider-app/app/ride-details.tsx:19, 33
Finding: The rider app opens ride-details.tsx with a rideId parameter which
  calls GET /rides/{rideId}. The backend get_ride() handler (line 847):
  (1) Fetches the ride by ID.
  (2) Checks is_rider = ride["rider_id"] == current_user["id"].
  (3) Also checks is_driver (for the assigned driver's view).
  (4) Falls back to an admin role check.
  (5) Returns 403 if none of the above match.
  A crafted deep link spinr.app/ride/{foreign_id} cannot expose another user's
  ride — the authenticated user's identity is cross-checked against the ride
  record server-side. IDOR is not possible on this path. PASS.

[11-11] PASS | Share Token (spinr-track.app/{token}) Validated — Not a Ride ID
File: backend/routes/rides.py:1198–1247
Finding: The /track/{share_token} public endpoint (line 1198) does NOT accept
  a raw ride_id. It queries rides by shared_trip_token (a 32-byte
  secrets.token_urlsafe value). The token is unguessable. The endpoint also:
  (a) Rejects tokens older than 24 hours (line 1214).
  (b) Returns only a minimal safe subset of ride data (status, addresses, and
    a filtered driver object with name/location/vehicle — no PII, no payment
    data, no phone numbers).
  A crafted spinr-track.app URL cannot enumerate rides or access another
  user's data because the token is cryptographically random and ride_id is not
  exposed on this path. PASS.

[11-12] PASS | Firebase App Check Enforced on All Rider-Specific Backend Endpoints
File: backend/core/middleware.py:27, 351
Finding: Per finding [03-11] (verified in D03 audit): _APP_CHECK_ENFORCEMENT
  = True and FirebaseAppCheckMiddleware is registered with
  enforcement_enabled=True. Every /api/* request — including all rider
  endpoints — requires a valid X-Firebase-AppCheck header. WebSockets, health
  probes, and docs are correctly exempted. The public /track/{share_token}
  endpoint is served under /api/v1/rides/track/... and therefore IS protected
  by App Check — unauthenticated web browsers cannot reach it directly.
  Note: the /track endpoint intentionally has no user auth requirement (safety
  contacts need it), so App Check alone gates it. This is by design; the token
  is the access control mechanism. PASS.

[11-13] PASS | Rate Limiting Reads X-Forwarded-For Behind CDN/Proxy
File: backend/utils/rate_limiter.py:63–67
Finding: default_limiter is created with key_func=get_ipaddr (slowapi.util).
  get_ipaddr reads X-Forwarded-For when present, falling back to the TCP
  remote address. This means real client IPs are used behind Railway / Fly.io /
  Cloudflare reverse proxies. PASS.

[11-14] PASS | Redis-Backed Rate Limiting in Production with Startup Validation
File: backend/utils/rate_limiter.py:36–59
      backend/core/middleware.py:264–276
Finding: _rate_limit_storage_uri falls back to "memory://" only when
  RATE_LIMIT_REDIS_URL is unset. _validate_production_config() (middleware.py:
  264–276) raises RuntimeError if RATE_LIMIT_REDIS_URL is empty in
  ENV=production — the server refuses to start. Development environments log
  a clear warning about per-worker counters. PASS.

[11-15] PASS | ride_request_limit Applied to POST /rides (Ride Creation)
File: backend/routes/rides.py:515–516
Finding: The create_ride endpoint is decorated with @ride_request_limit which
  maps to default_limiter.limit("10/minute") (rate_limiter.py:174). PASS.

────────────────────────────────────────────────────────────────────────────────
TASK 11 TALLY
────────────────────────────────────────────────────────────────────────────────
| Severity        | Count |
|-----------------|-------|
| CRITICAL        |     0 |
| HIGH            |     1 |
| MEDIUM          |     3 |
| LOW             |     0 |
| PASS            |    11 |
| RECOMMENDATION  |     0 |
| TOTAL           |    15 |

================================================================================
[AUDIT IN PROGRESS — TASKS 12–16 PENDING]
================================================================================
