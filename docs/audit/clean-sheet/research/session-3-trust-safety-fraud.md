# Research Session 3 — Trust, safety & fraud

*Clean-sheet rebuild audit, research phase. Written 2026-09-25. Report only: no code, config or data was changed. No PII. Fix-oriented wording only; this repo is public, so no card describes how to beat a control.*

## §0 Method, inputs, deviations and limits

**Brief.** `docs/audit/clean-sheet-prompt/research-sessions.md` §1 (shared method, card format, rules) and §4 (Session 3: scope, questions, guardrails). Guardrails applied to every card: no ad or behavioural-profiling SDKs; every fraud action has an appeal path; the false-positive cost is stated.

**Deviations from §1 (by coordinator instruction, not by choice):**
- **One file, not four.** §1 asks for `current-state.md`, `techniques-radar.md`, `recommendations.md` and `DEFERRED.md` in `research/trust-safety-fraud/`. The coordinator asked for exactly one file at this path. The four sections map as: §1 = current state, §2 = techniques radar, §3 = build order (recommendations), §5 = deferred / open questions.
- **No lane agents spawned.** Lanes C (current state) and A (adversary) were run by this session directly, applying the charters of `spinr-safety-sos-reviewer`, `spinr-fraud-auditor` and `spinr-insurance-period-auditor` (`.claude/agents/`). Lane R (external research) was run directly with WebSearch/WebFetch.
- **No commit, no push, no PR** (coordinator instruction; §1's minute 50–60 step is skipped).

**Inputs read, cited, not re-audited:** `02-findings/trust-safety-fraud.md` (TSF-001..012; TSF-010 WITHDRAWN), `03-benchmark.md` (BENCH-001 withdrawn, BENCH-006, BENCH-009, §6 fairness), `04-blueprint.md` (Safety, Trust & Fraud and Promotions & Loyalty cards), `08-hostile-review.md` §1.4 (fraudster persona), `ROADMAP.md` (X4, N22, N23), `matrices/edge-case-matrix.md` Flow 6 (SOS reds), `rapid-baseline-2026-09-24/A8-trust-safety.md` and `ESCALATIONS.md` (E10), `rapid-baseline-2026-09-24/A2-security.md` and `02-findings/security.md` (SEC-A2-005, SEC-R10-015, App Check), `10-live-checks.md` (VERIFIED-LIVE flags, 2026-09-25 ~03:30 UTC), `.claude/context/domain-safety.md`.

**Correction carried in from the coordinator (and re-confirmed by direct read):** a 4-digit pickup code already gates trip start — generated by `generate_pickup_otp` (`backend/dependencies/__init__.py:78-80`), shown to the rider under "SHARE THIS PIN WITH YOUR DRIVER" (`rider-app/app/driver-arriving.tsx:779-792`), entered by the driver and checked with `hmac.compare_digest` plus a per-ride failure lockout that pushes the rider a "check you are with the right driver" message (`backend/routes/drivers/ride_flow.py:1164-1210`, lockout notice at `:1150-1160`). This session researches only the **rider-verifies-driver** direction and the missing-code support path, not a new PIN.

**Live facts used (VERIFIED-LIVE, `10-live-checks.md` §1, will drift):** `instant_payout_enabled` true in all 6 service areas; `referral_payout_velocity_cap_per_day` = 5; `incentive_eligibility_enforced` = false; `route_deviation_alert_enabled` = true; `rideless_sos_enabled` = false; `minimal_fcm_offer_payload_enabled` = false; `dual_approval_exports_enabled` = false; Meta Conversions API token and dataset id both set, so per-ride Purchase events are very likely live (config VERIFIED-LIVE, sending INFERRED). **Not** checked live: `sos_paging_webhook_url` (so TSF-001's "dark" status is repo-VERIFIED, live-UNKNOWN) and App Check enforcement (`APP_CHECK_ENFORCEMENT`, Railway/Fly env, not read).

**Evidence labels:** VERIFIED (read in code this session), VERIFIED-LIVE (from `10-live-checks.md`), INFERRED, ASSUMED, PROPOSED, UNKNOWN. Every external claim carries a URL and the date read (2026-09-25). An external claim without a URL is ASSUMED. Legal/regulatory claims without a primary source are ASSUMED and listed in §4 for `05-escalations.md`.

**Scale screen.** Spinr is a Saskatchewan-first operator with a small team (the blueprint and `06-operating-model.md` assume roughly two people doing trust & safety part-time). Anything that pays off only at Uber scale (ML fraud models, graph databases, 24/7 in-house safety centre) is HOLD.

**Web access (limit).** WebSearch worked. **WebFetch was blocked by the egress proxy** for every primary-source domain tried (firebase.google.com, uber.com, help.uber.com, priv.gc.ca, laws-lois.justice.gc.ca, developer.apple.com returned an empty shell). One page was fetched in full: Google's Play Integrity verdicts page. Every other external claim below rests on **search-result snippets of the named URL, not a full read of the page**; those claims are labelled INFERRED, not VERIFIED. About 20 searches were used. Statute text (PIPEDA s.7(3), s.9(2.1)–(2.4)) could not be read, so every PIPEDA-section claim is ASSUMED and escalated in §4.

---

## §1 Current state (Lane C) — what this session found beyond the W2 lane

Only what is **new or changed** relative to `02-findings/trust-safety-fraud.md` and A8 is listed. Their verified items are cited, not repeated.

| # | Surface | What the code does today | Label | New vs. prior audit |
|---|---|---|---|---|
| CS-1 | Device attestation on go-online | `driver-app/utils/deviceIntegrity.ts:75-157` asks for a nonce, tries native Play Integrity / App Attest modules, then posts device fields to `/drivers/attest-device`. **The native modules do not exist in the repo** (no `SpinrPlayIntegrity`/`SpinrAppAttest` implementation anywhere; `driver-app/plugins/` has no attestation plugin), so `integrityToken` is always null and `attestMethod` is always `heuristic`. | VERIFIED | **New.** SEC-A2-005 said "no Play Integrity / App Attest verdict use beyond App Check" — true, and it is worse than stated: a scaffold exists that looks like attestation but is not. |
| CS-2 | Server-side verdict | `backend/utils/device_attestation.py:45-94` decides trust **only from client-reported fields** (`isDevice`, brand, model, OS version). It never reads `integrityToken`. The nonce endpoint (`backend/routes/drivers/location.py:1340-1350`) generates a random value but **does not store it**, so it cannot be checked as single-use. | VERIFIED | **New.** A modified client can report any value it likes; the verdict carries no cryptographic weight. |
| CS-3 | Who reads the verdict | `device_attestation.py:97-111` writes `drivers.device_risk_tier/…_reason/…_attested_at` (migration 110). **No backend reader and no admin-dashboard reader** were found (grep of `backend/` and `admin-dashboard/src`). The module docstring promises "enhanced GPS monitoring and admin visibility" for untrusted devices; neither exists. The client ignores a failed verdict (`driver-app/hooks/useDriverDashboard.ts:1926-1931`, warn-only) and fails open on error (`deviceIntegrity.ts:145-156`). A failed store logs at `warning` (`device_attestation.py:110`). | VERIFIED | **New.** A write-only signal. |
| CS-4 | App-level attestation (Firebase App Check) | Client configures App Check with **Play Integrity (Android) and DeviceCheck (iOS)**, not App Attest (`shared/services/firebase.ts:104-125`). Server verifies the header with `firebase_admin.app_check.verify_token` (`backend/core/middleware.py:417-490`) and enforces by default when `ENV=production` (`backend/core/config.py:459-469`). No limited-use / replay-protected tokens are used (`verify_token` is called without consuming the token). OTP endpoints and browser surfaces are exempt. Production enforcement state is UNKNOWN (not in `10-live-checks.md`). | VERIFIED (code) / UNKNOWN (live) | Re-confirms SEC-R10-015; adds the DeviceCheck-not-App-Attest and no-replay-protection details. |
| CS-5 | GPS plausibility | Server: `backend/utils/location_integrity.py:41-175` — mock flag, accuracy sanity, client speed, and **position-derived speed** (the one check a modified client cannot fake). Client: `driver-app/utils/locationIntegrity.ts` (mock flag, speed, 5 km/5 s teleport; gates display only, never capture) and `driver-app/utils/sensorIntegrity.ts` (accelerometer variance vs. GPS speed). Batch path rejects mocked points terminally (`backend/utils/breadcrumbs.py:532-533`). Post-trip road-match scoring (`backend/utils/route_validation.py`) runs from `backend/routes/drivers/_shared.py:841-864` and stores `rides.route_validation`; the only downstream action found is a log line. | VERIFIED | Re-confirms A8/TSF (b)#34. Adds: rejected-point counts are **not aggregated per driver** anywhere, so repeated spoofing is not visible as a pattern. |
| CS-6 | Detection-signal table precedent | `backend/utils/distance_integrity.py` appends detection-only rows to `ride_distance_integrity_events` (migration 246; append-only trigger added 2026-09-21 per C118). Five fixed `kind` values, never changes a fare, never blocks a caller. | VERIFIED | **New relevance.** This is a working template for the blueprint's proposed `trust_signals` table; reuse its shape instead of designing from scratch. |
| CS-7 | Incentive budget and window | `backend/services/incentive_service.py:1-30`: start/end window, `conditions`, and `max_budget` are enforced **only** when `incentive_eligibility_enforced` is true. Live value: **false** (VERIFIED-LIVE). So an ended or over-budget campaign keeps paying. | VERIFIED + VERIFIED-LIVE | **New for this lane.** It turns TSF-002 (collusion) from "bounded by the campaign budget" into "unbounded". |
| CS-8 | Instant payout | `backend/routes/drivers/payouts.py:829-846` per-area kill switch only; no velocity or clearing rule found. Live: `instant_payout_enabled` true in 6/6 areas (VERIFIED-LIVE). | VERIFIED + VERIFIED-LIVE | Re-confirms 08-hostile-review §1.4 and ROADMAP N22; urgency raised. |
| CS-9 | Promotion per-user gates | `backend/routes/promotions.py:183-190,226-230,327-335` — per-account `max_uses_per_user`, `first_ride_only` by real ride count, and an authoritative per-user gate (migration 257). Referral self-referral block and per-referrer cap (live value 5/day) stand. | VERIFIED + VERIFIED-LIVE | Per-**account** controls are sound; per-**person** (many accounts) is the open gap (E10). |
| CS-10 | New-device notice | `backend/routes/auth.py:602-616` + `backend/utils/refresh_tokens.py:238-267` email a "new sign-in" notice when a login's user-agent was never seen for that user. It is hard-coded to the `rider` audience, is post-hoc (not a step-up), and returns "not new" on a blank user-agent or a DB error. | VERIFIED | **New to this lane.** TSF-005 said "no SIM-swap-specific signal" — correct, but a weak generic ATO notice already exists and is the cheapest thing to build on. |
| CS-11 | SOS paging and triage | Paging (`backend/utils/safety_paging.py`) is called **only** from the two SOS endpoints (`backend/routes/rides/safety.py:313,766`). The safety check-in escalation and the route-deviation alert (**live**, `route_deviation_alert_enabled` true) go to `notify_safety_team` (admin WS broadcast, email to `safety_alert_emails`, `logger.critical`) and never page. The admin incident model has `status` and `assigned_to_admin_id` (`backend/routes/admin/safety.py:52,321`) but no acknowledgement timestamp and no "unacknowledged after N minutes" escalation. Domain doc names Opsgenie as a drop-in target; Atlassian ended Opsgenie new sales on 2025-06-04 and ends support 2027-04-05 (INFERRED, [Atlassian migration page](https://www.atlassian.com/software/opsgenie/migration), [Atlassian support](https://support.atlassian.com/opsgenie/docs/what-changes-when-you-migrate-to-jira-service-management/), read 2026-09-25). | VERIFIED | **New.** Extends TSF-001: now that route-deviation alerts are live, more incidents are being created with no page behind them. |
| CS-12 | Admin incident read audit | `backend/routes/admin/safety.py` calls `log_admin_action` only at `:304` (create), `:375` (update) and `:444` (merge). `GET /incidents/{id}` (`:165`) writes no audit row. | VERIFIED | **Closes TSF-011's UNKNOWN on the audit half** (reads are not logged). The returned field set was still not read line by line. |
| CS-13 | Driver fatigue | No online-hours counter, rest-period rule or break reminder anywhere in `backend/` (grep for fatigue / hours-online / rest / break terms: zero hits). | VERIFIED (absence) | **New.** |
| CS-14 | Passkeys / step-up | No passkey or WebAuthn code in `backend/`, `rider-app/app`, `driver-app/app` or `admin-dashboard/src`. | VERIFIED (absence) | Consistent with TSF-005. |
| CS-15 | Meta Conversions API | `backend/utils/meta_capi.py` sends server-side events with SHA-256-hashed email, phone and user id (Meta "Advanced Matching"). Live: token + dataset set, so per-ride events are very likely sent (`10-live-checks.md` §1). | VERIFIED (code) + VERIFIED-LIVE (config) | Guardrail tension, not a fraud tool — see card G-1. |

