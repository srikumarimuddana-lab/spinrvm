# Continue Here — 2026-05-03 11:50am CST

## Branch
`fix/rider-app-expo-sdk55` → PR #406 open, NOT yet merged to main

## What Was Done This Session

### Completed
1. PR #406 created — 12 commits, Expo SDK 55 full migration for rider-app + driver-app
2. iOS minimum target pinned — minimumOsVersion: '16.0' in both app.config.ts (SDK 55 raised iOS floor 13→16)
3. CI dep-health gate added — .github/workflows/mobile-dep-check.yml
4. Dependency upgrade runbook — docs/dependency-upgrade-runbook.md
5. compileSdkVersion 35 + Kotlin 2.0.21 pinned in both apps expo-build-properties
6. Stale Stripe plugin removed from driver-app app.config.ts
7. New Architecture DISABLED — newArchEnabled: false in both apps (root cause of all 7 Gradle failures + OTP freeze)
8. expo-modules-core removed from direct deps (was failing expo doctor check)

### Build History
- Builds 1-5: Various dep failures
- Build 6 (rider): New Arch Gradle conflict
- Build 7 (driver): Stale Stripe plugin + New Arch
- Build 8 (rider+driver parallel): IN PROGRESS — first build with New Arch disabled (bg job bqmqyssb7)

## Known Issues Still Open
1. OTP freeze — expected to self-resolve with New Arch disabled; if not, trace otp.tsx nav logic
2. react-native-maps-directions unmaintained (2019) — used in 8 screens, entire ride flow at risk
3. react-native-worklets redundant alongside reanimated 4.x — remove from both apps
4. Firebase packages on ^ semver range — should pin to 24.x.x
5. Android 15 16KB page alignment — Play Store Aug 2025 requirement, LogRocket .so unknown

## Immediate Next Steps
1. Wait for Build #8 — if green, merge PR #406
2. Test OTP on device
3. If Build #8 fails — check logs at expo.dev/accounts/srikumarimuddana
4. Remove react-native-worklets from both apps
5. Pin Firebase to 24.x.x
6. Plan react-native-maps-directions replacement

## Resume
/gsd-resume-work
