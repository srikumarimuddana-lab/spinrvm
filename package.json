{
  "name": "spinr-monorepo",
  "private": true,
  "description": "Spinr ride-sharing monorepo root — tooling only, not deployed",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
