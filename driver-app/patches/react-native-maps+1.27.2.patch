diff --git a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
--- a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
@@ -92,6 +92,7 @@
     public GoogleMap map;
     private Bundle savedMapState;
     private ArrayList<MapFeature> savedFeatures = null;
+    private int featureRestoreGeneration = 0;
     private boolean shouldRestorePadding = false;
 
     private MarkerManager markerManager;
@@ -319,6 +320,7 @@
     @Override
     protected void onAttachedToWindow() {
         super.onAttachedToWindow();
+        final int restoreGeneration = ++featureRestoreGeneration;
 
         // Reset lifecycle flags when reattaching
         synchronized (this) {
@@ -326,22 +328,30 @@
         }
 
         attachLifecycleObserver();
-        if (savedMapState != null) {
+        if (savedMapState != null || savedFeatures != null) {
             super.onCreate(savedMapState);
             super.onStart();
             super.onResume();
             prepareAttacherView();
             getMapAsync((map)->{
+                // A newer attachment owns the saved children after a detach.
+                if (destroyed || restoreGeneration != featureRestoreGeneration) {
+                    return;
+                }
                 onMapReady(map);
                 if (savedFeatures != null && !savedFeatures.isEmpty()) {
-                    for (int i = 0; i < savedFeatures.size(); i++) {
-                        MapFeature savedFeature = savedFeatures.get(i);
+                    features.clear();
+                    ArrayList<MapFeature> toRestore = savedFeatures;
+                    savedFeatures = null;
+                    for (int i = 0; i < toRestore.size(); i++) {
+                        MapFeature savedFeature = toRestore.get(i);
                         if (savedFeature != null) {
                             addFeature(savedFeature, i);
                         }
                     }
+                } else {
+                    savedFeatures = null;
                 }
-                savedFeatures = null;
             });
         }
     }
@@ -349,6 +359,7 @@
     // Override onDetachedFromWindow to detach lifecycle observer
     @Override
     protected void onDetachedFromWindow() {
+        ++featureRestoreGeneration;
         synchronized (this) {
             // Save instance state if not already saved and map is ready
             if (map != null && isMapReady) {
@@ -377,7 +388,10 @@
             Log.e("MapView", "Error during stop in onDetachedFromWindow: " + e.getMessage());
         }
 
-        savedFeatures = new ArrayList<>(features);
+        // Re-detaching before getMapAsync completes must keep pending children.
+        if (savedFeatures == null) {
+            savedFeatures = new ArrayList<>(features);
+        }
         features.clear();
         shouldRestorePadding = true;
         removeView(attacherGroup);
@@ -1166,27 +1180,36 @@
     }
 
     private void safeAddFeature(int index, MapFeature mapFeature){
-        if(paused || features.size() < index){
-            if (savedFeatures == null) {
-                savedFeatures = new ArrayList<>();
-            }
-
+        if(savedFeatures != null){
             // Ensure the list is large enough to set at the given index
-            while(savedFeatures.size() <= index){
+            while(savedFeatures.size() < index){
                 savedFeatures.add(null);
             }
-            savedFeatures.set(index, mapFeature);
+            savedFeatures.add(index, mapFeature);
             return;
         }
 
         // Ensure the list is large enough to set at the given index
-        while(features.size() <= index){
+        while(features.size() < index){
             features.add(null);
         }
-        features.set(index, mapFeature);
+        features.add(index, mapFeature);
     }
 
     public void addFeature(View child, int index) {
+        // When detached, skip addToMap calls and just track in savedFeatures
+        if (savedFeatures != null) {
+            if (child instanceof MapFeature) {
+                safeAddFeature(index, (MapFeature) child);
+            } else if (child instanceof ViewGroup) {
+                ViewGroup children = (ViewGroup) child;
+                for (int i = 0; i < children.getChildCount(); i++) {
+                    addFeature(children.getChildAt(i), index);
+                }
+            }
+            return;
+        }
+
         // Our desired API is to pass up annotations/overlays as children to the mapview component.
         // This is where we intercept them and do the appropriate underlying mapview action.
         if (child instanceof MapMarker) {
@@ -1273,10 +1296,19 @@
     }
 
     public int getFeatureCount() {
+        if (savedFeatures != null) {
+            return savedFeatures.size();
+        }
         return features.size();
     }
 
     public View getFeatureAt(int index) {
+        if (savedFeatures != null) {
+            if (index < savedFeatures.size()) {
+                return savedFeatures.get(index);
+            }
+            return null;
+        }
         if (index < features.size()) {
             return features.get(index);
         }
@@ -1284,7 +1316,16 @@
     }
 
     public void removeFeatureAt(int index) {
-        MapFeature feature = features.remove(index);
+        MapFeature feature;
+        if (savedFeatures != null) {
+            if (index < savedFeatures.size()) {
+                feature = savedFeatures.remove(index);
+            } else {
+                return;
+            }
+        } else {
+            feature = features.remove(index);
+        }
         if (feature instanceof MapMarker) {
             markerMap.remove(feature.getFeature());
             feature.removeFromMap(markerCollection);
diff --git a/node_modules/react-native-maps/src/specs/NativeComponentMarker.ts b/node_modules/react-native-maps/src/specs/NativeComponentMarker.ts
--- a/node_modules/react-native-maps/src/specs/NativeComponentMarker.ts
+++ b/node_modules/react-native-maps/src/specs/NativeComponentMarker.ts
@@ -8,7 +8,8 @@
 
 import {codegenNativeComponent, codegenNativeCommands} from 'react-native';
 import type {
   Int32,
+  Float,
   Double,
   BubblingEventHandler,
   DirectEventHandler,
@@ -71,7 +72,10 @@
   y: Double; // Non-nullable Double for y
 }>;
 
 export interface MarkerFabricNativeProps extends ViewProps {
+  /** Android map bearing; not the generic native View rotation. */
+  markerRotation?: WithDefault<Float, 0>;
+  flat?: WithDefault<boolean, false>;
   /**
    * Sets the anchor point for the marker.
    * The anchor specifies the point in the icon image that is anchored to the marker's position on the Earth's surface.
diff --git a/node_modules/react-native-maps/src/MapMarker.tsx b/node_modules/react-native-maps/src/MapMarker.tsx
--- a/node_modules/react-native-maps/src/MapMarker.tsx
+++ b/node_modules/react-native-maps/src/MapMarker.tsx
@@ -515,8 +515,13 @@
 
     return (
       <AIRMapMarker
         {...this.props}
+        // Android Fabric needs an explicit map prop in its codegen schema.
+        // Keep the iOS rotation path unchanged.
+        {...(Platform.OS === 'android'
+          ? {markerRotation: this.props.rotation ?? 0, rotation: undefined}
+          : {})}
         ref={this.marker}
         // @ts-ignore
         image={image}
         // @ts-ignore
diff --git a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MarkerManager.java b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MarkerManager.java
--- a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MarkerManager.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MarkerManager.java
@@ -76,8 +76,8 @@
             if (initialProps.hasKey("draggable")){
                 options.draggable(initialProps.getBoolean("draggable", false));
             }
-            if (initialProps.hasKey("rotation")){
-                options.rotation(initialProps.getFloat("rotation", 0));
+            if (initialProps.hasKey("markerRotation")){
+                options.rotation(initialProps.getFloat("markerRotation", 0));
             }
             if (initialProps.hasKey("flat")) {
                 options.flat(initialProps.getBoolean("flat", false));
@@ -129,8 +129,18 @@
         return MapMarker.getExportedCustomDirectEventTypeConstants();
     }
 
     @Override
+    public void setMarkerRotation(MapMarker view, float value) {
+        view.setRotation(value);
+    }
+
+    @Override
+    public void setFlat(MapMarker view, boolean value) {
+        view.setFlat(value);
+    }
+
+    @Override
     public void setAnchor(MapMarker view, @Nullable ReadableMap map) {
         double x = map != null && map.hasKey("x") ? map.getDouble("x") : 0.5;
         double y = map != null && map.hasKey("y") ? map.getDouble("y") : 1.0;
         view.setAnchor(x, y);
diff --git a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerInterface.java b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerInterface.java
--- a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerInterface.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerInterface.java
@@ -14,7 +14,9 @@
 import com.facebook.react.bridge.ReadableMap;
 import com.facebook.react.uimanager.ViewManagerWithGeneratedInterface;
 
 public interface RNMapsMarkerManagerInterface<T extends View> extends ViewManagerWithGeneratedInterface {
+  void setMarkerRotation(T view, float value);
+  void setFlat(T view, boolean value);
   void setAnchor(T view, @Nullable ReadableMap value);
   void setCalloutAnchor(T view, @Nullable ReadableMap value);
   void setImage(T view, @Nullable ReadableMap value);
diff --git a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerDelegate.java b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerDelegate.java
--- a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerDelegate.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMarkerManagerDelegate.java
@@ -24,8 +24,14 @@
   }
   @Override
   public void setProperty(T view, String propName, @Nullable Object value) {
     switch (propName) {
+      case "markerRotation":
+        mViewManager.setMarkerRotation(view, value == null ? 0f : ((Double) value).floatValue());
+        break;
+      case "flat":
+        mViewManager.setFlat(view, value == null ? false : (boolean) value);
+        break;
       case "anchor":
         mViewManager.setAnchor(view, (ReadableMap) value);
         break;
       case "calloutAnchor":
diff --git a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
--- a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
+++ b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
@@ -1652,9 +1652,11 @@
   RNMapsMarkerProps() = default;
   RNMapsMarkerProps(const PropsParserContext& context, const RNMapsMarkerProps &sourceProps, const RawProps &rawProps);
 
 #pragma mark - Props
 
+  Float markerRotation{0.0};
+  bool flat{false};
   RNMapsMarkerAnchorStruct anchor{};
   RNMapsMarkerCalloutAnchorStruct calloutAnchor{};
   ImageSource image{};
   RNMapsMarkerCalloutOffsetStruct calloutOffset{};
diff --git a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
--- a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
+++ b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
@@ -637,10 +637,12 @@
 #endif
 RNMapsMarkerProps::RNMapsMarkerProps(
     const PropsParserContext &context,
     const RNMapsMarkerProps &sourceProps,
     const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),
 
+    markerRotation(convertRawProp(context, rawProps, "markerRotation", sourceProps.markerRotation, {0.0})),
+    flat(convertRawProp(context, rawProps, "flat", sourceProps.flat, {false})),
     anchor(convertRawProp(context, rawProps, "anchor", sourceProps.anchor, {})),
     calloutAnchor(convertRawProp(context, rawProps, "calloutAnchor", sourceProps.calloutAnchor, {})),
     image(convertRawProp(context, rawProps, "image", sourceProps.image, {})),
     calloutOffset(convertRawProp(context, rawProps, "calloutOffset", sourceProps.calloutOffset, {})),
@@ -667,13 +669,19 @@
 folly::dynamic RNMapsMarkerProps::getDiffProps(
     const Props* prevProps) const {
   static const auto defaultProps = RNMapsMarkerProps();
   const RNMapsMarkerProps* oldProps = prevProps == nullptr
       ? &defaultProps
       : static_cast<const RNMapsMarkerProps*>(prevProps);
   if (this == oldProps) {
     return folly::dynamic::object();
   }
   folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
+  if (markerRotation != oldProps->markerRotation) {
+    result["markerRotation"] = markerRotation;
+  }
+  if (flat != oldProps->flat) {
+    result["flat"] = flat;
+  }
   
   if (anchor != oldProps->anchor) {
     result["anchor"] = toDynamic(anchor);
