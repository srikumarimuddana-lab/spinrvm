# Capacity table — standards-and-scale.md §5, filled (Lane A3)

**Source:** Lane A3 (fable / general-purpose), 2026-09-24. Every vendor-doc fetch was blocked by the egress proxy; vendor limits are INFERRED from search snippets. App-side ceilings are VERIFIED from repo files. No load test has run against the production shape (`docs/runbooks/capacity-scaling.md` §8) — every 10× figure is arithmetic, not a measurement. The single most important row is Supabase: the plan tier is recorded nowhere in the repo (A3-006).

| Vendor | What to measure | Known limit | Source label | Tripwire (exists today?) | What breaks at 10× | First lever |
|---|---|---|---|---|---|---|
| **Supabase Postgres** | PostgREST round-trips/s, thread-pool queue depth, connections, egress, disk | Compute tier **not recorded anywhere in repo** ("Small ← pre-sized here" in `docs/runbooks/capacity-scaling.md` §5 is a recommendation, not an observed fact). App-side ceiling: `DB_THREAD_POOL_SIZE=64` + `DB_THREAD_POOL_QUEUE_SIZE=64` per uvicorn worker (`backend/repositories/_base.py:165-170`), `UVICORN_WORKERS=2` on Fly (`backend/fly.toml:29`) → 256 concurrent PostgREST calls at 2 warm machines, 1,024 at full 8-machine wake; circuit breaker 5 failures/30 s fleet-wide (`_base.py:72-155`, runbook §4). Disk ceiling approximated at 8 GB (`supabase-capacity-monitor.yml:54`, self-described "manually-maintained approximation"). Supavisor per-tier pool sizes: hard-coded per compute size, not changeable without tier upgrade | VERIFIED (repo) for app-side; tier & per-tier connection counts **UNKNOWN** (supabase.com blocked; snippet only says limits are per-tier and PostgREST-heavy apps should keep pool ≤40% of max_connections — INFERRED) | 70% target; **actual**: `capacity_watchdog` queue depth >50 ×3 ticks (`backend/utils/capacity_watchdog.py`, runbook §6) — **silent unless `ALERT_WEBHOOK_URL`/`ALERT_EMAIL_TO` set**; disk monitor no-op until `SUPABASE_ACCESS_TOKEN` exists (runbook §9) | Not row volume — **round-trip volume**: 8.6 M PostgREST requests for 202 rides in 96 days (`docs/audit/2026-08-26-db-query-optimization-recommendations.md:28`), 800–900 round-trips per unmatched ride request (`:191-204`), and 45 loops polling from every process (A3-001). At 10× users the loop floor stays constant but per-ride fan-out ×10 lands on a fixed, non-autoscaling tier | (1) `SPINR_PROCESS_ROLE=api` on API replicas + a worker machine (registry already exists, `backend/core/background_loop_registry.py`), (2) finish C50 direct pool for dispatch (`core/config.py:67-96`, pool default max 8), (3) tier bump (manual, ~2 min) |
| **Supabase Storage** | document/profile-image bytes, signed-URL rate | UNKNOWN — no repo file cites a storage quota; only client-side profile-image compression exists (`routes/users.py:631`) | UNKNOWN | none | Cost only; document uploads scale with driver onboarding, not rides | Lifecycle rule on rejected/expired docs; keep client compression |
| **Redis** | ops/s, memory, pub/sub fan-out, eviction policy | Provider/plan **not recorded** (runbooks say "Upstash / Redis Cloud / Fly Redis", `docs/runbooks/redis-down.md:38`, `railway-fly-failover.md:147`). Metrics exist: `spinr_redis_used_memory_bytes`/`maxmemory_bytes` from `INFO` on `/metrics` scrape (`docs/adr/010:38`, `utils/redis_client.py:521-547`). In-process dict fallback when unset (ADR-004) | VERIFIED (metrics exist); plan UNKNOWN | none wired to those gauges (ADR-010 says alerting is aggregation-side; no repo alert rule found) | **First component to degrade at 10× drivers** — every GPS ping does ~5–6 Redis ops (active-ride cache GET `websocket.py:1185`, integrity GET/SET `location_integrity.py:190`, write-gate `location_write_gate.py:104-146`, location SET `socket_manager.py:582-596`, PUBLISH `ws_pubsub.py:197-229`) → A3-002 arithmetic | Coalesce per-ping Redis ops into one Lua/pipeline; TTLs already present; shard `spinr:ws:dispatch` channel by key prefix |
| **Fly.io / Railway** | connections/machine, CPU, RSS, event-loop lag, WS count | Fly: 2 warm `shared-cpu-2x`/4 GB + 6 burst `shared-cpu-1x`/2 GB, 750 soft / 1000 hard **connections** per machine, `auto_stop_machines="off"` (`backend/fly.toml:11-16, 47-58, 85-95`) → 6,000 soft / 8,000 hard WS users, arithmetic only. Railway standby: `UVICORN_WORKERS` default **4** (`railway.json:8`) vs Fly 2 — parity gap. **Runbook drift**: `capacity-scaling.md` §2 still says `auto_stop_machines="suspend"`, 1 GB VMs, 18 loops | VERIFIED (repo); CPU at 750+ conns **never measured** (runbook §8) | Fly proxy soft-limit wake; no CPU alert | CPU on shared-cpu at ~1 Hz JSON parse + 5 Redis awaits per ping per driver; `fly.toml:17` itself says the limits "are not a supported-user count" | `flyctl scale count` (live); measure before raising soft_limit |
| **Vercel** (admin) | invocations, bandwidth, build min | Plan UNKNOWN (renewal-calendar.md has no Vercel row; `docs/runbooks/admin-rollback.md:32` confirms auto-deploy). Next.js/Vercel compress responses by default (INFERRED, not fetched) | UNKNOWN | none | Cost only; admin is internal, low fan-out | ISR/caching if needed — not a 10× concern |
| **Stripe** | API req/s, webhook backlog, balance | Global ~**100 ops/s live** per account; search endpoints 20 read/s (INFERRED — docs.stripe.com blocked, snippet from search). App handles `RateLimitError` → 429+Retry-After (`routes/payments.py:561-570`, VERIFIED). Balance polled by `billing-usage-monitor.yml:63-124` (VERIFIED) | INFERRED (limit) / VERIFIED (handling) | 50% target — **no request-rate counter exists**; only balance | Settlement bursts at trip-end peaks: 10× rides ≈ still ≪100/s for a SK-scale fleet; webhook processing SLA <500 ms is the earlier bind | Already idempotent (`claim_stripe_event`); queue retries via `payment_retry` loop |
| **Twilio** | OTP send rate, balance, conversion | Canada forbids A2P on long codes; toll-free default **3 MPS** to CA (upgradeable), short code 100+ MPS (INFERRED — help.twilio.com snippets; which number type Spinr uses is **UNKNOWN**, no messaging-service/10DLC reference in backend). Balance alert at 20/5 units (`billing-usage-monitor.yml:54-55`, VERIFIED) | INFERRED / VERIFIED (balance) | balance only | A sign-up spike >3 OTP/s queues at Twilio → OTP latency → retries → `send-otp` 3/min limiter (`docs/runbooks/rate-limits.md:99`) 429s. SMS-pumping: per-IP OTP limiter exists; geo-permissions not verifiable from repo | Verify number type; Twilio Verify + geo permissions; raise TF throughput |
| **FCM / APNs** | send rate, failure % | FCM HTTP v1 default **600k msgs/min/project**, 429 `QUOTA_EXCEEDED` when exceeded (INFERRED — firebase.google.com snippet). App batches `send_each` at `_FCM_BATCH_SIZE=500` (`backend/features.py:1527,1539`, VERIFIED); `push_retry` loop 30 s (`utils/push_retry.py:113`) | INFERRED / VERIFIED | failure rate >2% — metric `_record_push_outcome` exists (ride-experience audit C97) but no alert rule found | Not a 10× concern (600k/min ≫ any SK scale); risk is credential validity (C97 #1 still open) | Collapse keys for location-type pushes; keep retry |
| **Google Maps** | calls/ride per SKU, USD/day | `MAPS_DAILY_BUDGET_USD` default **$5/day** breaker, Redis-counted per SKU, fails open on Redis error (`backend/utils/maps_budget.py:115-116, 15-17`, VERIFIED); atomic reserve via Lua (C104, `:253-334`). GCP-side budget: **not monitored** by repo (`billing-usage-monitor.yml:17-25`) | VERIFIED | budget % (breaker = hard stop, not tripwire) | At 10× rides the $5/day cap trips mid-day → proxy routes 503 → autocomplete/geocode dead for riders (fare estimate falls back to haversine/OSRM). This is a **cliff, not a slope** | Raise budget with a warn tripwire at 70%; OSRM-first already in place (`utils/maps_eta.py`) |
| **Sentry** | event quota, spike protection | Plan/quota UNKNOWN; backend samples traces/profiles at **0.1** (`backend/server.py:701-702`, `utils/sentry_runtime.py:47-48`, VERIFIED); error events unsampled | VERIFIED (sampling) / UNKNOWN (quota) | none | 10× users × loud-error policy (CLAUDE.md "never swallow") → quota burn on a single regression; spike protection is a plan feature, unverifiable here | Per-issue rate limits in Sentry; `before_send` dedupe for known-noisy classes |
| **Expo EAS** | build minutes, OTA bandwidth | Plan UNKNOWN; `[build]` commit gate (ADR-002:53); `runtimeVersion` fencing 2.7/2.8 driver, 2.1/2.2 rider (`driver-app/app.config.ts:33,203`, `rider-app/app.config.ts:33,124`) | VERIFIED (gating) / UNKNOWN (plan) | none | OTA bandwidth ×10 on every JS push; build minutes unaffected by users | Already gated; add OTA rollout % |

## Stress validation plan (standards-and-scale.md §5, unchanged — not yet run)
Locust against staging only (target guard, `loadtest/`): baseline at 1× current peak → spike at 10× → 2-hour soak at 3× → chaos (vendor down, Redis down, slow Supabase). Record p95 per SLA path and the first component to degrade. A3's arithmetic predicts the order: Redis ops/s (A3-002) → Supabase round-trips (A3-001) → Maps budget cliff (A3-004) → Fly CPU at high connection counts.
