# Continue Here — 2026-05-04 22:30 CST

## Branch
`fix/rider-app-expo-sdk55` — HEAD: `589e2f7d`

## What Was Done This Session

### 1. Firebase Setup Complete
- Firebase project `spinrapp-6e464` confirmed existing
- `backend/.env` has all Firebase credentials (single-line JSON fixed)
- Local backend started clean, Firebase initialised silently (no error = success)
- Railway redeployed with same Firebase variables — health confirmed

### 2. Backend URL Confirmed
- Both apps `.env` point to Railway production
- `EXPO_PUBLIC_BACKEND_URL=https://spinr-backend-production.up.railway.app`
- App Check skipped for sideload testing (Play Integrity rejects sideloaded APKs)

### 3. Expo Dependency Fix — DONE, NOT YET INSTALLED
`rider-app/package.json` and `driver-app/package.json` edited but yarn install NOT run.

Changes made:
- `react-native-reanimated`: 4.3.0 -> ~3.16.7  (v4 requires New Arch which is OFF)
- `react-native-worklets`: removed              (zero app usage, v4-only dep)
- `@gorhom/bottom-sheet`: ^5.1.0 -> ^4.6.4     (rider-app only; code uses v4 API)
- `resolutions.react-native-reanimated`: ~3.16.7

## IMMEDIATE NEXT STEPS

### Step 1 - Install
```bash
cd /c/Users/TabUsrDskOff111/Documents/Spinrvm/spinrvm/rider-app && yarn install
cd ../driver-app && yarn install
```

### Step 2 - Commit
```bash
cd /c/Users/TabUsrDskOff111/Documents/Spinrvm/spinrvm
git add rider-app/package.json rider-app/yarn.lock driver-app/package.json driver-app/yarn.lock
git commit -m "fix(android): downgrade reanimated 4.x->3.16.7, remove worklets, pin bottom-sheet v4"
```

### Step 3 - EAS Build
```bash
cd rider-app
eas build --profile development --platform android
```
Download APK from expo.dev (~10-15 min), sideload onto Android device.

### Step 4 - Driver app (after rider confirms clean)
```bash
cd ../driver-app && eas build --profile development --platform android
```

## Key Decisions Made This Session
- Skip App Check for testing: Play Integrity rejects sideloaded APKs
- Use Railway not local IP: removes WiFi dependency
- Reanimated v3 not v4: New Arch disabled; v4 requires New Arch
- bottom-sheet v4: code already written for v4 API
- worklets removed: no app-code usage

## Firebase Project Reference
- Project: spinrapp-6e464 / Project Number: 879808882715
- Rider Android App ID: 1:879808882715:android:150f53e023204c47cb30be
- Driver Android App ID: 1:879808882715:android:1bb8188887003b54cb30be
- Service account: firebase-adminsdk-fbsvc@spinrapp-6e464.iam.gserviceaccount.com

## Build History Summary (8 prior attempts)
#1 async-storage Maven error -> downgrade 3.x->2.2.0
#2 Native pkg misalignment -> bulk RN 0.85 realignment
#3 expo-modules-core duplicate -> pin via resolutions
#4 driver-app same issues -> mirror fixes
#5 Transitive dep mismatches -> full SDK55 matrix pass
#6 Stripe incompatible -> upgrade 0.63.0
#7 Kotlin + New Arch = OTP freeze -> compileSdk 35, New Arch OFF
#8 Gradle/Kotlin mismatch AGP 8.12 -> wrapper 8.13, Kotlin 2.1.20

## Remaining Known Issues (not blocking EAS build)
- react 19.2.0 with SDK55 (SDK55 built for React 18)
- @react-native-firebase ^24.0.0 (newer than SDK55 matrix)
- react-native-maps 1.27.2 (watch for map crashes on device)
- No Redis on Railway (rate limiter in-memory)
- PR to main not yet created (21+ commits on branch)
