# 08 — Hostile review of the clean-sheet blueprint (Step 6)

**Target:** `docs/audit/clean-sheet/04-blueprint.md` (R19, 562 lines, 18 Rebuild Delta cards, §2 hypothesis verdicts, §7 top 5).
**Date:** 2026-09-25 · **Mode:** report-only. This file is the only write. No code, config, data, or other audit file was changed; nothing was committed or pushed.

> Evidence labels as in the master prompt §4: **VERIFIED** (read the code or document directly in this pass), **INFERRED** (reasoned from evidence, not executed), **ASSUMED** (needs a human or a primary source), **PROPOSED** (a recommendation), **UNKNOWN** (must be obtained). Tax, legal and regulatory statements without a primary source are ASSUMED.

---

## §0 Method and limits

**Brief.** Assume the blueprint is wrong. Attack its top 5 deltas (§7), its hypothesis verdicts (§2) and the cards most relevant to each persona, as eight adversaries: competitor, driver, rider, fraudster, regulator, SRE, CFO, COO (`roles.md` §3 supplies the attack surfaces). Look for unsupported claims, contradictions, sequencing that breaks a live session, rollback plans that are not real rollbacks for applied data, scope creep, drift toward "What Spinr Is NOT", and staffing a small team cannot meet. Tie-breakers from the master prompt §4 decide close calls: reliable over clever, safe over fast, incremental on the live product over rewrite.

**Read.** The full blueprint; master prompt §4, §5 and §7.3; `roles.md` §3 and R19/R20; `07-reverification.md` (all 16 checks and its §4 corrections; QUAL-003 stays withdrawn); the finding cards the blueprint's most load-bearing claims rest on (DISPATCH-001, DRIVER-004, CORP-001, TSF-001, TSF-010, BENCH-001, SEC-R10-001/-002, the asymmetric-JWT card in `security.md` §5, SKB-001/-002, STRAT-002/-004 and the Spinr Pass rows in `strategy.md`, the COMP-0xx headings, and the REL-001/-003 loop counts); `01-inventory/epics.md` headings; ADR titles 001–016 and ADR-005 in full; CLAUDE.md "What Spinr Is NOT", "Critical Conventions" and "Pre-merge release gates".

**Targeted code checks run in this pass (read-only).**
- The two ride guards (`routes/drivers/_shared.py:885`, `routes/rides/_shared.py:433`). Result: confirmed.
- Pickup OTP: `dependencies/__init__.py:78-80`, `routes/drivers/ride_flow.py:1165-1210`, and the rider-app screens that display the code.
- Direct table writes that bypass `repositories/_base.py`, counted with grep across `backend/` (tests excluded).
- `supabase.rpc` call count.
- `platform_fee_percent` readers.
- The rider WS handler's `default:` branch and its `driver_assigned` UI label.
- The instant-payout gate (`routes/drivers/payouts.py:829-846`).
- Corporate wind-down (`services/corporate_wallet_winddown_service.py:140-185`).
- Twilio client construction (`sms_service.py`).
- Whether the known-forks check runs in CI.
- The migration runner's advisory lock.
- `migration-duplicate-nightly.yml`.
- The outbox worker placement (`core/background_loop_registry.py:123-147`).
- `expo-updates` in both apps.
- Visible commit authorship.

**Limits.**
- No production state was read: flag values, `schema_migrations`, secrets, and the Stripe, Sentry and Supabase consoles. The Stripe, Sentry, Twilio and Redis connectors were unavailable in this session.
- The clone is shallow: history starts 2026-09-21. Any claim here about team size is therefore INFERRED from about four days of commits.
- I did not re-audit domains. Where I disagree with a lane, I cite the file:line I read.
- Grep-based counts are lower bounds. For example, multi-line write chains are not matched by a single-line regex.
- The synthesis files were read twice: once at the start, and again just before this file was finished. See §2.4.

---

## §1 Verdict per persona

Each row gives the strongest objection, whether it holds, and the change the blueprint needs. Supporting objections follow each table row.

### 1.1 Competitor — "your gap list is partly wrong and your moat is asserted, not shown"

| Strongest objection | Holds? | Required change |
|---|---|---|
| The blueprint's second-ranked safety "table stakes" gap does not exist. Trip PIN is marked **BUILD** in card 2, card 8, §5.1 Track B "Later", the §5.3 `trip_pin_enabled` 30-day pilot, and §5's list of mid-session-visible changes. But the product already has a 4-digit rider-visible code that the driver must enter before `in_progress`. It is `pickup_otp`: generated by `dependencies/__init__.py:78-80` (`secrets.choice`, `PICKUP_OTP_LENGTH` 4), checked with `hmac.compare_digest` and a brute-force lockout at `routes/drivers/ride_flow.py:1165-1210`, and described in code as "the only production path from driver_arrived -> in_progress". The rider sees it on `driver-arriving.tsx:779`, `ride-status.tsx:440` and `driver-arrived.tsx:346`. Card 2 itself says "the existing pickup-OTP flow ... is already the production start path", and still schedules a PIN build. TSF-010's evidence was a grep for `trip_pin\|ride_pin\|verification_pin\|rider_pin`, and BENCH-001 restates TSF-010. This is the same too-narrow-grep failure as QUAL-003 and INT-003. | **VERIFIED** (code read this pass) | Withdraw TSF-010 and BENCH-001 as "no mechanism exists". The residual gaps are narrower: (a) rides with a NULL/blank `pickup_otp` return 409 "contact support to start it" (`ride_flow.py:1188-1203`), so the support-mediated start path needs a check; (b) the rider-verifies-driver direction relies on photo and plate. Remove the PIN build from cards 2 and 8, from §5.1–§5.3, and from §8's absence list. |

Supporting objections:
- **"Structurally cannot copy" is asserted, not shown.** §7 says an incumbent "could not copy the 0 % fare" and "structurally cannot" ship the driver trip receipt, and card 4 calls the cost-stack disclosure "the one genuinely defensible edge". None of these cites a source. A funded competitor can match a price temporarily. Per `strategy.md` (≈ −$0.93/ride, itself an estimate), Spinr loses money on every consumer ride, so it cannot outlast a price war. Whether incumbents already show per-trip rider-paid versus driver-earned breakdowns in some markets was not checked by any lane. These claims are **ASSUMED**; §5 item 14 asks for the wording to change.
- **Nothing a rider or driver would notice ranks in the top 5.** All five of §7's deltas are internal architecture. Customer-visible items (rider self-serve dispute, stranded-rider recovery, cash-out UI, multi-stop client) sit in card "Now/Next" rows with no ranking. From a competitor's point of view, the programme's first quarter produces nothing it would have to respond to. **INFERRED.**

### 1.2 Driver — "you fix my receipt but leave the hidden penalty"

| Strongest objection | Holds? | Required change |
|---|---|---|
| DRIVER-004 is re-verified in `07-reverification.md` (decline lowers `acceptance_rate`, `ride_flow.py:798`; dispatch divides ETA by it on the default ETA-ranking branch, `dispatch_service.py:76-91`; no disclosure in the FAQ or driver-app). The blueprint cites it only in §0's correction list and card 4's `Cites:` line. No card has a step for it. An undisclosed penalty for declining is the kind of control-of-work signal CLAUDE.md ("Not a driver-control platform") and COMP-009 (unsigned ICA) warn about. The lane asked for disclosure, not removal. | **VERIFIED** (checked the blueprint: no step exists) | Add a **Now** step to card 1 (Booking & Matching): driver-facing disclosure copy for the acceptance-rate effect, following the lane's wording. Add a §9 legal question on whether acceptance-rate ranking plus an unsigned ICA changes the classification risk (ASSUMED). |

Supporting objections:
- **Rider recovery could under-pay the driver.** Card 2 proposes an "admin-mediated completion with a fare computed from the last plausible GPS fix". A driver whose phone dies mid-trip then has the fare set by an admin from a partial trail, and the card names no driver notice or dispute path. The card also routes the Period 3 close through `spinr-insurance-period-auditor` but never through the driver. **INFERRED.** Required: driver notification, a fare floor at the last plausible fix, and the existing driver `/disputes` path named explicitly.
- **The driver pass would still charge drivers.** Card 11's "benefits-based pass" is optional, but it still takes money from drivers, and its "priority support" benefit sits uneasily with §6's rejection of "premium rider priority tiers". `strategy.md` recommends that the founder memo decide whether a driver pass exists at all. Card 11 should defer the redesign to that memo instead of pre-selecting its contents. **INFERRED.**

### 1.3 Rider — "your recovery flow breaks your own invariant, and your first step is not invisible"

| Strongest objection | Holds? | Required change |
|---|---|---|
| Card 2 says a stranded rider "is unblocked from booking", while recovery is "a new `completion_requested_by_rider` flag + admin queue (additive), **not a new status**". The ride therefore stays `in_progress` while the rider books again. That breaks CLAUDE.md's invariant that "a rider may have at most one active ride at a time" (`active_statuses` includes `in_progress`), and the booking pre-check plus DB race path that card 1 lists under **Keep** would have to be bypassed. | **INFERRED** (from the card text and the CLAUDE.md invariant; booking code not re-read) | Choose one of two options: (a) unblock only after the admin completion moves the ride to `completed`, and state the queue SLA; or (b) define an explicit, reviewed exception to the invariant with the dispatch owner, a state-machine test, and a gate-4 dry run. Either way, record the decision in §9 Q16. |

Supporting objections:
- **The DISPATCH-001 first step is described wrongly.** Card 1's "first reversible step" says the blast radius is "rider WS handler must ignore an unknown type". That is not how the rider app works:
  - A new message type hits `default:` and is only logged (`rider-app/hooks/useRiderSocket.ts:251-252`), so the fix would do nothing.
  - The helper the finding recommends, `broadcast_ride_status(..., "driver_assigned")`, flows through the `ride_status_changed` branch (`useRiderSocket.ts:188-200`), which applies the status. The rider app already has a visible label for that state, "Driver confirming" (`rider-app/app/ride-status.tsx:216`).
  - The rider would then see a driver, and ~15 s later on timeout see "Driver Unavailable" (`useRiderSocket.ts:177-184`).

  That is a user-visible UX change on a live surface (gate 5), not a no-op. DISPATCH-001 itself offered option (b), a doc fix, pending product confirmation. The blueprint chose (a) without saying so. **VERIFIED** (code) / **INFERRED** (UX effect). Required: present (a) and (b) as a product decision, and if (a) is chosen, fill in the Change Impact Log "User experience effect" field.
- **Mobile rollback is not "revert".** Cards 2 and 17 give "revert" as the rollback for rider-app hook changes. Both apps ship `expo-updates` (`rider-app/package.json:78`, `driver-app/package.json:65`), so a JS-only rollback is an OTA republish. It reaches a device only on its next update check, and installed binaries keep the bad bundle until then. **VERIFIED** (dependency) / **INFERRED** (delivery timing). Required: mobile rollback lines should say "OTA republish of the previous bundle; effective on next launch".

### 1.4 Fraudster — "you open two cash-out doors before you install a camera"

| Strongest objection | Holds? | Required change |
|---|---|---|
| Card 4 makes the driver instant cash-out UI a **Next** step, and card 3 makes the rider self-serve dispute screen a **Now** step. The detection layer (card 8's `trust_signals`, same-pair frequency, cancellation farming, chargeback velocity) is Next or Later. The approval control on refunds (ADMIN-OPS-001: single-admin refunds, no cumulative cap) is Next. The collusion pattern TSF-002 names is a stolen-card "rider" plus a driver who cashes out instantly. On a 0 %-commission platform the chargeback lands on Spinr with no fare cut to absorb it. The instant-payout gate read in this pass is a per-service-area kill switch that lets drivers with no `service_area_id` through (`routes/drivers/payouts.py:829-846`). A grep of that file found no clearing-period or velocity logic, but that is an absence claim not re-grepped with other spellings. | **INFERRED** (sequencing) / **VERIFIED** (the gate as read) | Gate the cash-out UI on three things: a per-driver velocity cap or clearing rule, TSF-004's chargeback count on the Chargebacks tab (already a Now item), and the first `trust_signals` detector. Ship the rider dispute screen together with ADMIN-OPS-001's interim per-admin daily cap and alert (card 7's own "Simpler"). The instant-payout endpoint is already callable by any authenticated driver with no UI, so the velocity cap is a **Now** item on its own merits. |

Supporting objections:
- **The wind-down key is too coarse.** CORP-001 and card 5 use `client_idempotency_key=f"corp-close-{wallet_id}"`. That is correct for an exact replay. But the refund loop can `break` part-way on a Stripe error (`corporate_wallet_winddown_service.py:140-145`). If a concurrent second run then refunds a *different* set of top-ups, the constant key dedupes a debit for a different amount, and the ledger diverges from Stripe in the opposite direction. **INFERRED** (RPC behaviour on same key and different amount not re-read). Required: derive the key from the sorted Stripe refund ids, and also land CORP-001's part (2), the conditional status flip, which card 5 drops.
- **Promo stacking is scheduled as a sweep, not a gate.** TSF-006 was "not independently re-verified" and card 11 schedules it as a Next sweep. That is acceptable only if no new promo type ships first. **INFERRED.**

### 1.5 Regulator / auditor — "your plan is an architecture programme with the regulator's list in the appendix"

| Strongest objection | Holds? | Required change |
|---|---|---|
| The lanes filed live regulatory and PIPEDA obligations. The blueprint either has no card step for them or leaves them out entirely. A grep of the blueprint for `COMP-0` ids finds 001, 002, 003, 004, 007, 008, 013, 014, 015 and 018. Of these, 001–004 and 013 appear only in §9 questions or H7's list. COMP-005 (SGI monthly km return), COMP-009 (unsigned ICA), COMP-010 (WAV alternative), COMP-011 (accessibility contact not provisioned) and COMP-017 (SGI quarterly report) are not cited at all. COMP-013 is a past exposure of driver records, still reachable on many branches per the lane. It survives only as §9 Q22 ("is the purge request submitted?"). STRAT-004 is VERIFIED, HIGH and priority 100: a Meta ad SDK plus a per-ride `Purchase` conversion carrying value and hashed identity, undisclosed in the privacy policy and subprocessor list. It is a live collision with CLAUDE.md's "never add third-party ad SDKs", yet it appears only inside §6's "should NOT be built" list, framed as "a removal, not a build", with no card step and no §9 question. The inventory has no epic for driver eligibility or regulatory reporting, so these items had no card to land in. | **VERIFIED** (checked the blueprint text against the lane cards) | Add a card, or a §4 annex, called "Regulatory obligations" with a Now step per item. At minimum: STRAT-004 disclosure plus a founder decision (card 15 or a new card), COMP-013 closure evidence, COMP-002's dark eligibility rules (with the licence-class question left ASSUMED), COMP-005/-017 SGI reporting schedule, COMP-009 ICA e-signature, and COMP-011's contact. Add STRAT-004 to §9. |

Supporting objections:
- **Life-safety and CRITICAL items are outside the ranking.** TSF-001 (no automated SOS page fires anywhere; the runbook describes a system that does not exist) and SEC-R10-001 (CRITICAL: the leaked service-role key cannot be shown to have been rotated) sit under "not in the top five". The §7 ranking formula scores architectural closure. It has no term for harm. Under the tie-breaker "safe over fast", these precede every delta. **VERIFIED** (blueprint §7 text) / **INFERRED** (ranking judgement).
- **New tables with no retention class.** `ride_status_events` (actor ids for every ride transition), `trust_signals` (per-person fraud flags) and the generalised approvals table are new personal-data stores. None of the cards gives a retention class or a purge step, and COMP-007 already found retention drift in both directions. `trust_signals` is also a profiling store, so its purpose and disclosure need a line. **INFERRED**; the legal effect is **ASSUMED**.
- **The value of the event log to a regulator is assumed.** H1 and top-5 #1 justify `ride_status_events` partly by "a regulator/plaintiff can be shown one queryable trail". No cited obligation requires a ride transition log. The regulatory trail that *is* required is the insurance-period log, and it already exists. **ASSUMED.**

### 1.6 SRE — "your parallel plan collides with itself, and your 'blocking' gate is a laptop hook"

| Strongest objection | Holds? | Required change |
|---|---|---|
| §5.1 says "Tracks A–F share no files by construction". Three problems with that claim. (1) Card 10 (Maps), card 14 (Legacy cohort) and card 9's backend egress half are not assigned to any track. (2) Card 14 step 4 replaces the ~59 `EXCLUDE_LEGACY_RIDES` references "promotions and earnings first", and `routes/drivers/earnings.py` belongs to Track A. (3) Card 18 step 7's `compare_and_swap()` helper goes in `repositories/_base.py`, which Track A owns for the money guard. Separately, top-5 #3's first step makes pre-commit check 11 "blocking". That check is a local hook (`.claude/hooks/pre-commit:253-266`), no workflow under `.github/workflows/` references `known-forks`, and the hook's own comment explains why it is warn-only: "a fork registry entry doesn't know whether a given change actually needs porting". A blocking local hook can be bypassed with `--no-verify`, does not run on web edits or for other contributors, and trains reflexive trailer overrides. | **VERIFIED** (blueprint text, hook, workflow grep) | Assign cards 9-backend, 10 and 14 to tracks, and list the real file overlaps with a merge order. Move fork parity to a PR-level CI check that reads an override from the PR body, and make the per-fork contract test the definition of done (H5 already says so). |

Supporting objections:
- **Migrate-on-deploy "rollback" only rolls back the switch.** The rollback for `MIGRATE_ON_DEPLOY` in §5.3 is "variable back to `dry_run`". That stops *future* auto-applies. It does not undo a bad migration already applied to live data. Per-migration rollback SQL is mandatory (CLAUDE.md) and the runner has an advisory lock (`run_migrations.py:52`), so concurrent applies are handled. What is missing is expand/contract ordering: Fly and Railway deploy from `main` in parallel, so new code can start before or after the schema change. **INFERRED.** Required: state that `auto` mode requires backward-compatible (expand-only) migrations, and that a contract step always goes through `approve`.
- **More gates on a CI whose required checks already false-red.** Card 16 adds eight or more new static gates, "warn one cycle, then block". It does this on a CI where the required summary jobs false-red (QUAL-001), 21 `continue-on-error` lines remain, and branch-protection state is unknown (C21). Each new blocking gate raises the chance a real red gets ignored. **INFERRED.** Required: fix QUAL-001 and publish the C21 answer before promoting any new gate to blocking, and cap the number of gates in "warn" at one time.
- **Timelines add up to most of a year.** §5.3 sets "≥ 7 days shadow per site, then 14 days flagged-on" for about 13 status-write sites, and Track B is serial. That is roughly 13 × 21 days, about nine months. The FLOAT8 window ends "after a T4A run", which happens once a year. Top-5 #1 is labelled "Cost M". **INFERRED.** Required: allow sites to be batched per shadow window once three sites have run clean, and label the calendar cost next to the effort size.
- **Loop counts disagree.** The blueprint uses 44 in some places and 45 in others. REL-001/-003 explain the difference: 44 loops plus the watchdog make 45 registry entries. CLAUDE.md's 42 is stale. One sentence in §0 would stop a reader filing this as a new contradiction. **VERIFIED.**

### 1.7 CFO — "your money guard can't see the money"

| Strongest objection | Holds? | Required change |
|---|---|---|
| Top-5 #2, H2's first step and card 3's "first reversible step" all rest on one claim: `money_write_guard` sits in `repositories/_base.py`, and "every writer passes through them, so the guard sees all". That is false for the tables that matter. A single-line grep finds **71** `.table(...).insert/update/upsert` writes outside `_base.py`, and it is a lower bound. They include `supabase.table("rides").insert(...)` and `.update(...)` at `repositories/ride_repo.py:260,276` (the table whose FLOAT8 columns are the problem) and `supabase.table("payouts").insert(...)` at `services/stripe_payout_sync_service.py:457,464` (the table where HIST-001's B28 chain began). Other direct writers include `stripe_connect_ledger_service.py`, `driver_repo.py`, `corporate_repo.py`, several import and correction services, and 41 `supabase.table(` sites under `routes/`. There are about 90 `supabase.rpc(` calls on top, whose arguments the guard never inspects. §5.3's exit criterion, "zero `float` rejections in prod metric" for 14 days, would read green while the two most important writers go unchecked. That is false assurance on a money path. | **VERIFIED** (grep this pass) | Before the guard counts as a control: (a) publish a coverage report listing every writer to a registered money column, including direct `.table()` writes and RPC argument builders; (b) route `ride_repo` and the payout sync through the guard, or wrap them, *first*; (c) change the exit criterion to "coverage = 100 % of registered columns **and** zero rejections". Keep the semgrep allowlist until (a) is complete, and do not describe it as superseded. |

Supporting objections:
- **Pricing is not in the top 5.** Every consumer ride costs Spinr about $0.93 (STRAT-002, an **INFERRED** estimate). The programme puts M-sized architecture first and leaves the booking-fee and model memo outside the ranking as a "human decision". That is a CFO's first question, not a footnote. Required: put the memo in the pre-programme list (§6 item 0 below). Its content stays a founder decision (gate 9).
- **FLOAT8 column list overstated.** H2 cites `08_complete_schema.sql` for six FLOAT8 columns. §0 of the blueprint and `money-cra.md` support three there (`surge_multiplier`, `distance_fare`, `time_fare`). The others (`total_fare`, `tip_amount`, `driver_earnings`) are **INFERRED** from `::text::numeric` casts and a code comment. Required: relabel them.
- **The column rename has no flag rollback.** The last step of the FLOAT8 dual-write is "rename after a T4A run". A rename on `rides` is destructive for every reader, including SQL rollups 161/166/227/341/349/350 and admin queries. The §5.3 rollback ("read flag off; `_d` inert") covers dual-write, not the rename. Required: a contract step with its own rollback SQL and a compatibility view.

### 1.8 COO — "who, exactly, does all this?"

| Strongest objection | Holds? | Required change |
|---|---|---|
| The plan runs six parallel tracks and names about 15 owner roles: dispatch, fulfilment, payments, earnings, corporate, admin, a trust-and-safety owner "none today", data, platform, CI, mobile, security, growth, registry, plus several new reviewer agents. It carries 28 human-only questions. The visible history (shallow, from 2026-09-21) shows two human author identities plus agent commits, and card 6 records the admin audience as **2 users**. Consequences: card 7's money approvals "at threshold 'any amount'" would need both admins for every wallet credit and dispute refund; one absence blocks refunds. Card 2's stranded-rider admin queue and card 8's SOS response both need a night rota that TSF-001 shows does not exist. §5.1 assumes six serial queues run concurrently. | **INFERRED** (team size from shallow history and card 6's "2 users") | Set a WIP limit: at most two tracks active at once, with Track D's human items first. Collapse owners into named human hats (for example, founder, backend lead, mobile lead) with agents as reviewers, not owners. Set the approval threshold above routine support refunds, and keep the interim daily cap. Do not ship any queue that needs a response SLA (rider recovery, money approvals) until on-call exists. Turn §9 into a weekly decision slot with a named decider per question. |

Supporting objections:
- **Reviewer agents are multiplying.** Cards 10, 11 and 17 each propose a new reviewer agent (`spinr-maps-routing-reviewer`, `spinr-promotions-reviewer`, `spinr-shared-logic-reviewer`). More agents means more config for the same humans to maintain, and the tooling-hygiene history shows that drift. Required: extend existing charters first, as cards 10 and 11 already allow.

---

## §2 Contradictions found

### 2.1 Inside the blueprint

| # | Where | Contradiction | Evidence |
|---|---|---|---|
| I-1 | §1 HIST-006 row and §3 "Schema" row vs card 18 | §1 and §3 say "8 pending incl. RLS enable". Card 18 says "C43's RLS enable is live but the repo says open" (SEC-R10-002, VERIFIED-LIVE 2026-09-24), and SEC-R10-008 adds migration 450 as a 9th pending file. §1 and §3 inherited `00-history.md`'s pre-SEC-R10-002 wording. | `security.md:54-62,135,379`; `00-history.md:138,145,374` |
| I-2 | Card 2 vs its own "Clean-sheet" line; card 8; §5.1; §5.3; §8 | Card 2 marks trip PIN **BUILD** and also says the pickup-OTP flow is "already the production start path". The pickup OTP *is* the rider-shown 4-digit code the driver enters. | §1.1 above |
| I-3 | H7 vs cards 2, 3, 9, 17 | H7 records "no client-side flag SDK ... mobile behaviour is mostly server-decided". Cards 2, 3, 9 and 17 put rider-app and driver-app changes "behind a flag" without naming how the flag reaches the device. Existing mobile flags are delivered per feature through a backend response field (for example `rideless_sos_enabled`, `rider-app/app/_layout.tsx:411`). | VERIFIED (code) |
| I-4 | §5.1 "share no files by construction" | False. Cards 9-backend, 10 and 14 have no track. Card 14 edits Track A's `earnings.py` and the promotions files. Card 18's CAS helper goes in Track A's `_base.py`. | §1.6 above |
| I-5 | §5.2 #4 | "`ride_status_events` helper (B) precedes deleting the v1 insurance writers", but the stated reason is the **v2 RPC**, which is a different artefact. This makes a regulatory fix (HIST-010) wait on a discretionary event-log programme of about nine months (§1.6). | blueprint lines 450, 460 |
| I-6 | Card 3 "First reversible step" | "Every writer passes through them, so the guard sees all" is false. | §1.7 above |
| I-7 | Card 16 | The card says its clean-sheet version "replac[es] prose rules in CLAUDE.md", then says it "will *not* recommend ... rewriting CLAUDE.md's rules", and §9 Q28 adds a new rule. The intended scope is readable, but the wording contradicts itself. | card 16 lines 388, 394 |
| I-8 | §6 "should NOT be built" vs card 11 | §6 rejects "premium rider priority tiers". Card 11 proposes a paid driver pass whose benefits include "priority support". | card 11 line 318 |
| I-9 | H2 vs §0 | §0 VERIFIED three FLOAT8 columns in `08_complete_schema.sql`. H2 attributes six to the same file. | §1.7 above |
| I-10 | Card 6 vs §3 "Reading the diagram" | Card 6 promises that "a leaked replica env no longer mints super-admin tokens" by minting "on the issuing path only". The issuing routes (`routes/auth.py`, `routes/admin/auth.py`) run in the same process image on every replica. Keeping the private key off serving replicas needs a separate issuer, and §3 says "nothing in it is a new service". | `security.md:398`; blueprint line 166 |

### 2.2 Blueprint vs lane findings

| # | Finding | Lane says | Blueprint says | Resolution |
|---|---|---|---|---|
| F-1 | TSF-010 / BENCH-001 | No PIN (grep for four spellings) | BUILD PIN | **Both are wrong:** the pickup OTP exists. File a re-verification note like QUAL-003's. |
| F-2 | DISPATCH-001 | Options (a) emit or (b) doc fix, "confirm with rider-app/product" | Picks (a); blast radius "ignore unknown type" | Present it as a decision. The rider app renders `driver_assigned` as "Driver confirming". |
| F-3 | CORP-001 | Key **and** conditional status flip | Key only | Restore part (2), and make the key refund-set-specific (§1.4). |
| F-4 | DRIVER-004 | Disclose the mechanic | Cited, no step | Add a Now step (§1.2). |
| F-5 | STRAT-004 | HIGH, priority 100, disclosure "before next release" | Mentioned only in §6 as "a removal, not a build" | Add a card step and a §9 question (§1.5). |
| F-6 | COMP-005/-009/-010/-011/-017 | Filed with severities | Not cited | Regulatory annex (§1.5). |
| F-7 | STRAT-002 | "Decision needed before any acquisition spend" | Outside the top 5 | Pre-programme item (§6 item 0). |
| F-8 | SKB-002 | Deliberately staged behind driver-app readiness | Card 9: flip "Now" once readiness confirmed. §5.2 #5: every dark flag waits for the flag registry. | Exempt live PII fixes from §5.2 #5. The registry must not delay a leak fix. |

### 2.3 Blueprint vs recorded ADRs

| # | ADR | Conflict |
|---|---|---|
| A-1 | ADR-005 (Dual auth: Firebase ID token + short-lived **HS256** JWT; admin revocation by `JWT_SECRET` rotation) | Card 6 says **KEEP** "the custom-JWT model (ADR-005)" while replacing ADR-005's algorithm and its admin-revocation mechanism. Changing a recorded decision needs a superseding ADR, which the card never mentions. Card 6 also says "HS256 was chosen when there was one process (ADR-005)", but ADR-005 does not say that. Its Decision section explicitly designs for **cross-replica** revocation (Redis session mirroring). The rationale attributed to the ADR is unsupported. |
| A-2 | ADR-005 vs CLAUDE.md (not the blueprint's error, noted for the synthesizer) | ADR-005 gives admin tokens 12 h. CLAUDE.md "Token lifetimes" says 1 h. The blueprint's drift row 6 covers admin trust, not lifetime. |
| — | ADR-001, -004, -007, -010, -011, -012, -014, -016 | No conflict found. The blueprint states or respects each, and card 2's trail-based completion correctly waits on ADR-016's sign-offs. |
