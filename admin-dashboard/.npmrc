legacy-peer-deps=true
# Enforce registry to prevent hijacked mirror attacks on developer machines.
registry=https://registry.npmjs.org/
# Audit every install; fail on high-severity CVEs.
audit=true
audit-level=high
