/**
 * Re-export of the shared AI aurora background so the rider AI assistant and
 * the support chat tab (both apps) share one implementation and stay visually
 * in sync. See `shared/components/AiAuroraBackground.tsx` for the component.
 */
export { default } from '@shared/components/AiAuroraBackground';
