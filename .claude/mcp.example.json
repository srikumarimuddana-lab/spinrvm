{
  "_comment": "Template for Spinr MCP server configuration. Do NOT commit real keys. Copy the block(s) you want into .claude/settings.local.json (gitignored) under a top-level 'mcpServers' key, replacing the ${...} placeholders with real values from 1Password / settings.",
  "mcpServers": {
    "supabase": {
      "_comment": "Read-only Supabase MCP. Use a service key with row-level read access scoped to non-PII tables, OR use the anon key restricted by RLS. NEVER put the service role key here — that bypasses RLS and defeats PIPEDA guardrails.",
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest", "--read-only"],
      "env": {
        "SUPABASE_URL": "${SPINR_SUPABASE_URL}",
        "SUPABASE_ACCESS_TOKEN": "${SPINR_SUPABASE_READONLY_TOKEN}"
      }
    },
    "context7": {
      "_comment": "Context7 for up-to-date library documentation (FastAPI, Supabase, Stripe, Expo, Next.js). Free tier is fine for read-only docs; no user data ever leaves to this service.",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    },
    "stripe": {
      "_comment": "Official Stripe MCP. Use a RESTRICTED, TEST-MODE secret key (sk_test_...) scoped to the minimum resources needed — never the live secret key or an unrestricted key. Verifying webhook/idempotency fixes against real event payloads is the main use case; it should never touch live customer charges from an editor session.",
      "command": "npx",
      "args": ["-y", "@stripe/mcp", "--api-key=${SPINR_STRIPE_TEST_SECRET_KEY}"]
    },
    "vercel": {
      "_comment": "Official Vercel MCP — remote server, OAuth on first connect (no key to store here). Gives read access to admin-dashboard's deployments/build logs. The OAuth flow authorizes whatever Vercel account/team you sign in with, so use a team member scoped to this project if you don't want broader org access.",
      "url": "https://mcp.vercel.com"
    },
    "playwright": {
      "_comment": "Official Microsoft Playwright MCP (@playwright/mcp) — no credentials needed, runs a local browser. Useful for driving admin-dashboard's Next.js dev server through real UI flows (rider/driver-app web builds too) instead of reasoning about screenshots blind. Drop --executable-path if your machine already has `npx playwright install chromium` run; --headless/--no-sandbox are only needed in a headless CI-like container, not a normal dev machine with a display.",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--isolated"]
    },
    "maestro": {
      "_comment": "Official Maestro MCP (mobile-dev-inc/maestro-mcp) for rider-app/driver-app mobile E2E — matches the .maestro/ directory already in this repo. Requires the Maestro CLI installed first (`curl -fsSL \"https://get.maestro.mobile.dev\" | bash`, needs Java 17+) AND a running iOS simulator / Android emulator or attached physical device — it has nothing to drive without one. Not usable in a sandboxed/headless CI container; local dev machine only.",
      "command": "maestro",
      "args": ["mcp"]
    }
  }
}
