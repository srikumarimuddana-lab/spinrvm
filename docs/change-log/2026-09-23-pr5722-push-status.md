# PR 5722 pushed fixes — 2026-09-23

Pushed at the user's request to stop additional testing and publish completed work.

Completed: atomic cumulative driver refund holds and removal of the separate webhook writer (findings 1/2); driver dispute refund isolation (3); completion acknowledgement without queued GPS recapture (5); offer acceptance hold reset (7).

Partially completed: refresh generation schema/binding and race checks plus local-only displaced-phone logout (6); authoritative backend no-show deadline (8).

Still unfinished: SOS database status and client error handling (4); driver login displacement integration and pending-offer cleanup (6/10); mobile no-show deadline consumption (8); separation of refund adjustments from cash-paid reports (9). Uncommitted implementation/test work is deliberately excluded from this push.

Deployment: apply migrations 451/452 before these backend changes. Keep driver_refund_holds_enabled and driver_single_session_enabled false. Refund holds may be enabled only after every backend replica has removed the old Python writer. Do not enable driver single-session enforcement until the remaining login and cleanup integration is completed. No production migration, settings change, deployment, or mobile build was performed.

Verification already completed: refund PostgreSQL tests 10 passed; auth PostgreSQL tests 4 passed; refund webhook tests 4 passed; dispute suites 39 passed; refresh helper suites 75 passed; refresh route tests 29 passed; no-show backend and driver regression tests 114 passed; mobile offer suites 132 passed; socket lifecycle suite 22 passed. These are overlapping targeted suites, not a full repository run.

Rollback: keep both new flags disabled, retain audit rows and token generations, and revert individual application commits if needed. Never restore the separate Python hold writer after enabling atomic holds, decrement token generations, or silently delete refund adjustments. Existing historical excess holds require explicit reconciliation. Payout eligibility still uses the existing completed-payout timestamp heuristic.
