# C136 hand-off (WIP, stopped 2026-09-24 by Kiran)

Plan: `docs/audit/2026-09-24-c136-destination-mode-and-push-token-ownership.md`. Draft PR #5769.
Stopped mid-run on Kiran's request so someone else can take over. Nothing has been applied to or run against production.

## State per task
- **T1 destination expiry (backend): code written, NOT fully verified.** Migration `465_drivers_destination_mode_expiry.sql` (additive, rollback in header). `DESTINATION_MODE_TTL = 2h` + `is_destination_mode_active()` in `services/dispatch_service.py` (NULL/expired expiry = filter off). `destination_expires_at` added to both `matching.py` column lists and `dispatch_candidates._DISPATCH_COLUMNS`. The PostGIS RPC returns ids only, so no RPC change was needed. Going offline clears destination mode (`routes/drivers/status.py`). `GET /drivers/destination` returns `destination_expires_at` + `active`. Tests added in `tests/services/test_dispatch_service.py`, `tests/test_drivers_extended.py`, `tests/test_go_offline_clears_destination.py`. The existing `test_dispatch_service.py` passed before these edits. The full suite has NOT been re-run since.
  - Loose end: `routes/rides/estimates.py:350` has a third candidate column list without `destination_expires_at`. If that path calls the destination filter, expired-by-default will make it ignore destination mode. Check it.
- **T2 driver-app banner: code written, NOT fully verified.** `components/DestinationModeBanner.tsx`, `utils/destinationModeState.ts`, mounted in `app/driver/(tabs)/index.tsx` (idle state, stacked above ForecastStrip). The settings row shows On/Off. i18n in en/es/fr. JS only, no native change (OTA-eligible). Banner, settings and dashboard Jest tests passed (49/49) during the run.
  - Open: `__tests__/screens/driver-dashboard-route.test.ts` (a source-string contract test on index.tsx) failed once, possibly because of CRLF line endings. Must pass unchanged; do not edit the test to fit. `yarn lint` has not been confirmed clean on the touched files.
- **T3 exclusion log/metric: NOT STARTED.**
- **T4 push-token single owner: code written, NOT reviewed.** `_detach_token_from_other_users` in `routes/notifications.py`; tests in `tests/test_p3_push_notifications.py`. On a detach failure it logs an error and registration proceeds (the policy is documented in the docstring and needs Divya/Kiran's call). Uses `get_rows(limit=1000)` per column, not `$in`.
- **T5 cleanup script: DONE.** `backend/scripts/dedupe_shared_push_tokens.py`: dry-run by default, and `--apply` also needs `--i-have-signoff`. Tokens are never printed. 7/7 tests pass (`tests/test_dedupe_shared_push_tokens_script.py`). Never run against production.

## Not done
- Wave 3: no Tara verification and no Divya review has happened.
- No full pytest/jest run and no final ruff/lint pass across all touched files.

## Rollout reminder
Migration 465 must be applied BEFORE deploying this backend, because the candidate select names the new column and PostgREST would 400 every dispatch without it.
