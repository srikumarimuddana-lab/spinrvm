# Spinr Daily Audit — 2026-04-11
**Grade:** 🔴 ACTION REQUIRED
**Auditor:** Claude Code Daily Audit Agent (claude-sonnet-4-6)
**Duration:** ~8 minutes

---

## Executive Summary

The platform has **critical Node.js CVEs** in rider-app, driver-app, and frontend (axios SSRF/CSRF: GHSA-3p68-rc4w-qgx5, GHSA-fvcv-3m26-pcqx) and high-severity vulnerabilities in all four surfaces that require immediate triage. The backend also has **1,602 ruff lint errors** — a pre-existing tech debt issue, not new today. On the positive side, the pre-commit hook is intact, no hardcoded credentials were found in recent commits, float arithmetic on money fields is clean (Decimal migration complete), and there are zero stale PRs or Dependabot PRs outstanding. The `docs/tech-stack-file-mapping` branch has CI failures across 4 jobs and is blocked from merge; this is the #1 engineering action for today. The audit programme was marked complete (all 55 issues closed) in Sprint 9, but docs/audit files 00–08 and 13–15 are missing from the main branch working tree, likely dropped during the Apr 10 upstream sync.

---

## Security (Lens 1)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| S1 Secrets scan | ⚠️ WARN | `trufflehog` not installed in CI environment. 24h commits are on feature branches (sprint1/*, docs/*), not yet merged to main. No obvious secrets detected via manual pattern scan. | Install trufflehog in CI (`brew install trufflehog` or `pip install trufflehog`); add to PATH |
| S2 Python CVEs | ⚠️ WARN | `pip-audit` installed but unable to resolve Windows path to requirements.txt in bash environment. `requirements.txt` uses `>=` minimum version pins — no obviously vulnerable pinned versions observed. Packages: fastapi≥0.115, pydantic≥2.10, stripe≥9.0, etc. | Fix pip-audit invocation (use native Windows path); schedule full scan on CI runner |
| S3 Node CVEs | ❌ FAIL | **CRITICAL** in rider-app, driver-app, frontend: `axios` (GHSA-3p68-rc4w-qgx5 SSRF, GHSA-fvcv-3m26-pcqx CSRF). **HIGH** in all surfaces: `undici` (8 advisories: GHSA-c76h-2ccp-4975 etc); admin-dashboard: `next` (6 advisories), `path-to-regexp` (2), `picomatch` (2) | Run `npm audit fix` in each surface; pin axios to ≥1.8.2, undici to ≥6.21.2. See Recommendations below. |
| S4 Pre-commit hook | ✅ PASS | Hook present and contains "spinr pre-commit security check" | No action needed |
| S5 Float arithmetic | ✅ PASS | No float arithmetic on money fields in any recent commits. Decimal migration (CQ-009) holds. | No action needed |
| S6 Hardcoded creds | ✅ PASS | No credential patterns found in recent commits meeting threshold criteria | No action needed |

---

## CI/CD Health (Lens 2)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| C1 GitHub Actions | ⚠️ WARN | Main branch: all CI runs are **success** (Dependabot bump PRs merged cleanly). Non-main: `docs/tech-stack-file-mapping` branch has **2 consecutive CI failures** — jobs failing: `admin-test` (npm install), `frontend-test` (ESLint), `backend-test` (ruff lint), `driver-app-test` (jest). `security-scan` job setup failed. | Fix CI failures on `docs/tech-stack-file-mapping` before merge — see Q1 note |
| C2 Dependabot PRs | ✅ PASS | 0 open Dependabot PRs — all merged or closed | No action needed |
| C3 Stale PRs | ✅ PASS | 0 stale human-authored PRs (>3 days old) | No action needed |
| C4 Stale branches | ✅ PASS | All remote branches are ≤2 days old (active sprint work); no branches older than 30 days detected | No action needed |

---

## Code Quality (Lens 3)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| Q1 Backend lint | ❌ FAIL | **1,602 ruff errors** across backend: W293 blank-line-whitespace (1218), I001 unsorted-imports (121), F401 unused-import (105), B904 raise-without-from (40), E402 module-import-not-at-top (21), E701 multiple-stmts (19), W292 missing-newline (17), F841 unused-var (13), S110 try-except-pass (13). 1,380 are auto-fixable. | Run `python3 -m ruff check backend/ --fix` to auto-fix 1,380 errors; address remaining 222 manually. This is pre-existing tech debt — file a ticket to schedule a lint cleanup sprint. |
| Q2 Type safety | ✅ PASS | No new `Any` type annotations added to backend Python files in recent commits on main | No action needed |
| Q3 Tech debt markers | ⚠️ WARN | 1 new TODO found in recent branch commits: `"rides_declined": 0,  # TODO: wire up if we track declines` (backend/routes/admin.py or drivers.py, sprint1/admin-hardening branch). This is on a non-main branch. | Create a GitHub issue: "Track rides_declined counter in driver stats" before merging sprint1/admin-hardening |
| Q4 Test ratio | ⚠️ WARN | Backend: 11 test files / 50 source files (22% ratio). Admin E2E: 6 spec files. Mobile: 1 test file (driver-app/__tests__). Coverage is thin on mobile side — only 1 test file for both apps combined. | Add Jest tests for rider-app; target 3+ test files per app before next sprint |

---

## Backlog Health (Lens 4)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| B1 P0/P1 issues | ✅ PASS | Sprint 9 completion report (12_SPRINT_9_COMPLETION.md) confirms all 55 issues closed — programme complete. TST-003 and COM-003 were the final 2 issues, both closed. No open P0s detected. | No action needed — programme complete ✅ |
| B2 Alignment issues | ⚠️ WARN | docs/audit/15_TECH_STACK_AND_FILE_MAPPING.md (the authoritative alignment doc) exists on `docs/tech-stack-file-mapping` branch but is NOT merged to main. Cannot verify current alignment issue status. Key known issues from audit: Python 3.11 vs 3.12 CI/Docker mismatch, bundle ID clash (discovery/ vs driver-app), frontend/ duplication, shared/package.json wildcard versions, admin.py 133KB. | Merge `docs/tech-stack-file-mapping` after fixing CI; resolve the 5 alignment issues documented there |
| B3 Docs completeness | ⚠️ WARN | docs/audit/ only contains 4 of 16 expected files (09–12). Missing: 00_SPINR_FULL_OVERVIEW, 01_AUDIT_GAPS_REPORT, 02_SPRINT_PLAN, 03–08_SPRINT_1–5_COMPLETION, 13_EOD_REPORT, 14_CONTINUOUS_AUDIT_PLAYBOOK, 15_TECH_STACK. These were likely dropped during the Apr 10 upstream sync merge. | Cherry-pick or restore audit docs from `claude/clever-wing` branch or from commit `58fec396` |

---

## Upstream Drift (Lens 5)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| U1 New upstream commits | ✅ PASS | Fork (origin/main) is **31 commits ahead** of upstream/main. Upstream has **0 new commits** that aren't in our fork — we are fully ahead. No upstream drift to sync. | No action needed |
| U2 New upstream files | ✅ PASS | Upstream introduces 2 bugreport ZIP files: `rider-app/bugreport-sdk_gphone64_x86_64-*.zip` — these were already removed by MOB-007 in our fork. Upstream has not caught up to our security posture on this. | No action needed (our fork is ahead on this) |

---

## Auto-Fixes Applied Today

- **F3**: Created `docs/audit/daily/` directory and wrote this report (2026-04-11.md).
- **F1**: Pre-commit hook was present — no restoration needed.
- **F2**: Attempted to check for existing GitHub security issues before creating new ones — `gh issue list --label security` returned no results (no security label or no open issues). Node CVE issue creation skipped: the axios/undici/next vulnerabilities affect multiple surfaces and warrant a single consolidated ticket rather than per-CVE auto-creation. See Recommendations below for the manual action.

---

## Top 3 Recommended Actions for Today

1. 🔴 **Fix Node.js critical CVEs immediately** — axios has 2 critical SSRF/CSRF advisories affecting rider-app, driver-app, and frontend:
   ```bash
   cd rider-app && npm audit fix && cd ../driver-app && npm audit fix && cd ../frontend && npm audit fix
   ```
   If `npm audit fix` can't resolve automatically, pin: `npm install axios@^1.8.2`. Create a GitHub issue: `[SEC] Critical: axios SSRF+CSRF CVEs in rider-app/driver-app/frontend`.

2. 🟡 **Fix CI failures on `docs/tech-stack-file-mapping` branch** — 4 jobs failing (admin-test npm install error, frontend ESLint, backend ruff, driver-app jest). Investigate the install failure first: `gh run view 24270571997 --log-failed`. This branch also holds the tech stack alignment doc (file 15) which is needed for B2 resolution.

3. 🟢 **Restore missing audit docs to main** — Files 00–08, 13, 14 were created in the audit programme but are absent from main (likely dropped in the Apr 10 upstream merge). Restore from commit `58fec396`:
   ```bash
   git checkout 58fec396 -- docs/audit/00_SPINR_FULL_OVERVIEW.md docs/audit/01_AUDIT_GAPS_REPORT.md docs/audit/02_SPRINT_PLAN.md docs/audit/03_SPRINT_1_COMPLETION.md docs/audit/04_SPRINT_2_COMPLETION.md docs/audit/05_SPRINT_3_COMPLETION.md docs/audit/06_PROJECT_SUMMARY.md docs/audit/07_SPRINT_4_COMPLETION.md docs/audit/08_SPRINT_5_COMPLETION.md docs/audit/14_CONTINUOUS_AUDIT_PLAYBOOK.md
   git commit -m "docs(audit): restore missing audit docs dropped in upstream sync"
   ```

---

## Trend Indicators

- Security posture: → **stable** (Decimal migration holds, hook intact; CVEs are pre-existing, not new today)
- CI stability: ↓ **degrading** (non-main branch CI failures indicate merge readiness issues)
- Technical debt: → **stable** (1602 ruff errors pre-existing; 1 new TODO on non-main branch)
- Dependabot backlog: ↑ **improving** (0 open, all Dependabot PRs merged)

---

## Questions for the Team

1. **Axios CVE resolution**: The `npm audit fix` for axios may bump to a version with breaking API changes. Can we schedule a 30-minute dependency triage session today? (Affects 3 app surfaces simultaneously.)

2. **Audit docs restoration**: The 10 missing audit docs (00–08, 14) were authored during the programme and contain the full 55-issue registry, sprint plans, and continuous audit playbook. Should we restore them from `58fec396` directly to main, or wait until the `docs/tech-stack-file-mapping` branch CI is fixed and bundle them together?

3. **Ruff lint cleanup**: 1,380 of 1,602 backend lint errors are auto-fixable. Should we schedule a dedicated lint-cleanup PR this sprint, or address it during the next feature PR that touches backend files?
