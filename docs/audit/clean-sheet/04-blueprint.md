# 04 — Clean-Sheet Blueprint: Rebuild Delta cards for every L2 epic

**Lane:** R19 Chief Architect · **Wave:** W4 · **Programme:** Spinr Clean-Sheet Rebuild Audit
**Date:** 2026-09-24 · **Mode:** report-and-recommend only (this file is the lane's only write; no code, config or data was changed)

> Evidence labels on every claim: **VERIFIED** (read the code/doc directly this session, or a lane report's VERIFIED claim that I re-checked with a targeted read), **INFERRED** (reasoned from evidence), **ASSUMED** (needed to proceed; a human must confirm), **PROPOSED** (future-state recommendation), **UNKNOWN** (must be obtained; who can answer is named). Severity per `audit-framework/templates/run-audit.md`. Tie-breakers from the master prompt §4 apply throughout, above all **incremental on the live product over rewrite** — Spinr is in live testing with real users and real Stripe charges since at least 2026-07-15 (`00-history.md` §5).

---

## §0 Method, inputs, limits

**What this file is.** One §7.3 Rebuild Delta card per L2 epic (18 cards, §4), preceded by the two things the cards depend on: which of the Historian's 17 recurrence families are *architectural* rather than process (§1), and a verdict on each of the seven architecture hypotheses the role card told me to test rather than assume (§2). §3 draws the target architecture those verdicts imply; §5 sequences it on a live product; §6 lists what should not be rebuilt; §7 ranks the five deltas that matter most.

**Inputs read (all under `docs/audit/clean-sheet/` unless stated).** `00-history.md` (17 families, 38 decay signals, 27 drift rows), `W0-SUMMARY.md`, `01-inventory/epics.md` (18 epics, 90 features, 1,788 traceability rows), every W1–W3 lane file in `02-findings/` (`admin-ops`, `compliance`, `corporate`, `dispatch`, `driver-journey`, `integrations`, `money-cra`, `quality`, `reliability`, `rider-journey`, `security`, `strategy`, `support-kb`, `trust-safety-fraud`, `ux-a11y`), `matrices/` (listing only), `03-benchmark.md` (§4–§7), the rapid baseline's `blueprint-top10.md` (10 cards, consumed not repeated), the master prompt §4/§7.3, `clean-sheet-prompt/roles.md` §R19, `greenfield-extensions.md` §11/§13, `CLAUDE.md`, `docs/known-forks.md`, and ADRs 001–016 (titles; ADR-011, -014, -016 read in part).

**Targeted code reads performed (VERIFIED, this session, read-only).** Number of ride state-guard implementations (`grep 'def _require_ride_in_state'` → exactly two: `routes/drivers/_shared.py:885`, `routes/rides/_shared.py:433`); `RideStatus` enum (`models/ride_status.py:17-25`, eight values); absence of any ride event-log table (`ls backend/migrations | grep -i ride_.*event|history|transition` → only two *index* migrations, 71 and 114; no `ride_status_events`/`ride_status_history` in any `.py`/`.sql`); the transactional outbox (`services/outbox.py`, `migrations/399_transactional_outbox.sql` — one topic, `ride_receipt.v1`, producer is an `AFTER UPDATE` trigger, `outbox_receipts_enabled` default `FALSE`); idempotency-key references per route file (15 files, 373 mutating route decorators repo-wide); feature-flag plumbing (`settings_loader.get_app_settings()` — single `settings` row, 60 s in-process TTL, keep-last-known-on-rowless; 281 call sites; 37 `*_enabled: bool` fields on `schemas.AppSettings`); `@spinr/shared` wiring (`shared/package.json` exports map, `@shared/*` path alias in both mobile `tsconfig.json`s, 743 `shared` imports across the two apps' `app/`+`components/`); client generation (no `openapi`/`orval`/`swagger` dependency in any `package.json`; `shared/api/client.ts` 1,416 lines and `admin-dashboard/src/lib/api.ts` 708 lines, both hand-written; `shared/types/api/wsEvents.ts` 135 hand-typed lines vs 112 distinct `"type": "…"` WS literals emitted by the backend); `rides` money column types (`migrations/08_complete_schema.sql:130-136` — `surge_multiplier`, `distance_fare`, `time_fare` are `DOUBLE PRECISION`); pre-commit check 11 (`.claude/hooks/pre-commit:253-266`, warn-only); `docs/known-forks.md` (4 registry rows + one refresh-successor note; the admin track-page marker is still unregistered).

**Limits.**
- No production state was read: `app_settings` values (which of the 37 flags are on), `schema_migrations` (which of 553 files are applied), Fly/Railway secrets, Stripe, Sentry. Every "dark by default" statement is about the *schema default*, not the live value, unless a lane or an ADR records the live value (ADR-016 records `fare_lock_enabled = true` in production on 2026-09-11 — VERIFIED as a document claim, not re-read live).
- I did not re-audit domains. Where a card's verdict depends on how the code works today, I cite the lane's finding id and, where I re-read, the path:line. Where lanes disagree I say so.
- `02-findings/ux-a11y.md` was still being written by another lane during this pass; it was read at 815 lines (mtime 22:43 UTC) and re-read just before this file was finished (§8 records the final state).
- Uber/Lyft "do" claims are inherited from `03-benchmark.md` (VERIFIED there only where it cites a public page) or from the lane cards (mostly INFERRED). None were fetched by this lane.
- Tax, legal and regulatory statements in this file are ASSUMED unless a lane cited a primary source; they are collected in §9 rather than decided here.
- **QUAL-003 is withdrawn** (a corporate surge-bypass test exists) and is not cited anywhere below.

**How to read a card.** Each §4 card uses the §7.3 format verbatim and adds two lines the role card asked for: a **Rewrite-only** line (ideas that only make sense from zero — listed so nobody tries them on the live product) and a **First reversible step** line naming the smallest change, its blast radius and its rollback. "Now / Next / Later" follow the roadmap vocabulary; "Rewrite-only" means *do not schedule*.

---

## §1 Architectural recurrence families

**Test applied.** A family is *architectural* if the same defect would recur even with perfect process (perfect reviewers, blocking gates, up-to-date docs) because the code's shape invites it — typically because one contract is written in N places with nothing that binds them. It is *process* if a single mechanical gate (a static test, a required check, a generated doc) removes it without changing any production code shape. Several are both; the table says which half is which.

| Family (`00-history.md`) | Occurrences (VERIFIED there) | Verdict | Why (architectural shape, in one line) | Single source of truth it points at (§3) |
|---|---|---|---|---|
| HIST-001 Float-on-NUMERIC money writes | 5-link chain B28→B36 in 6 days; 13 "found while fixing the previous" entries; MONEY-006/-008 add 5 new instances outside the semgrep allowlist (`money-cra.md` §3.12) | **Architectural** | The repository layer (`repositories/_base.py`) accepts untyped dicts, so no writer knows a column is NUMERIC; three `rides` money columns are themselves FLOAT8 (`08_complete_schema.sql:130-136`, VERIFIED) so even a correct Decimal is lost at the boundary | Money: typed write boundary + ledger (§2 H2) |
| HIST-002 CarMarker fork ×5 + unregistered third copy | 34/25 commits on the two copies; 26 change-log filenames; track-page copy fixed 4× in 3 days | **Architectural** (the "surgical change" rule is the process half) | One component's logic lives in three files; the only guard diffs a props interface | Shared mobile core (§2 H5) |
| HIST-003 Legacy-ride exclusion at 59 call sites | A26 ($0.00 balances in production), A31–A34, B31; 62 change-log filenames | **Architectural** | A cohort is a JSONB marker tested by a Mongo-shaped constant at every aggregate; the product meaning flipped once (A32) and every site had to be revisited; DRIVER-002 shows two endpoints still disagree | Data model: a generated column + view (§4 card 14) |
| HIST-004 Silent swallow / warning-and-continue | B42 (53 of 55 `payment_failed` events lost), C135 (7 of 8 unclaim sites), 21 live `continue-on-error` lines | **Process, with one architectural corner** | The static-test mechanism that closed HIST-008 has not been pointed at this class (process). The corner: side effects that run inline in a request (Zoho ticket for SOS, INT-002; receipt email) are the ones that get swallowed — an outbox makes "swallow" impossible for the durable subset (§2 H3) | Background work: outbox for durable side effects |
| HIST-005 Doc-vs-code drift | loop count drifted 5×; 27 rows | **Process** | Volatile facts hand-copied into 5–10 files; fix is generated facts + a test, no production code shape changes | — |
| HIST-006 Migration drift (68 duplicate prefixes; 116 untracked; 8 pending incl. RLS enable) | G2, C44, C125 | **Platform/ops architecture** | Hand-numbered files + a manual apply step; the fix is migrate-on-deploy gated on CI evidence and an allocator | Schema: one apply path (§4 card 18) |
| HIST-007 Tracker duplicate ids | 9 duplicate `###` ids | **Process** | One 29k-line file, no allocator | — |
| HIST-008 loguru called as stdlib | C60/C65/C69/C71 | **Process (closing)** | Two logging APIs; the static test that resolves re-exports is working — this is the template, not a problem | — |
| HIST-009 RLS written for an auth model never used | C107/C123/C129/C43 | **Architectural, decided** | Backend uses the service-role key exclusively; policies are dormant by recorded decision (C108). Not re-litigated here; §4 card 6 records the trigger that would reopen it | Auth: keep the custom-JWT model (ADR-005) |
| HIST-010 Insurance-period transitions written at every state change | 17 change-log filenames; two independent implementations disagreed (2026-09-20); #5748 moved Period 2 into the claim RPC | **Architectural, in transition** | Same shape as HIST-001/-011: a derived fact (period) *recorded* as a side effect at every call site instead of *derived* from the state transition; v2 RPC is the right direction | Ride state: one transition path (§2 H1) |
| HIST-011 Non-atomic check-then-act | B19, C54, C56, C66, C77, C104, C133; DISPATCH-003 and ADMIN-OPS-004 add two more shapes this wave | **Architectural** | PostgREST-shaped helpers make read-decide-write the natural shape; no `compare_and_swap` helper reviewers can grep for; admin editors have no version column | Ride state + admin: CAS helper, optimistic version |
| HIST-012 PII reaching a new egress each time | 20 change-log filenames; three copies of the FCM exclusion set; SKB-001 finds a fourth unfiltered admin push; SEC-R10-003 finds LogRocket | **Architectural** | Redaction is per-sink; each new channel is a fresh sink | Egress: one `redact(payload, channel)` boundary (§4 card 9) |
| HIST-013 CI silently not running | C13, C76 (3,206 zero-duration runs), C92–C96 | **Process/ops** | Fix is branch-protection visibility + a "did the required check run" meta-check (QUAL-001/-004) | — |
| HIST-014 `main` red after merge | 14 consecutive red merges 2026-09-04; C122 recurred | **Process** (outside the repo's power: C21) | — | — |
| HIST-015 Fixed in one copy, never the sibling (beyond CarMarker) | notifications ×2, referral, auth ×2 files, scheduled-ride validation, two migration runners, two insurance implementations, three FCM sets | **Architectural** | Same as HIST-002 at the backend level: the codebase's default answer to "one caller needs different behaviour" is a copy | Shared primitives + contract tests |
| HIST-016 Timezone per call site | 5 occurrences | **Architectural (small)** | `scheduled_timezone` is an optional request field; the service area is the natural owner of the default | Service-area config as the rule owner |
| HIST-017 Three OTP systems (SEC-R10-016: four code paths, one plaintext by design) | 10 change-log filenames | **Architectural** | No shared short-code primitive (hash, attempt counter, lockout, dev bypass) | Auth: one `utils/otp.py` |

**Count.** 9 families are architectural (001, 002, 003, 010, 011, 012, 015, 016, 017), 1 is platform/ops architecture (006), 1 is architectural-but-decided (009), 6 are process (004, 005, 007, 008, 013, 014). The nine architectural families reduce to **one shape in five places**: a contract that exists in N copies with nothing that binds them — (a) the money write boundary, (b) the ride/insurance state transition and its side effects, (c) cross-app UI logic, (d) outbound PII, (e) small shared primitives (OTP, CAS, vendor clients, timezone). The blueprint's job is to give each a single owner and the smallest step toward it. INFERRED: the reduction is my reading of the evidence, not something a lane stated.

**Two things the families do *not* say.** They do not say the ride state machine is wrong (`dispatch.md` §1: no bare status write across ~13 sites — VERIFIED by that lane and consistent with my two-guard grep), and they do not say the money maths is wrong (`money-cra.md` §1: `calculate_fare` is pure Decimal with an exact attribution invariant). The defects are at the *boundaries* (DB column types, side effects, sinks, siblings), which is why every card below prefers a boundary fix over a domain rewrite.

---

## §2 Hypothesis tests

Each hypothesis is tested against what the code does today, then given a verdict from {adopt, adopt-incrementally, reject, insufficient evidence}, with cost (S/M/L), risk on the live product, and the smallest incremental first step.

### H1 — One ride state-machine module with an append-only event log

- **What exists (VERIFIED).** Two canonical guards (`_require_ride_in_state` for driver-owned transitions, `_require_ride_in_state_rider` for rider-owned) plus an atomic CAS `{status: expected}` filter or a single-transaction RPC at every multi-actor write site; `dispatch.md` §4 tabulates ~13 production-reachable `rides.status` write sites and found none unguarded. `RideStatus` is a single enum with eight values and no subclass. **There is no ride event-log table**; "what happened to this ride" is reconstructed from `audit_logs`, WS log lines and the insurance-period rows. Side effects (WS emit to both keys, metric, insurance-period call) are re-implemented per site: DISPATCH-001 is a forgotten rider WS emit at one site; `dispatch.md` §4 marks the `/start` dev path "metric missing" and the completion path's driver-key emit "not verified"; HIST-010 is the same omission for insurance periods, fixed by moving the write *into* the claim RPC (#5748).
- **Verdict: ADOPT-INCREMENTALLY** — one transition helper and an append-only `ride_status_events` table, **not** an event-sourced rebuild and **not** the external prompt's 11-state machine (`greenfield-extensions.md` §13 already declined it; `dispatch.md` §6 concurs: MODIFY, not REPLACE). `rides.status` remains the source of truth; the event table is an append-only *record* of every transition (actor, from, to, reason, request id, ts), the same pattern the codebase already trusts for `driver_insurance_periods` (migration 64's append-only trigger, `trust-safety-fraud.md` §f).
- **Cost:** M (one migration + one helper + ~13 site migrations of ≤ 3 files each). **Risk on live product:** low if shadow-mode first (the `matching.py` `_shadow_ids`/`spinr_dispatch_admission_shadow_total` precedent, `dispatch.md` §6); medium if any site skips shadow. **Reversibility:** high — the table is additive, the helper is dark-flagged per the `driver_availability_v2_enabled` precedent.
- **What it buys.** Structurally closes the DISPATCH-001 class (a site cannot transition without the emit and the event row), gives the auditor/regulator persona a queryable trail, and makes `dispatch.md` §4's hand-built table a query. It also gives H3 its cheapest producer: the event row *is* the outbox message for non-latency-critical consumers (analytics, admin monitoring, Zoho).
- **Smallest first step.** Additive migration `ride_status_events` (no code) → `transition_ride_status()` in a new `routes/rides/transitions.py`, called in shadow from `scheduled → searching` only (`utils/scheduled_rides.py:448-463`, the newest and best-documented site) → compare N days → migrate sites one PR each, DISPATCH-001's site first. Insurance-period writes stay a separate explicit call (Period 2 opens at *offer*, not at the status write — CLAUDE.md nuance), moving into RPCs as v2 completes.

### H2 — Double-entry ledger as the only money writer

- **What exists (VERIFIED via `money-cra.md` §3.6/§3.11, schema re-read).** `financial_events` is a single-entry, append-only ledger (migration 58; UPDATE blocked; DELETE only inside the retention purge, 289) written *after* money moves by `record_event`, which by design never raises. Double-entry legs (`financial_event_entries`, 286) are a derived projection gated by `ledger_double_entry_enabled` (schema default `False`; "assuming off" on any read error, `ledger_service.py:433`); `ledger_atomic_settle_enabled` (default `False`) gates the atomic settle RPC for card payments only. Production values of both flags: UNKNOWN. Money truth lives on `rides.*`, where `total_fare`, `tip_amount`, `distance_fare`, `time_fare`, `driver_earnings`, `surge_multiplier` are FLOAT8 (`08_complete_schema.sql`, plus every SQL rollup casting `::text::numeric` — 161/166/227/341/349/350). Driver balance is recomputed from rides + payouts on every read. `corporate_wallet_apply_delta` is the one place where the ledger *is* the only writer, and it works (§3.13; one caller lacks a key — CORP-001).
- **Verdict: ADOPT as the end state; REJECT "only writer" as a near-term step; ADOPT-INCREMENTALLY the "ledger as constraint" path.** `money-cra.md` §7 reached the same verdict independently and I agree with its reasons: the legs projection has never run at volume; `record_event`'s never-raise contract would have to invert on a live settlement path; FLOAT8 columns must migrate first or the projection drifts; ~20 readers and 44 loops compute from `rides.*`. A ledger-first request path on a product with live charges is exactly the change gate 4 exists to stop.
- **Cost:** Now S (flag on in staging, registry + write guard, absorption meter); Next M (atomic header RPCs for wallet/corporate/refund/dispute/top-up); Later M (driver-payable view with dual-read); Rewrite-only L (`rides` money columns as a read model). **Risk:** low → medium → medium. **Reversibility:** flags at every step; legs are derived and re-projectable.
- **What it buys, in order.** (1) HIST-001 stops recurring at the boundary, not at the grep — a money-column registry generated from `information_schema` plus a repository write guard that rejects `float` for registered columns (`00-history.md` HIST-001 recommendation; MONEY-006 confirms the class is still open). (2) "payout ≤ collected + funded absorption" becomes measurable (MONEY-010) and then a constraint. (3) Reconciliation collapses from eleven population-mismatched checks (MONEY-013) toward one `Σ cash == Stripe balance`.
- **Smallest first step.** The registry + write guard behind `money_write_guard` (500 in dev/test, metric in prod) — it touches `repositories/_base.py` and a CI script only, no data. Alternative considered: keep widening the semgrep allowlist (7 exclusions already; 12 files covered, 13 money-adjacent files outside it per §3.12) — rejected, it cannot see column types.

### H3 — Transactional outbox for every side effect

- **What exists (VERIFIED).** A real Postgres transactional outbox (`migrations/399`, `services/outbox.py`, `utils/outbox_worker.py`, `scripts/outbox_admin.py`): producer is an `AFTER UPDATE` trigger on `rides` so the row commits with `payment_status='paid'`; lease/ack RPCs; dead-letter redrive; payloads are ids only. Scope: **one topic** (`ride_receipt.v1`), **default off**, and it needs a worker process — which `reliability.md` REL-003 shows is unset in the real Fly/Railway configs, so every machine runs all 45 loops. Adjacent machinery: `utils/push_retry.py` (a retry queue for FCM), the Redis per-client seq/outbox replay ring for WS (`socket_manager.py:346-348`; rider client never sends `last_seq`, RIDERJ-006), and the payment-retry loop with per-attempt keys (§3.10). Side effects that today run inline and can be lost: Zoho ticket creation incl. the SOS ticket (INT-002, warning-and-drop), receipt email when the outbox is off, insurance-period writes at v1 call sites (HIST-010).
- **Verdict: ADOPT-INCREMENTALLY, scoped to durable, non-latency-critical side effects; REJECT "every".** WS fan-out (< 100 ms SLA) and the offer push (P95 < 2 s) must stay in-request with their existing replay/retry paths; routing them through a polled outbox would breach the SLAs `CLAUDE.md` sets and add a worker to the dispatch critical path. Durable effects — receipts, support/Zoho tickets, emails, statement generation, analytics/admin projections — belong in the outbox, where "swallowed" becomes impossible by construction (the corner of HIST-004 that is architectural).
- **Cost:** S per topic once the worker runs; the worker itself is an ops step (REL-003). **Risk:** low — every topic is flag-gated and the API fallback path already exists (399's rollback block). **Reversibility:** flag off; rows drain; table stays.
- **Smallest first step.** Set `SPINR_PROCESS_ROLE` on one machine group (REL-003's own step 3) and flip `outbox_receipts_enabled` in staging → add `support_ticket.v1` with the SOS Zoho ticket as the first consumer (INT-002/TSF-001's blast radius) → make `ride_status_events` (H1) the producer for admin/analytics consumers. Dependency: the worker; do not add topics before it exists.

### H4 — Idempotency keys on every mutating endpoint

- **What exists (VERIFIED).** 373 mutating route decorators. Keys already exist where money or state moves: booking (idempotency key bucketed by pickup/dropoff/2-min window + DB unique index, `rider-journey.md` §b #1), payments (17 references), safety/SOS (19; idempotent per press), admin/corporate wallet (`client_idempotency_key`, #4602), payouts, Stripe (three layers, `money-cra.md` §1.3), the v2/v3 offer RPC (`request_id`, replay-detected). Concrete gaps: the corporate wind-down debit has no key (CORP-001); `apply_adjustment`'s key is *optional* so a caller can forget it; admin editors have last-write-wins with no version (ADMIN-OPS-004, ~10 editors incl. surge/fare config); `set_driver_available` is read-then-write (DISPATCH-003).
- **Verdict: REJECT "every endpoint"; ADOPT for the money/state subset by making the key *mandatory at the wrapper*, and ADOPT optimistic concurrency (a `version`/`updated_at` precondition) for admin editors, which is the correct primitive there — idempotency does not stop two admins overwriting each other.** A generic header middleware over 373 endpoints would be speculative complexity (most admin CRUD is naturally idempotent PUTs) and would not have caught CORP-001, which is a *missing argument*, not a missing header.
- **Cost:** S (wind-down key; make `client_idempotency_key` required in `corporate_wallet_service` wrappers; static test listing the money endpoints that must carry a key); M (version column + precondition on the ~10 admin editors). **Risk:** low. **Reversibility:** git revert (no data change for the key; the version column is additive).
- **Smallest first step.** CORP-001's one-line key, then a static test in the style of `test_loguru_call_conventions.py` that fails when a `*_apply_delta`/`wallet_apply_delta` wrapper is called without a key argument.

### H5 — One shared mobile core package with enforced parity

- **What exists (VERIFIED).** `@spinr/shared` is real: a package with an `exports` map, `@shared/*` aliases in both apps, 743 imports from the two apps' `app/`+`components/`, shared theme tokens, API client, stores, hooks and types. What is missing is *enforcement*: four registered forks in `docs/known-forks.md` (CarMarker, notifications inbox, referral, auth files) plus an unregistered third marker on the admin track page (HIST-002 #7); the only mechanical guard diffs a props interface; pre-commit check 11 is warn-only by its own comment; no `spinr-*` agent owns `shared/` logic (CARTO-002); `shared/components/Button.tsx` has zero driver-app consumers (`ux-a11y.md` §14). Three live cross-app bugs trace to this layer (CARTO-002).
- **Verdict: ADOPT-INCREMENTALLY — enforce parity on the package that already exists; REJECT a monorepo/workspace re-tooling as rewrite-only.** The package is the right shape; the work is reconciliation (one `CarMarker` with driver-only props gated explicitly; delete the driver-app copy; register or reconcile the track-page copy), a blocking version of check 11 with a commit-trailer override, a contract test per remaining fork as the definition of done (the auth pair already has one), and an owner (a narrow `spinr-shared-logic-reviewer` agent, CARTO-002's recommendation).
- **Cost:** S (registry row for the track page, check 11 blocking); M (CarMarker reconcile — needs device verification, C70/C90/C103 UNKNOWN access). **Risk:** medium for the marker (rider/driver have no visual tooling; CLAUDE.md gate 6 disclosure is mandatory); low for the rest. **Reversibility:** flagged component swap.
- **Smallest first step.** Add the track-page marker to `known-forks.md` and flip check 11 to blocking when both siblings exist and only one is staged (override via trailer). No production code changes.

### H6 — Contract-first API with generated clients

- **What exists (VERIFIED).** FastAPI already emits OpenAPI (Schemathesis fuzzes 532 GET operations from it, CLAUDE.md Testing). Nothing consumes it for clients: no generator dependency in any `package.json`; `shared/api/client.ts` (1,416 lines, carries auth/refresh interceptor logic) and `admin-dashboard/src/lib/api.ts` (708 lines, **234 commits — the most-changed file in the repo**, `00-history.md` §2) are hand-written; response shapes are hand-typed in `shared/types/api/*`. The WS contract is the weakest point: 135 hand-typed lines in `wsEvents.ts` against 112 distinct event-type literals emitted by the backend; `domain-dispatch.md` documents a `driver_assigned` event nobody emits (DISPATCH-001, drift row 9); the rider client never sends `last_seq` while the driver client does (RIDERJ-006); both referral endpoints carried a dead `referral_link` field until 2026-09-14.
- **Verdict: ADOPT-INCREMENTALLY for *types and drift detection*; REJECT a generated full client now; INSUFFICIENT EVIDENCE that generation would reduce the admin `api.ts` churn** (its 234 commits may be feature velocity, not drift — the Historian could not filter commits by message). A generated client would have to reproduce the token-refresh interceptor and the offline/cached client, and would touch every screen — L effort on a live product with no mobile E2E (QUAL-004, B25).
- **Cost:** S (CI job: snapshot `openapi.json` on `main`, diff on PR, fail on removed field/changed type — additive-only evolution); S–M (generate TS types only, into `shared/types/api/generated.ts`, adopt per endpoint); M (WS event schema: one backend enum → JSON schema → a `shared` test that every `wsEvents.ts` type matches and every backend literal is declared). **Risk:** low — none of it changes runtime behaviour. **Reversibility:** delete the job.
- **Smallest first step.** The OpenAPI snapshot-diff CI job (catches the referral-field class before it ships), then the WS event enum + test (catches DISPATCH-001's class). Generated *types*, not clients, and only where a screen is being touched anyway.

### H7 — Feature flags as a first-class service

- **What exists (VERIFIED).** One `settings` row (`id='app_settings'`) holding 37 `*_enabled` booleans plus vendor keys, read through `get_app_settings()` at 281 call sites with a 60 s in-process TTL and keep-last-known-on-rowless (a real kill-switch safety property, `settings_loader.py:46-68`). Per-flag read-failure semantics are a **recorded decision** (ADR-011, amended 2026-09-21): no universal rule; each money-path flag states fail-open/closed at its read site. Rollout is global (no per-area, per-cohort or percentage targeting), there is no flag registry (owner, default, failure mode, created, sunset), no client-side flag SDK (admin reads its `admin_theme_v2_enabled` through the settings endpoint; mobile behaviour is mostly server-decided), and a growing set of "built, dark, unverified" flags: `driver_availability_v2_enabled` (22 references), batch-claim/v3 dispatch (DISPATCH-002), `ledger_double_entry_enabled`, `ledger_atomic_settle_enabled`, `outbox_receipts_enabled`, `minimal_fcm_offer_payload_enabled` (SKB-002: a live PII gap sits behind it), eligibility recheck (COMP-002: every SK rule beyond document expiry is dark), `dual_approval_exports_enabled` (B10), `refresh_successor_commitment_enabled`.
- **Verdict: ADOPT-INCREMENTALLY as a *registry and lifecycle*, MODIFY not REPLACE the store; REJECT buying a flag vendor** (one more sub-processor with a data-residency question, cost, and the `app_settings` pattern already gives rotation-without-redeploy — CLAUDE.md Critical Conventions). Do not re-litigate ADR-011. The architectural gap is not the store; it is that flags have no lifecycle, so dark flags accumulate and the audit keeps finding "the fix exists but is off" (DISPATCH-003, SKB-002, COMP-002, B10, MONEY §3.11).
- **Cost:** S (generate `docs/flags.md` from `AppSettings` with owner/default/failure-mode/sunset; a test that every `*_enabled` field has those four annotations and that no flag is older than N days without a decision); M (per-service-area targeting for consumer-visible flags via `service_areas` config — the "rules as configuration" direction of `greenfield-extensions.md` §10). **Risk:** none for the registry; low for targeting. **Reversibility:** trivial.
- **Smallest first step.** The generated registry + annotation test. It also closes HIST-005 for this particular fact (the flag inventory in `admin-ops.md` §6 is hand-maintained today).

**Summary of verdicts.** H1 adopt-incrementally · H2 adopt (destination) / reject (near-term "only writer") / adopt-incrementally (constraint path) · H3 adopt-incrementally, scoped · H4 reject "every", adopt for money/state + optimistic concurrency for admin · H5 adopt-incrementally on the existing package · H6 adopt-incrementally (types + drift), reject generated client · H7 adopt-incrementally (registry + lifecycle), reject vendor.

---

## §3 Target architecture — single sources of truth

The target is not a new system. It is today's system with **one owner per contract** where §1 found N. Everything below is PROPOSED unless marked; each row names the smallest step, and every step is one of the §4 cards.

| Contract | Today (VERIFIED) | Single source of truth (target) | First step |
|---|---|---|---|
| **Ride state** | `rides.status` + 2 guards + per-site CAS; side effects per site; no event log | `rides.status` stays authoritative; **one `transition_ride_status()`** does CAS + `ride_status_events` row + WS emit to both keys + metric; multi-actor transitions move into RPCs as v2/v3 finish | additive `ride_status_events` + shadow helper on `scheduled→searching` |
| **Insurance period** | derived (`derive_insurance_period`, pure) *and* recorded at ~6 call sites; v2 RPC commits Period 2 with the claim | **Derivation is the truth; recording happens inside the same transaction as the state change** (v2 RPCs); reconciler is the only repair path; v1 `record_period_transition` sites deleted when v2 is on | finish #5748's rollout; delete v1 sites one per PR |
| **Money** | Decimal in code, FLOAT8 on `rides`, single-entry ledger written after the fact, legs dark, balances recomputed per read | **Typed write boundary** (registry + guard) now; **atomic header per settlement type** next; **ledger balance view** later; `rides` money as read model rewrite-only | registry + write guard behind `money_write_guard` |
| **Flags** | one settings row, 37 booleans, per-flag failure semantics (ADR-011), no lifecycle | same store + **generated registry with owner/default/failure-mode/sunset** + per-area targeting for consumer-visible flags | generated `docs/flags.md` + annotation test |
| **Shared mobile code** | `@spinr/shared` (743 imports) + 4 registered forks + 1 unregistered; props-only parity; warn-only hook | **one implementation per component; forks need a contract test to exist**; check 11 blocking; a shared-logic reviewer agent | register track-page marker; check 11 blocking |
| **API & WS contracts** | FastAPI OpenAPI unused by clients; hand-typed WS events (135 lines vs 112 backend literals) | **OpenAPI snapshot diffed in CI; one backend WS event enum → JSON schema → shared test**; generated TS *types* adopted per endpoint | snapshot-diff job |
| **Background work** | 45 loops on every replica; outbox for one topic, off; push retry queue; WS replay ring | **`SPINR_PROCESS_ROLE` split; outbox for durable side effects (receipts, tickets, emails, projections); in-request for latency-critical (WS, offer push)** | role split on one machine group; outbox on in staging |
| **PII egress** | per-sink scrubbers, three FCM exclusion sets, a fourth unfiltered push | **one `egress.redact(payload, channel)`** every `send_*`/WS/AI/Zoho/exception path goes through, with a static test | consolidate the three FCM sets into one function |
| **Schema** | hand-numbered files, manual apply, 8 pending incl. RLS enable | **migrate-on-deploy gated on the CI/Security evidence link** (the pattern `2026-09-23-fly-deploy-gate.md` introduced for Fly); timestamp prefixes | nightly `--status` alert first, then the deploy job |
| **Rules (tax, bylaw, timezone, eligibility)** | prose in three docs; timezone optional per request; eligibility flags | **service-area rulesets as configuration** (`greenfield-extensions.md` §10) and `compliance/obligations.yaml` (`compliance.md` §6) | server-side default `scheduled_timezone` from the service area |

```mermaid
flowchart LR
  subgraph Clients
    RA[rider-app] --> SH[(@spinr/shared\none impl per component\ncontract tests)]
    DA[driver-app] --> SH
    AD[admin-dashboard]
  end
  SH -- "OpenAPI snapshot + WS event schema (CI-diffed)" --> API

  subgraph Backend[FastAPI, one process image, role-split by SPINR_PROCESS_ROLE]
    API[routes/*] --> TR[transition_ride_status\nCAS + event row + WS + metric]
    TR --> RS[(rides.status\nride_status_events)]
    TR -. "same txn (v2 RPC)" .-> IP[(driver_insurance_periods\nappend-only)]
    API --> MW[money write boundary\nregistry + guard]
    MW --> LG[(financial_events\n+ legs when flag on)]
    MW --> RD[(rides money cols\nFLOAT8 -> NUMERIC dual-write)]
    API --> FL[get_app_settings\nflag registry + lifecycle]
    API --> EG[egress.redact]
    EG --> FCM[FCM] & WS[WS fan-out\nin-request, replay ring] & AI[AI] & ZH[Zoho]
    RS -- "trigger" --> OB[(outbox_messages\nreceipts, tickets, emails, projections)]
    OB --> WK[worker role\nlease/ack, dead-letter]
  end

  subgraph Platform
    MG[migrate-on-deploy\ngated on CI evidence] --> DB[(Supabase ca-central-1)]
    LG --> RC[reconcile: sum cash == Stripe]
  end
```

**Reading the diagram.** Nothing in it is a new service, queue, or vendor. The three boxes that do not exist today are `transition_ride_status`, the money write boundary, and `egress.redact`; the outbox, worker role, flag store and shared package exist and are under-used. The one platform change is the migrate-on-deploy job. This is deliberately consistent with `03-benchmark.md`'s HOLD verdicts (no Kafka, no mesh, no native rewrite, no own maps, no ML) and with ADR-014 (no distributed tracing until C11 is live and still insufficient).

---

## §4 Rebuild Delta cards — one per L2 epic (18)

Card order follows `01-inventory/epics.md` §2. Cards 1–5 are the live-tested money/ride surfaces and carry the most detail; cards 15–18 are engineering epics and are shorter. Every card separates **Rewrite-only** ideas from the incremental path and names the **First reversible step** with blast radius and rollback.

## Epic: Ride Booking & Matching
- Verdict per inherited pattern: **KEEP** the two-guard + atomic-CAS convention (`dispatch.md` §1, VERIFIED); **KEEP** the HMAC `estimate_token` that locks the shown surge to the charged one and the 3.5 s Directions wait (accepted SLA exception, CLAUDE.md); **MODIFY** the legacy default dispatch path (DISPATCH-002: whole-batch abort on a stale `ride_offers` row; the v3 RPC path already does per-row `ON CONFLICT DO NOTHING`); **MODIFY** the rider-side contract (DISPATCH-001: no rider event at `driver_assigned`); **MODIFY** float on the estimate path (MONEY-006: `features.py:900` rounds float sums on the canonical estimate); **KEEP** surge engine and cap; **BUILD** the missing client wiring for multi-stop (RIDERJ-001: server-side complete, zero client callers).
- Keep (already best-in-class): booking idempotency key + DB unique index (double-tap safe); pre-check + DB race path for "one active ride"; surge defence-in-depth (four builder clamps + charge-site clamp, corporate bypass ordered after token resolution, unconditional scheduled-ride exemption — `money-cra.md` §3.1–3.2); offer-timeout ordering (CAS revert *before* release logic, `matching.py:1794-1858`); quest enrichment batched off the hot path.
- Uber/Lyft do: internally event-sourced dispatch where every transition is a durable event fanned out to every consumer (INFERRED, `dispatch.md` §6); micro-zone surge (VERIFIED in `03-benchmark.md` §4 as coarser at Spinr, by design).       Spinr today: CAS write → immediate WS emit in the same task; the "event" is implicit; 13 sites each remember their own side effects.
- Clean-sheet Spinr would: route every `rides.status` write through one `transition_ride_status()` that also appends `ride_status_events` and emits to both connection keys (H1); fold the legacy dispatch insert into the same per-row conflict tolerance the v3 RPC already has; keep the rider informed at assignment. Why (the edge it creates): the DISPATCH-001 class becomes impossible rather than review-dependent; a regulator/plaintiff can be shown one queryable trail per ride; match-rate and cancellation KPIs read from one table.
- How (architecture/pattern): additive `ride_status_events` (append-only trigger like migration 64); helper in `routes/rides/transitions.py`; shadow-mode compare per the `_shadow_ids` precedent; DISPATCH-002 fixed by a `NOT IN (SELECT driver_id FROM ride_offers WHERE ride_id = …)` pre-filter or per-row `ON CONFLICT DO NOTHING` (constraint from migration 100 already exists).   Who: dispatch owner + `spinr-dispatch-reviewer` and `spinr-realtime-reliability-reviewer` per PR.   When: **Now** (DISPATCH-001 one-line emit; DISPATCH-002 pre-filter; MONEY-006), **Next** (event table + helper in shadow), **Later** (all sites through the helper; CAS into RPCs), **Rewrite-only** (below).
- Incremental path from today (no big-bang): 1 DISPATCH-001 rider emit (one site) → 2 DISPATCH-002 pre-filter on the legacy path (additive, no migration) → 3 `ride_status_events` migration → 4 helper in shadow on `scheduled→searching` → 5 one site per PR (≤ 3 files) → 6 rider-app multi-stop entry point behind a flag (RIDERJ-001) or a documented "backend-only" decision.
- Cost/effort: S (1–2) · M (3–5) · M (6) · Risk: low with shadow; medium for 6 (re-price disclosure on a live surface, gate 4 dry run) · Reversibility: flag per step · Build.
- Advantage type: operational + trust (auditable trail); the surge cap and pre-booking disclosure are the *defensible* trust edge (`03-benchmark.md` §4 #3) and must not be touched by any of this.
- "Why not?": the state machine was correct enough that nobody was forced to centralise it; the miss (DISPATCH-001) is a doc-vs-code drift, not a crash. Simpler: fix DISPATCH-001/-002 alone and stop — gets the two live defects without the event log; the event log is justified only because the pattern is a class (HIST-010 is the same shape).
- **Rewrite-only:** an 11-state machine (`greenfield-extensions.md` §13), event-sourced `rides` (status rebuilt from events), a separate dispatch service.
- **First reversible step:** DISPATCH-001's one WS send at `matching.py` claim site. Blast radius: rider WS handler must ignore an unknown type (it already handles `ride_status_changed` generically — `rider-journey.md` §b). Rollback: revert the commit; no data.
- Cites: DISPATCH-001/-002/-003/-004, MONEY-006, RIDERJ-001, HIST-010/-011, CARTO §5 (S-book-04 collision).

## Epic: Ride Fulfillment
- Verdict per inherited pattern: **KEEP** OTP-gated trip start (atomic CAS `{id, driver_id, status}` + Period 3 write + metric, `ride_flow.py:1220-1252`) and the "driver's own transition not echoed to the driver" decision (documented, `socket_manager.py:484-529`); **KEEP** WS auth path (SEC-R10-019 PASS) and the REST resync on every `onopen` (RIDERJ-006); **MODIFY** the abandoned-`in_progress` ride path (DRIVER-005/RIDERJ-002/BENCH-009: alert-only by design; rider locked out of booking indefinitely); **MODIFY** rider WS client to send `last_seq` (RIDERJ-006 asymmetry); **BUILD** trip PIN/identity verification (TSF-010/BENCH-001); **MODIFY** `set_driver_available` to an atomic UPDATE or finish `driver_availability_v2` (DISPATCH-003).
- Keep (already best-in-class): `release_driver_and_close_period()` as the single release implementation replacing five hand-mirrored copies (`insurance_periods.py:272-410`) — the one place HIST-015 was actually reconciled; `DriverLocationStatus` degrades by elapsed time with a live region; `evaluate_gps_plausibility` gating location-derived writes.
- Uber/Lyft do: RideCheck / Smart Trip Check-In proactive prompts on anomalies; trip PIN; (VERIFIED public pages via `03-benchmark.md` BENCH-001/-009).       Spinr today: `stale_in_progress_ride_alerter` (10 min, read-only) + Sentry; no PIN; rider has "Location updates delayed" and SOS.
- Clean-sheet Spinr would: keep "never `cancelled` after `in_progress`" (CLAUDE.md invariant) but add a **rider-visible, time-boxed recovery** — after N minutes of driver silence the rider gets copy, a "confirm I've arrived" affordance that requests an admin-mediated completion with a fare computed from the last plausible GPS fix (ADR-016's classified trail), and is unblocked from booking; add a 4-digit trip PIN shown to the rider and entered by the driver at start (the existing pickup-OTP flow is the natural host — it is already the production start path). Why: closes the two safety/dispute table-stakes the benchmark ranks #2 and #8; unblocks a stranded rider without violating the state machine.
- How: recovery = a new `completion_requested_by_rider` flag + admin queue (additive), not a new status; PIN = extend `verify_pickup_otp` with a rider-visible code (flagged); `last_seq` = client change in `useRiderSocket.ts` only.   Who: fulfilment owner + `spinr-safety-sos-reviewer` (PIN) + `spinr-insurance-period-auditor` (any completion path touches Period 3 close).   When: **Now** (`last_seq`, DISPATCH-003 atomic UPDATE), **Next** (rider recovery copy + admin queue; PIN flagged), **Later** (auto-completion from trail — needs ADR-016's three product sign-offs first).
- Incremental path: 1 rider `last_seq` → 2 atomic `is_available` UPDATE (single-point fix, all release paths funnel through `set_driver_available`) → 3 rider copy + "request completion" affordance behind `stale_ride_rider_recovery_enabled` → 4 admin queue for it → 5 trip PIN behind a flag, pilot cohort → 6 trail-based auto-completion only after ADR-016 sign-offs.
- Cost/effort: S / S / M / S / M / L · Risk: low, low, medium (live rider surface — announce), low, medium (touches trip start), high · Reversibility: flags · Build.
- Advantage type: trust/safety; PIN is table stakes (feature, copyable); rider recovery is operational.
- "Why not?": the invariant "never cancel after start" is correct and was applied literally — nobody designed the *other* exit; PIN was never prioritised at pilot scale (`trust-safety-fraud.md` §f).
- **Rewrite-only:** an offline-first trip ledger on device with post-hoc reconciliation (rural SK, `greenfield-extensions.md` §12) — worth designing later, not on this codebase's current WS/REST shape.
- **First reversible step:** `useRiderSocket.ts` sends `last_seq` on reconnect. Blast radius: rider-app hook only; server already supports it. Rollback: revert; the full-state refetch still covers it.
- Cites: DRIVER-005, RIDERJ-002/-006, DISPATCH-003, TSF-010, BENCH-001/-009, HIST-010, ADR-016.

## Epic: Ride Completion & Payments
- Verdict per inherited pattern: **KEEP** pure Decimal `calculate_fare` with the attribution invariant and nightly check; **KEEP** hold = quote + incremental auth; **KEEP** three-layer Stripe idempotency and the cumulative refund RPC; **KEEP** the trust-first absorption policy but **METER** it (MONEY-010); **REPLACE** float enforcement (registry + write guard, HIST-001/MONEY-006); **MODIFY** ledger (turn legs on in staging, then atomic headers per settlement type — H2); **MODIFY** completion re-pricing to recompute tax (MONEY-007; live impact depends on `fare_lock_enabled`, which ADR-016 records as `true` in production on 2026-09-11 — VERIFIED as a document claim, so MONEY-007's live exposure is likely limited to re-priced rides when the lock does not apply, INFERRED); **MODIFY** reconciliation populations (MONEY-013); **BUILD** rider self-serve dispute (RIDERJ-003/BENCH-002 — the backend `POST /disputes` exists with zero rider-app callers); **BUILD** top-up chargeback handling (MONEY-009: `apply_refund` exists with zero callers).
- Keep (already best-in-class): `financial_events` append-only with GUC-guarded purge; `payment_retry` per-attempt keys + CAS on `payment_retry_count`; orphaned-hold reconciler; refund `dedupe_key` derived from the Stripe id.
- Uber/Lyft do: ledger service as system of record, ride rows carry references (INFERRED, `money-cra.md` §7); in-app dispute with reason codes (VERIFIED public help pages, BENCH-002).       Spinr today: money on `rides.*` FLOAT8; ledger after the fact; drivers and admins can file structured disputes, riders cannot.
- Clean-sheet Spinr would: ledger-as-constraint now, ledger-first later (H2); one `shared/money` formatter for all three apps (three independent float formatters today, `money-cra.md` §7); the rider dispute screen calling the endpoint that already exists. Why: 0 %-commission is only provable if the arithmetic is boundary-safe and the driver can add it up (card 4); a self-serve dispute is the cheapest support-deflection on the list.
- How: registry generated from `information_schema` in CI + guard in `repositories/_base.py` behind `money_write_guard`; `ledger_double_entry_enabled` on in staging with the projection run over history; extend `ledger_atomic_settle_enabled`'s RPC shape to wallet/corporate/refund/dispute/top-up; a `stripe_fee` column (STRAT-002) so margin is measurable; rider-app dispute screen over `POST /disputes`.   Who: payments owner + `spinr-money-auditor` on every step; accountant for the chart of accounts before "Next".   When: **Now** (guard, meter, staging legs, rider dispute), **Next** (atomic headers, MONEY-007/-009/-013), **Later** (balance view, FLOAT8 dual-write per §3.6's 5-step plan), **Rewrite-only** (rides money columns as read model).
- Incremental path: 1 registry + guard (dark) → 2 legs on in staging, `reconciliation.py` leg checks green 7 days → 3 rider dispute screen behind a flag → 4 atomic header RPC per settlement type → 5 `driver_payable` view + dual-read → 6 FLOAT8 `_d` twin columns, trigger-mirrored, dual-read, rename after a T4A run → 7 read model (rewrite-only).
- Cost/effort: S / S / S / M / M / M / L · Risk: low, low, low, medium (settlement path — gate 4 dry run against `mock_supabase_client` + before/after scenario), medium, medium (hottest table; nullable adds are catalog-only) · Reversibility: flags; `_d` columns inert if the flag is off · Build (a hosted ledger would move CRA-retained records out of `ca-central-1` — rejected, `money-cra.md` §7).
- Advantage type: trust + operational; table stakes, defensible only as execution.
- "Why not?": money truth grew on the ride row in April; the ledger was bolted on with "never fail a payment for bookkeeping" as the priority — right for launch, wrong for audit. Simpler: atomic headers + absorption meter alone close lost-record and unmeasured-liability risk without double entry.
- **Rewrite-only:** ledger-first request path; dropping money columns from `rides`; Stripe Connect destination charges with the platform balance as the ledger (a merchant-of-record change — §9).
- **First reversible step:** `money_write_guard` registry + guard, default off. Blast radius: `repositories/_base.py` write helpers (every writer passes through them, so the guard sees all; it *rejects* nothing until the flag is on). Rollback: flag off.
- Cites: MONEY-006/-007/-008/-009/-010/-011/-013, RIDERJ-003, BENCH-002, HIST-001/-011, STRAT-002, ADR-016.

## Epic: Driver Earnings & Payouts
- Verdict per inherited pattern: **KEEP** the offer card showing `driver_earnings` (not gross) with distance/duration/surge/destination (VERIFIED `driver-journey.md` §5); **KEEP** weekly auto-payout with the GST/SIN compliance gate and per-outcome notification; **KEEP** `driver_earnings_with_tip` recompute-from-source; **MODIFY** the earnings *display* layer — five endpoints each decide legacy inclusion independently (DRIVER-002 extends HIST-003) and two sum float tips (MONEY-001); **BUILD** one driver trip-receipt view (`driver-journey.md` §4); **BUILD** the driver-app UI for instant cash-out (BENCH-004/§7 #10: backend built, unreachable); **MODIFY** T4A box mapping (MONEY-012, ASSUMED — accountant); **REMOVE** the payout-schedule ambiguity (STRAT-012: `interval: "manual"`, one payout ever, never filed as an item).
- Keep (already best-in-class): `platform_share` does not exist as a variable — 0 % is structural (`money-cra.md` §1.1); Connect ledgers explicitly "not income records".
- Uber/Lyft do: Instant Pay at ~1.5 % (VERIFIED, `03-benchmark.md` §7 #10); per-trip breakdown with deadhead (`strategy.md` §10).       Spinr today: instant payout fee exists in policy, no driver-app UI; per-trip stack not shown in one place.
- Clean-sheet Spinr would: one `driver_trip_receipt` view that the trip list, period statement, T4A and the ledger all read from (the rider receipt already has this shape in `receipt_pdf.py`); publish the per-ride cost stack to drivers once STRAT-002's `stripe_fee` exists ("this ride cost Spinr $x; you kept $y") — the genuinely novel trust product in `strategy.md` §10. Why: turns a structural 0 % into a *felt* 0 % that a competitor cannot announce their way into.
- How: fix DRIVER-002/MONEY-001 (one file each) → per-trip tax line on `/earnings/trips` → `driver_trip_receipt` view behind a flag → fold the five endpoints into wrappers over it; cash-out button over the existing endpoint behind a flag.   Who: earnings owner + `spinr-money-auditor`; accountant for MONEY-012/T4A.   When: **Now** (DRIVER-002, MONEY-001, STRAT-012 decision), **Next** (view, cash-out UI), **Later** (cost-stack disclosure — needs STRAT-002 measurement first).
- Incremental path: 1 DRIVER-002 + MONEY-001 → 2 tax line → 3 view + flag, pilot cohort → 4 endpoints as wrappers → 5 cash-out UI flagged → 6 cost-stack line.
- Cost/effort: S / S / M / M / S / M · Risk: low (display-only until 5; 5 moves money and needs gate 4) · Reversibility: flags · Build.
- Advantage type: **trust/data — the one genuinely defensible edge in this file** (a driver who has personally verified the arithmetic recruits better than an ad).
- "Why not?": each earnings endpoint was added surgically; nobody owned "the number the driver sees". Simpler: steps 1–2 alone capture most of the trust value.
- **Rewrite-only:** driver balance as a ledger account (card 3's Later step delivers it incrementally instead).
- **First reversible step:** DRIVER-002 — make the lifetime total and the itemised list use one legacy-inclusion rule. Blast radius: two `earnings.py` functions; `EXCLUDE_LEGACY_RIDES` has 59 other references (HIST-003) — touch only these two. Rollback: revert.
- Cites: DRIVER-002/-004, MONEY-001/-010/-012, BENCH-004, STRAT-002/-012, HIST-003.

## Epic: Corporate / B2B Billing
- Verdict per inherited pattern: **KEEP** the row-locked `SECURITY DEFINER` RPC pair (`corporate_wallet_apply_delta` / `corporate_allowance_apply_delta`) as the single writer — this is the one domain where H2's end state already exists; **KEEP** the atomic-claim cascade pattern; **MODIFY** idempotency from optional to mandatory (CORP-001, H4); **MODIFY** `update_corporate_wallet_config` with a column allow-list (CORP-003); **BUILD** a migration-diff linter for the two RPCs (CORP-002: migration 277 silently regressed 258's fix, re-fixed in 297/319); **MODIFY** statements (MONEY-011/COMP-015: no GST registration number — ASSUMED whose number belongs there); **MODIFY** signup consent (CORP-004/COMP-008); **MODIFY** go-to-market (STRAT-010: dark, unpriced, no MRR metric — the declared profit engine cannot be bought).
- Keep (already best-in-class): membership-derived tenant scoping (`_ensure_member`/`require_company_admin`); dedupe order (Stripe PI → (wallet, ride, type, scope) → client key) in migration 297/376.
- Uber/Lyft do: credit-limit-per-employee against a company payment method (INFERRED, `corporate.md` §8) — same shape.       Spinr today: same shape as two RPCs; unsellable self-serve.
- Clean-sheet Spinr would: keep the RPC pair; make the key a required parameter of every wrapper; lint RPC redefinitions against the last-applied version rather than whichever file the author branched from; ship duty-of-care as the corporate SKU (check-in loop + trip share + insurance-period proof — `strategy.md` §10 "genuinely novel"). Why: CORP-002 has already happened once; corporate is the only revenue line whose economics improve with volume (`strategy.md` §9).
- How: `client_idempotency_key` required in `corporate_wallet_service` wrappers + static test; `scripts/lint_rpc_migrations.py` reusing the RLS tier's throwaway Postgres; plan rows + one billed pilot company.   Who: corporate owner + `spinr-corporate-billing-reviewer`; founder for pricing.   When: **Now** (CORP-001/-003, consent), **Next** (linter, pilot billing, MRR metric), **Later** (duty-of-care SKU).
- Incremental path: 1 wind-down key → 2 allow-list → 3 wrapper keys mandatory + test → 4 RPC linter (blocking from day one — `corporate.md` §8's own warning against the advisory-then-forget pattern) → 5 plan rows + pilot → 6 SKU.
- Cost/effort: S / S / S / M / M / L · Risk: low; 5 is visible to a pilot company (announce) · Reversibility: revert; flags · Build.
- Advantage type: operational (no double-spend) + trust ("every money-moving RPC call is keyed, provably") + the only **cost**-type edge in the model.
- "Why not?": the RPC pair was built right; the wrappers grew optional parameters surgically. Simpler: CORP-001 alone (one line).
- **Rewrite-only:** none — `corporate.md` and `strategy.md` both say nothing here needs a rewrite.
- **First reversible step:** `client_idempotency_key=f"corp-close-{wallet_id}"` at `corporate_wallet_winddown_service.py:164`. Blast radius: one caller; the RPC's dedupe path (376) already exists. Rollback: revert; a duplicate key on an already-closed wallet is a no-op by design.
- Cites: CORP-001/-002/-003/-004/-005, MONEY-009/-011, COMP-008/-015, STRAT-010/-011.

## Epic: Authentication & Authorization
- Verdict per inherited pattern: **KEEP** the custom-JWT model with role re-read from `users` per request (ADR-005; do not move to Supabase-issued sessions — C108's dormant-RLS decision stands) and admin per-request `admin_staff` re-validation (`dependencies/__init__.py:355-360`; note CLAUDE.md's "admin JWTs fully trusted" is drift row 6 and false in the direction that matters); **KEEP** refresh rotation with reuse detection, `token_version` revocation, audience separation; **MODIFY** HS256 → asymmetric with `kid` (SEC-R10 card, `security.md` §5, Next); **MODIFY** the refresh grace-window ordering (SEC-R10-004); **REPLACE** three-to-four OTP implementations with one `utils/otp.py` (HIST-017/SEC-R10-016); **MODIFY** `OTP_PEPPER` fallback to `JWT_SECRET` (SEC-R10-005); **MODIFY** the env-var privileged paths (SEC-R10-006) and the fail-open Redis denylist for admin single-session logout (SEC-R10-007); **BUILD** the `load_ride_for(caller, ride_id)` authz helper + static test (SEC-R10-011: 55 of 102 id-scoped routes are fetch-then-compare with nothing enforcing the compare).
- Keep (already best-in-class): `algorithms=[...]` pinned on every decode; WS auth (SEC-R10-019 PASS); real-IP guard test on the auth pair (`known-forks.md` row 4).
- Uber/Lyft do: asymmetric tokens verified at the edge with rotating key sets (INFERRED, `security.md` §5).       Spinr today: one shared secret is mint key, verify key, rate-limiter decode key and OTP pepper fallback on every replica of both providers.
- Clean-sheet Spinr would: one OTP primitive, one authz loader, asymmetric mint on the issuing path only, `/.well-known/jwks.json`. Why: a leaked replica env no longer mints super-admin tokens; the next route copied without an ownership check fails a test instead of leaking a row.
- How: per `security.md` §5's 5-step dual-verify plan (HS256 accept stays on until step 4); `utils/otp.py` adopted by login, corporate e-mail and pickup OTP one at a time; authz helper + a static test in the `test_loguru_call_conventions.py` style.   Who: backend owner + `spinr-security-auditor`; whoever runs secret rotation.   When: **Now** (SEC-R10-001 rotation decision — §9; OTP pepper; authz helper), **Next** (asymmetric JWT, OTP consolidation), **Later** (JWKS for partners).
- Incremental path: 1 rotate the 2026-07-30 key or prove it was (human) → 2 `OTP_PEPPER` set explicitly → 3 `load_ride_for` helper + static test, refactor 55 routes mechanically → 4 `utils/otp.py` under login OTP first → 5 dual-verify ES256 dark → 6 mint ES256 for admin audience (2 users) → 7 mobile after one refresh cycle → 8 HS256 accept off.
- Cost/effort: S / S / M / M / M / S / S / S · Risk: 3 is a pure refactor; 5–8 medium (a broken dual-verify logs everyone out — dark launch) · Reversibility: high while HS256 accept is on · Build (PyJWT).
- Advantage type: trust/operational.
- "Why not?": HS256 was chosen when there was one process (ADR-005); OTP paths were each hardened where the incident was. Simpler: keep HS256, stop using it as the pepper, rotate on a schedule with a dual-secret window.
- **Rewrite-only:** Supabase-issued user sessions with live RLS (would reopen HIST-009 and change every client's auth); an external identity provider.
- **First reversible step:** `load_ride_for()` helper added *alongside* existing checks (no route changed yet) + the static test in warn mode. Blast radius: none until routes migrate. Rollback: delete.
- Cites: SEC-R10-001/-004/-005/-006/-007/-011/-016/-019, HIST-009/-015/-017, drift row 6, ADR-005.
