# Spinr Daily Audit — 2026-04-12
**Grade:** 🔴 ACTION REQUIRED
**Auditor:** Claude Code Daily Audit Agent (claude-sonnet-4-6)
**Duration:** ~6 minutes

## Executive Summary

The CI/CD Pipeline is **failing on main** — the driver-app test job is broken due to a missing module import path (`../../driverStore`) and mock setup errors in `store/__tests__/driverStore.test.ts`. This is the single most important issue today and is blocking any new PR merges from having a green baseline. On a positive note, yesterday's highly productive sprint resolved all 9 PRs including the Node.js critical CVEs, ruff lint errors (1,602 → 0), admin.py monolith split, and trufflehog CI integration. The security posture is significantly improved. No secrets, no float arithmetic issues, and no hardcoded credentials were found today. The audit programme remains complete (all 55 issues closed as of Sprint 9). The #1 action today is fixing the driver-app test failures to restore CI green on main.

---

## Security (Lens 1)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| S1 Secrets scan | ⚠️ WARN | `trufflehog` not in local PATH; CI has it (PR #26). 52 commits in last 24h (mostly branch activity, EOD report). No obvious secret patterns found via git diff pattern scan. | Verify CI trufflehog job passes on next green run |
| S2 Python CVEs | ⚠️ WARN | `pip-audit` not in local PATH. requirements.txt uses pinned versions from `uv pip compile` (PR #24). No obviously vulnerable packages observed. | Run `pip install pip-audit && pip-audit -r backend/requirements.txt` on next dev session |
| S3 Node CVEs | ✅ PASS | `rider-app`: critical=0, high=0, moderate=0. Other surfaces (`driver-app`, `admin-dashboard`, `frontend`) not found in expected paths — app directories are at root level not nested. Prior critical axios CVEs resolved by PR #23. | Confirm driver-app, admin-dashboard, frontend npm audit also clean |
| S4 Pre-commit hook | ✅ PASS | Hook present — `grep -c "spinr pre-commit security check"` returns 1 | No action needed |
| S5 Float arithmetic | ✅ PASS | No float arithmetic on money fields in any recent commits. CQ-009 Decimal migration holds. | No action needed |
| S6 Hardcoded creds | ✅ PASS | No credential patterns found in recent diff meeting threshold criteria | No action needed |

---

## CI/CD Health (Lens 2)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| C1 GitHub Actions | ❌ FAIL | **CI/CD Pipeline failing on main** — 5 consecutive failures (most recent: 2026-04-12T15:09:39Z). Root cause: `driver-app-test` job fails with (1) `Cannot find module '../../driverStore'` in `store/__tests__/driverStore.config.test.ts` and (2) `Cannot read properties of undefined (reading 'mockResolvedValueOnce')` in `store/__tests__/driverStore.test.ts` — mock setup broken | Fix driver-app test import path and mock setup. See Recommended Actions below. |
| C2 Dependabot PRs | ✅ PASS | 0 open Dependabot PRs (previous dep bump PR #d3d7c72d merged) | No action needed |
| C3 Stale PRs | ⚠️ WARN | 2 open PRs: **#22** (11d old — "Claude/upgrade expo 54 o jm v7", stale, likely superseded) and **#27** (2d old — "Add automated testing infrastructure") | Triage PR #22 (close or rebase); drive PR #27 to merge once CI green |
| C4 Stale branches | ⚠️ WARN | Multiple sprint6/*, sprint7/* branches 3d old (completed sprint work). `docs/tech-stack-file-mapping` branch 20h old. These are all merged or superseded. | Run `git push origin --delete sprint6/jwt-refresh sprint6/otp-hardening sprint6/decimal-money sprint7/merge-sprint6-security sprint7/otp-lockout-redis sprint7/migration-runner` (human approval required) |

---

## Code Quality (Lens 3)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| Q1 Backend lint | ⚠️ WARN | `ruff` not in local PATH. As of PR #27 (merged 2026-04-11), backend ruff errors resolved 1,602 → 0. CI lint job validates on each push. | Confirm CI lint job passes on next green run; install ruff locally for dev experience |
| Q2 Type safety | ✅ PASS | No new `Any` type annotations in backend code from recent commits | No action needed |
| Q3 Tech debt markers | ✅ PASS | Zero new TODO/FIXME/HACK/XXX markers introduced in last 24h | No action needed |
| Q4 Test ratio | ⚠️ WARN | Backend: 11 test files / 65 source files (17% ratio). Total test files across all surfaces: 14. Mobile test coverage is thin — PR #27 (testing infra) addresses this. | Drive PR #27 to merge once CI is restored |

---

## Backlog Health (Lens 4)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| B1 P0/P1 issues | ✅ PASS | Audit programme complete — all 55 issues closed as of Sprint 9 (2026-04-09). Final 2 issues: TST-003 (E2E placeholder) and COM-003 (WCAG 2.1 AA) closed in Sprint 9. No P0 or P1 issues are open. | No action needed |
| B2 Alignment issues | ⚠️ WARN | Of 5 critical misalignments: **A1** (JWT_SECRET literal) — startup guard added but literal remains; **A2** (shared wildcard versions) — RESOLVED (now `^11.4.1`, `^0.5.1`); **A3** (frontend/ duplication) — still open; **A4** (discovery bundle ID) — uses `BUNDLE_ID` env var, not hardcoded clash; **A5** (Python version mismatch) — RESOLVED (CI now 3.12). B1 (admin.py monolith) — RESOLVED (PR #30 split into 15-file package). 2/5 critical resolved, 1 partially. | Address A1 (remove literal default) and A3 (resolve frontend/ duplication) in next sprint |
| B3 Docs completeness | ⚠️ WARN | Files 00–12, 14–16 present. **File 13 is missing** from `docs/audit/`. | Identify what file 13 should contain (likely a sprint completion report) and create it |

---

## Upstream Drift (Lens 5)

| Check | Status | Finding | Action |
|-------|--------|---------|--------|
| U1 New upstream commits | ✅ PASS | Fork is 67 commits ahead of upstream; upstream has 0 new commits not in fork. No upstream drift. | No action needed |
| U2 New upstream files | ⚠️ WARN | Upstream has 3 files fork lacks: `backend/routes/admin.py` (superseded by split), `rider-app/bugreport-sdk_gphone64_x86_64-BE4B.251210.005-2026-04-01-21-00-06.zip`, `rider-app/bugreport-sdk_gphone64_x86_64-BE4B.251210.005-2026-04-01-21-01-04.zip`. The zip files are Android bug report dumps — should NOT be in source control. | Do not pull the zip files. Add `*.zip` to `.gitignore`. admin.py is superseded by fork's split. |

---

## Auto-Fixes Applied Today

- **F3:** Daily report written to `docs/audit/daily/2026-04-12.md` ✅

No F1 (pre-commit hook present) or F2 (no new critical CVEs with open GitHub issues) actions required.

---

## Top 3 Recommended Actions for Today

1. 🔴 **Fix driver-app CI failures (BLOCKING)** — Two separate test failures in `driver-app`:
   - `store/__tests__/driverStore.config.test.ts:1`: Change import from `'../../driverStore'` to correct relative path (likely `'../driverStore'` or the store's actual location)
   - `store/__tests__/driverStore.test.ts`: The `mockApi` object is `undefined` — check that `jest.mock('@shared/api/client')` is correctly returning an object with `.post` and `.get` mock functions. Likely needs `jest.mock('@shared/api/client', () => ({ default: { post: jest.fn(), get: jest.fn() } }))`.

2. 🟡 **Triage PR #22** (11 days old — expo 54 upgrade): Determine if this PR is still needed given all the subsequent work. If expo is already at 54 in main, close the PR. If not, rebase or close it.

3. 🟢 **Clean up workspace** — Before starting new work:
   ```bash
   git stash show -p stash@{0}  # review WIP, then: git stash drop or git stash pop
   git stash drop stash@{1}     # drop duplicate
   git add .claude/settings.json .gitignore CLAUDE.md && git commit -m "chore: commit workspace config files"
   ```

---

## Trend Indicators

| Dimension | Trend | Notes |
|-----------|-------|-------|
| Security posture | ↑ **Improving** | axios CVEs resolved; trufflehog in CI; Firebase patched; pre-commit hook intact |
| CI stability | ↓ **Degrading** | 5 consecutive CI/CD Pipeline failures on main as of today — driver-app tests broken |
| Technical debt | ↑ **Improving** | Ruff: 0 errors (was 1,602); admin.py monolith split; docs restored |
| Dependabot backlog | ↑ **Improving** | 0 open Dependabot PRs (was building up) |
| Test coverage | → **Stable** | PR #27 (testing infra) open; mobile tests remain thin |
| Stale PRs | → **Stable** | 2 open (#22 stale, #27 in-progress) |

---

## Questions for the Team

1. **CI is broken on main** — is the driver-app test failure a known issue from the PR #27 testing infrastructure work? If the failing tests were added by PR #27 and that PR is still open, the fix should happen there before merge. If these tests pre-exist, they need an urgent patch.

2. **PR #22 (expo 54)** — should this be closed? Given the major refactoring and dependency updates since it was opened 11 days ago, it likely needs to be either closed or completely rebased.

3. **File `docs/audit/13_*.md`** — what should document 13 contain? The sequence jumps 12→14. This may be a sprint completion report (Sprint gap) that was never written.
