---
context: default
phase: null
status: paused
last_updated: 2026-05-03 9:45pm CST
branch: fix/rider-app-expo-sdk55
latest_commit: 2b630634
---

# Continue Here — 2026-05-03 9:45pm CST

## No Blocking Constraints This Session

All changes this session were config-only and committed cleanly. No methodology failures.

<current_state>
Branch `fix/rider-app-expo-sdk55` is clean and pushed.
Latest commit: `2b630634` (fix: kotlinVersion 2.0.21 → 2.1.20, both app.config.ts)

EAS builds should now be running or queued against this commit. The Kotlin fix is the
last known build blocker — check EAS dashboard to confirm pass/fail.

Admin dashboard .env.local now points to http://localhost:8000 (was Railway production).
Admin portal is ready to run locally — just needs backend started.
</current_state>

<completed_work>

## Build Fixes (all committed, pushed)
- `cd4211d3` — Gradle wrapper 8.10.2 → 8.13 in both apps (AGP 8.12.0 requires ≥ 8.13)
- `2b630634` — kotlinVersion 2.0.21 → 2.1.20 in both app.config.ts files

## Full Build Chain Audit
The 10-build failure sequence is now fully understood and fixed:
| Build # | Root cause | Fixed |
|---|---|---|
| 1–7 | New Architecture enabled | ✅ Disabled |
| 8 | runtimeVersion was dynamic object | ✅ Made literal string |
| 9 | Gradle wrapper 8.10.2 (needs ≥ 8.13) | ✅ Bumped to 8.13 |
| 10 | kotlinVersion 2.0.21 (needs 2.1.20) | ✅ Fixed this session |

## Runtime Failure Audit (all 4 surfaces)
Complete mapping done. Key findings:
- Admin login broken: .env.local pointed to Railway (unknown ADMIN_PASSWORD)
- Firebase files: ALREADY PRESENT (google-services.json in both apps) — not an issue
- .env files: ALREADY EXIST for all surfaces — not an issue
- Twilio: dev mode fallback to OTP "1234" when unconfigured — not a blocker for local testing
- react-native-maps-directions: pure JS, no build risk, works with existing Maps API key

## Admin Dashboard Fix
`admin-dashboard/.env.local` changed:
  BEFORE: BACKEND_URL=https://spinr-backend-production.up.railway.app
  AFTER:  BACKEND_URL=http://localhost:8000
  (file is gitignored — correct)
</completed_work>

<remaining_work>

## Immediate (Tier 1 — do first)
1. Start local backend: `cd backend && python3 -m backend.server`
2. Start admin dashboard: `cd admin-dashboard && npm run dev`
3. Verify login: http://localhost:3000 → admin@spinr.ca / SpinrDev2026!

## After Admin Portal Works (Tier 2)
4. Configure Twilio in admin Settings UI (writes to app_settings Supabase table)
5. Add Firebase service account to backend/.env:
   - Download from: Firebase console → spinrapp-6e464 → Project Settings → Service Accounts
   - Add: FIREBASE_SERVICE_ACCOUNT_JSON=<one-line JSON>
   - Add: FIREBASE_DRIVER_APP_ID=<from driver app google-services.json>
   - Add: FIREBASE_RIDER_APP_ID=1:879808882715:android:150f53e023204c47cb30be
6. Check EAS build status for commit 2b630634 — if passed, download APK and install on device
7. Test OTP login on device (verify OTP freeze bug is resolved by New Arch disable)

## After Device Testing (Tier 2 → Tier 3)
8. Create PR from `fix/rider-app-expo-sdk55` → `main`
   - 14 file diff: withGradleWrapper.js (both apps), app.config.ts changes, yarn.lock,
     mobile-dep-check.yml CI workflow, docs/dependency-upgrade-runbook.md
9. Full ride lifecycle E2E: rider books → driver notified (FCM) → accepts → trip → payment

## Before Production (Phase 5)
- OI-001: Admin a11y — driver card div needs role="button"
- OI-002: Admin a11y — close button needs aria-label
- OI-003 to OI-006: Flip security gates from continue-on-error to blocking
- Sentry alert rule for REFRESH TOKEN REUSE (UI only, no code)
</remaining_work>

<decisions_made>

- kotlinVersion source of truth is `@react-native/gradle-plugin/gradle/libs.versions.toml`
  — always check this file when any Kotlin-related build failure occurs
- Admin dashboard uses local backend for development (localhost:8000), not Railway
  — Railway's ADMIN_PASSWORD is unknown; local .env has SpinrDev2026!
- Both Supabase DB connections (local backend + Railway backend) hit the SAME database
  — no data loss switching between them
- EAS builds are NOT triggered manually — GitHub integration queues them on push
- Tier strategy: admin portal first → mobile OTP → full ride flow
  — each tier is a prerequisite for the next; don't skip ahead
- react-native-maps-directions (v1.9.0, unmaintained) is NOT a build risk
  — pure JS wrapper, Google Maps API key already set, low priority to replace
</decisions_made>

<blockers>

- EAS build on 2b630634: pending / unknown status — check EAS dashboard
  Workaround: none needed, just wait. Kotlin fix is the last known blocker.
- Firebase service account JSON: not in backend/.env
  Workaround: backend starts fine in dev mode; push notifications won't work until added.
- Admin login not yet verified: user needs to run the two start commands above.
</blockers>

## Required Reading (for resuming agent)
1. `.planning/HANDOFF.json` — structured machine-readable state
2. `rider-app/app.config.ts` and `driver-app/app.config.ts` — confirm kotlinVersion is 2.1.20
3. `admin-dashboard/.env.local` — confirm BACKEND_URL is http://localhost:8000
4. EAS dashboard (expo.dev) — check build status for commit 2b630634

## Infrastructure State
- `admin-dashboard/.env.local` → `http://localhost:8000` (was Railway, now local)
- `backend/.env` → complete for local dev; missing FIREBASE_SERVICE_ACCOUNT_JSON
- `rider-app/.env` + `driver-app/.env` → both present, point to Railway production backend
- Local backend: NOT running (user needs to start)
- Admin dashboard: NOT running (user needs to start)
- EAS builds: queued/running on commit 2b630634

## Kotlin Version Reference (pin this for future sessions)
```
Source of truth: node_modules/@react-native/gradle-plugin/gradle/libs.versions.toml
  kotlin = "2.1.20"
  agp    = "8.12.0"

Both apps must have in expo-build-properties android block:
  kotlinVersion: '2.1.20'
  compileSdkVersion: 35
  targetSdkVersion: 35
  minSdkVersion: 25
```

<context>
Two main threads this session:
1. Android build fix: methodically peeled back each EAS build failure layer. The full chain
   is now resolved. Kotlin 2.0.21 → 2.1.20 is the last fix. No more build surprises expected.
   The one remaining risk is @logrocket/react-native (old build.gradle) but it's low probability.

2. Getting the apps actually running: discovered that STATE.md was outdated (.env files DO exist,
   Firebase files DO exist). The real blocker was admin login pointing at Railway with unknown
   password. Fixed by routing to local backend. Admin portal should now work in < 10 minutes.
</context>

<next_action>
Start local backend: `cd backend && python3 -m backend.server`
Then admin dashboard: `cd admin-dashboard && npm run dev`
Then verify admin login at http://localhost:3000 (admin@spinr.ca / SpinrDev2026!)
</next_action>
