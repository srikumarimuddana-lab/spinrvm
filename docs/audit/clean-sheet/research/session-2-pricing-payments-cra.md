# Research Session 2 — Pricing, payments & CRA

*Clean-sheet rebuild audit, research phase. Written 2026-09-25. Report only: no code, config or data was changed. No PII. Fix-oriented wording only; this repo is public. Money examples use made-up amounts only.*

## §0 Method, inputs, deviations and limits

**Brief.** `docs/audit/clean-sheet-prompt/research-sessions.md` §1 (shared method, card format, rules) and §3 (Session 2: scope, questions, guardrails). Guardrails applied to every card: 0% driver commission; surge cap 2.5×; every charge is a disclosed line item; a tax conclusion without a primary source goes to the accountant escalation list, labelled ASSUMED.

**Deviations from §1 (by coordinator instruction, matching Session 3's precedent):**
- **One file, not four.** §1 asks for `current-state.md`, `techniques-radar.md`, `recommendations.md` and `DEFERRED.md` in `research/pricing-payments/`. The coordinator asked for exactly one file at `research/session-2-pricing-payments-cra.md`, structured the way `session-3-trust-safety-fraud.md` is. The four sections map as: §1 = current state, §2 = techniques radar, §3 = build order (recommendations), §6 = deferred / open questions.
- **No lane agents spawned.** This session read the money-domain findings directly (they already exist, current as of 2026-09-24/25, and re-auditing them would duplicate `money-cra.md`, `strategy.md`, `corporate.md`, `admin-ops.md`, `08-hostile-review.md`, `04-blueprint.md` H2) and ran external research directly with WebSearch/WebFetch.
- **No commit, no push, no PR** (coordinator instruction).

**Inputs read, cited, not re-audited** (repo HEAD as of those files' own headers, 2026-09-24/25):
`docs/audit/clean-sheet/02-findings/money-cra.md` (MONEY-006..013, §3.1–3.13 closures, §5 CRA table, §7 Rebuild Delta, §8 top 5/escalations) · `strategy.md` (STRAT-001 revenue-model mismatch, STRAT-002 negative per-ride margin, STRAT-003 "no cut" copy tied to a switched-off booking fee, STRAT-008 broken pricing config) · `corporate.md` (CORP-001 wind-down idempotency gap) · `admin-ops.md` (ADMIN-OPS-001 no dual-approval on money-moving admin actions) · `04-blueprint.md` §2 H2 (double-entry ledger hypothesis) and the Ride Completion & Payments / Driver Earnings & Payouts / Corporate epics · `08-hostile-review.md` §1.7 (CFO persona), U-1/U-7 (money-guard and FLOAT8 corrections), and its §6 re-sequenced roadmap items · `ROADMAP.md` N9/N14/N15/N19/N20/N22/N23, X5/X6/X7/X9/X12/X19, L16 · `05-escalations.md` §B (E-T1..E-T9) · `10-live-checks.md` (VERIFIED-LIVE 2026-09-25 ~03:30 UTC).

**Correction on one brief claim, checked directly against the source.** The brief states `10-live-checks.md` verified live that `rides.distance_fare/time_fare/surge_multiplier` are `DOUBLE PRECISION`. That is not what `10-live-checks.md` says — the file's live Supabase reads cover PST/instant-payout/flag booleans and the migration-tracking gap (§2 "LIVE-001"), not a column-type read of `rides`. The FLOAT8/`DOUBLE PRECISION` claim for those three columns is from `money-cra.md` §3.6, which cites `backend/migrations/08_complete_schema.sql:130-136` — a **static migration-file read, VERIFIED**, not a live `information_schema` read. `08-hostile-review.md` U-7 makes the same distinction: 3 of the 6 FLOAT8 columns money-cra names (`surge_multiplier`, `distance_fare`, `time_fare`) are VERIFIED from that file; the other 3 (`total_fare`, `tip_amount`, `driver_earnings`) are INFERRED from `::text::numeric` casts in SQL rollups, not from a tracked CREATE (the `rides` table has none — it was created by hand in Supabase). This session treats the three as VERIFIED-STATIC and the other three as INFERRED, per that correction, and notes a live `information_schema.columns` read on `rides` as still open (`ROADMAP.md` §9 Q2's owner).

**Live facts used (VERIFIED-LIVE, `10-live-checks.md` §1, will drift):** 0 of 6 service areas have `pst_enabled` true; `instant_payout_enabled` true in 6/6 service areas with no server-side velocity cap (confirmed by grep this session, see §2); `ledger_double_entry_enabled` and `ledger_atomic_settle_enabled` production values were **not** in the live-check batch (still UNKNOWN, `money-cra.md` §3.11); `dual_approval_exports_enabled` off; `incentive_eligibility_enforced` off (cited by Session 3, re-used here since it feeds the incentive-gaming question); the Meta Conversions API token/dataset are both set, so per-ride Purchase events are very likely live (STRAT-004, out of this session's scope except where it touches disclosed line items).

**Evidence labels:** VERIFIED (read in code this session), VERIFIED-STATIC (a migration/schema file read, not a live DB read), VERIFIED-LIVE (from `10-live-checks.md`), INFERRED, ASSUMED, PROPOSED, UNKNOWN. Every external claim carries a URL and the date read (2026-09-25). A CRA/tax conclusion without a primary source fetched **in full** (not a search snippet) is ASSUMED and goes to §5 for the accountant, per the brief's explicit rule — this is a harder bar than Session 3 applied to its non-tax legal claims, and is applied that way throughout §4 below.

**Scale screen.** Spinr is Saskatchewan-first, 0%-commission, two cities, live app testing with real money. A technique that only pays off at Uber/DoorDash scale (real-time ML pricing elasticity models, a hosted enterprise ledger platform, a dedicated risk team) is HOLD unless the smallest version of it is cheap enough to matter now.

**Web access (limit).** WebSearch worked for every query tried (about 16 used). **WebFetch was blocked by the egress proxy for every domain tried this session** — `canada.ca`, `sets.saskatchewan.ca`, `ryan.com`, `c-b-a.ca`, `dominionaudit.com`, `stripe.com` all returned `EGRESS_BLOCKED`, confirming Session 3's finding was not domain-specific (government sites) but a general block on this tool this session. **Zero pages were fetched in full.** Every external claim below rests on a WebSearch result snippet — Google's own summarisation of the page, not the page's text — and is labelled INFERRED at best. Per the brief's explicit, stricter rule for this session ("tax conclusions without a primary source go to the escalation list for an accountant"), **every CRA/SK-tax claim in §2.8 and §4 is ASSUMED and escalated**, even where a snippet and Spinr's own `money-cra.md`/`05-escalations.md` already point the same direction. This is a harder bar than Session 3 applied to its non-tax legal claims (which it labelled INFERRED and did not all escalate); Session 2 follows the brief's stricter instruction because these are the load-bearing GST/PST/T4A conclusions, not adjacent context.

---

## §1 Current state — what the money-domain audit already established (cited, not re-derived)

This section summarises `money-cra.md`, `strategy.md`, `corporate.md`, `admin-ops.md`, `04-blueprint.md` H2, `08-hostile-review.md` and `ROADMAP.md` for readers who have not read them, and adds only what this session independently re-checked in code. Nothing here is a new finding unless marked **New this session**.

### 1.1 The fare/ledger/payment core (VERIFIED, `money-cra.md` §1, §3)

- `calculate_fare` is a pure Decimal function; `platform_share` does not exist as a variable anywhere — 0% commission is structural, not a policy switch (`fare_service.py:184-237`, `money-cra.md` §1.1).
- Surge is clamped at every fare-persisting call site (9 sites catalogued, `money-cra.md` §3.1); corporate and scheduled rides are exempt (§3.2).
- Stripe idempotency is three layers deep (SDK keys embed amount, `claim/unclaim_stripe_event`, DB CAS) and the cumulative-refund RPC is atomic (§3.10, N4).
- Pre-auth holds are sized to exactly the quote (`RIDE_AUTH_BUFFER_CAD = 0.00`); tip/overage is collected by incremental authorization or a second PaymentIntent, never silently dropped (§3.9).
- The ledger (`financial_events`) is single-entry, append-only, and never fails a payment by design (`record_event` never raises) — this is the steelman's #5 and, per H2 below, also the reason it cannot be the money-truth boundary yet.
- Trust-first absorption policy is explicit and documented: uncollected fares, lost disputes and absorbed tips are platform-funded so the driver is always kept whole (`docs/change-log/2026-09-21-uncollected-rides-stay-payable.md`, §1.6 steelman) — the right call for a 0%-commission brand, but **unmeasured** (MONEY-010).

### 1.2 What is broken or dark, by theme (VERIFIED unless noted, all cited from `money-cra.md`)

| Theme | Finding | Cite |
|---|---|---|
| Float money math | Canonical fare-estimate path (`features.py:900`) and the corporate-booking path that persists it do float `round()`; referral bonuses write `_f()` into a NUMERIC column; five instances outside the semgrep allowlist | MONEY-006/-008, §3.12 |
| Re-pricing | Completion-time distance re-pricing keeps the booking-time tax line, so GST on the receipt stops being 5% of the subtotal above it — **live impact likely bounded**: `ADR-016` records `fare_lock_enabled = true` in production since 2026-09-11 (a document claim, VERIFIED as a claim, INFERRED as still-true today) | MONEY-007, `04-blueprint.md` Ride Completion epic |
| Reconciliation | Two 02:00 UTC loops compare mismatched populations (booking-time PI `created` vs settlement-time `ride_completed_at`); a ride whose hold and capture straddle 18:00 CST — Saskatchewan peak commute — is flagged as missing one day and orphaned the next | MONEY-013 |
| Chargebacks | `charge.dispute.created` resolves only `rides` by PaymentIntent; a dispute on a wallet top-up or corporate top-up PI leaves the ledger blind and the credited balance spendable; `corporate_wallet_service.apply_refund` exists with **zero callers** | MONEY-009, `corporate.md` §4 (independently re-confirmed by R6) |
| Payout ceiling | "Payout ≤ collected" is not an invariant — it is deliberately false by trust-first policy — and the resulting exposure (`Σ payouts − Σ collected`) is not measured or capped anywhere | MONEY-010 |
| Instant payout | Live in 6/6 service areas (VERIFIED-LIVE), server endpoint reachable with **no velocity cap or clearing rule** (re-confirmed this session: `grep -n "velocity\|daily_cap" backend/routes/drivers/payouts.py` → 0 hits beyond the fee/kill-switch code) | 08-hostile-review §1.4, ROADMAP N22 |
| Corporate wind-down | The one ledger debit without an idempotency key, guarded only by a non-CAS status flip — a concurrent "Close Account" retry double-debits the ledger against one real Stripe refund on an unreopenable account | CORP-001 |
| Admin controls | Wallet credit/debit and dispute refunds are reachable by a single `finance`/`support`-preset admin with no second approver and no cumulative cap; the export path already has a dark dual-approval mechanism that money actions lack | ADMIN-OPS-001 |
| Receipts/tax | No GST/HST registration number on any receipt or corporate statement; T4A fills both Box 048 and Box 020 for GST-registered drivers (double-reports the same income); PST is off in all 6 service areas against the only evidence found (a PST-46 snippet naming "Taxicab or limousine transportation services … and Other passenger transportation services") | MONEY-011/-012, G9/MONEY-003, `10-live-checks.md` |
| Revenue-model honesty | ToS/PRD describe a business (premium rider features, partner referrals) that has no code; "100% of your fare" is true only because the booking fee is set to $0 live; per-ride contribution margin is negative (STRAT-002's ASSUMED ≈ $0.93/ride Stripe-and-API cost) and unmeasured | STRAT-001/-002/-003 |
| Live pricing config | No minimum fare in effect, no differentiated per-minute rate, identical class multipliers in Regina/Saskatoon — a 1 km ride pays a Regina driver ≈$4 gross before their own costs | STRAT-008 |

### 1.3 The write-site inventory the money guard must start from (VERIFIED, `08-hostile-review.md` U-1, `04-blueprint.md` H2, re-confirmed this session)

`04-blueprint.md` H2's own first draft claimed "every writer passes through `repositories/_base.py`, so the guard sees all." `08-hostile-review.md` refuted this (U-1: **Refuted, VERIFIED**) and the blueprint's changelog corrected it: about 71 direct `.table()` writes bypass `_base.py`, and about 90 `supabase.rpc(` calls carry arguments the guard never sees. This session independently re-read the two named files:

- `backend/repositories/ride_repo.py:264-274` (`update_ride`) — a direct `supabase.table("rides").update(updates).eq("id", ride_id).execute()`, confirmed by reading the function this session (the line numbers in the blueprint/hostile-review, 260/276, land inside `insert_ride`/`update_ride`; both write `rides` directly, bypassing any column-level guard `_base.py` might hold).
- `backend/services/stripe_payout_sync_service.py:453-465` — a direct `supabase.table("payouts").insert(chunk)` / row-by-row fallback insert, confirmed this session; `payouts.amount` is exactly the column MONEY-006's class already broke once (B28/331).

Any money-column write guard (H2's "Now" step) that is scoped only to `repositories/_base.py` gives false assurance on precisely the two tables — `rides` and `payouts` — that carry the fare and the payout amount. This is why the build order below (§3) puts the write-site inventory *before* the guard, matching `04-blueprint.md`'s own corrected "First reversible step."

### 1.4 Cross-references this session used and did not re-derive

`strategy.md` STRAT-001 (revenue-model/ToS mismatch), STRAT-002 (negative per-ride margin, unmeasured), STRAT-003 ("no cut" copy contingent on `booking_fee = 0`), STRAT-008 (broken live pricing config) · `corporate.md` CORP-001 (wind-down idempotency + TOCTOU) · `admin-ops.md` ADMIN-OPS-001 (no dual-approval on money-moving admin actions) · `04-blueprint.md` §2 H2 (double-entry ledger hypothesis: **adopt as end state, reject "only writer" now, adopt-incrementally "ledger as constraint"**) and its Ride Completion & Payments / Driver Earnings & Payouts / Corporate epics · `08-hostile-review.md` §1.7 (CFO persona: pricing memo missing from the top-5, FLOAT8 claim overstated) · `ROADMAP.md` N9/N14/N15/N19/N20/N22/N23 and X5/X6/X7/X9/X12/X19 · `05-escalations.md` §B (E-T1..E-T9, all already ASSUMED there) · `10-live-checks.md` (PST off 6/6, instant payout on 6/6, the two ledger flags still unread live).

---

## §2 Techniques radar

Every card follows the brief's format. Guardrails from the brief are checked explicitly where they bind: **0% driver commission** (G-commission), **surge cap 2.5×** (G-surge), **every charge is a disclosed line item** (G-disclosure). A CRA/tax conclusion is never the payload of a verdict in this section — only the *process* around it is; the substantive tax answer is always routed to §4.

### 2.1 Upfront guaranteed vs metered pricing

### Keep quote-locked metered pricing (fare shown at booking, HMAC token, incremental settlement) — ADOPT (keep)
- What it is (plain language): the rider sees a fare built from disclosed components (base, distance, time, booking fee, area fees, tax) before booking; an HMAC `estimate_token` locks the *shown* surge multiplier to the *charged* one; the final settlement adjusts only distance/time from the actual trip, not the whole pricing model.
- Who uses it / source: this is closer to how metered taxi and most Canadian TNC pricing historically worked than to Uber's 2021+ "Upfront Fares," which prices from trip characteristics **and** an algorithmic estimate of "what a rider is likely willing to pay" and "the lowest pay a driver is likely to accept" — a Columbia Business School study (cited via search) found Uber's revenue share of the fare rose from about 32% to over 42% between 2022 and late 2024 under this model, with trip time/distance/surge explaining only 77% of observed price and pay levels — implying the remaining ~23% is not derivable from the disclosed trip facts ([Uber Under the Hood: Understanding Upfront Fares](https://uberpubpolicy.medium.com/understanding-upfront-fares-7ab69c656101), [coverage of the Columbia study](https://len-sherman.medium.com/will-other-companies-follow-ubers-lead-into-the-black-hole-of-opaque-algorithmic-pricing-d79acd9cfe35), snippets, read 2026-09-25, INFERRED). Uber's own help page confirms the fare **can still change mid-trip** for a driver detour or heavy traffic ([Uber Help: my upfront fare was not honored](https://help.uber.com/en/riders/article/my-upfront-fare-was-not-honored?nodeId=ff65490e-2ffb-41cf-a709-4611521c7b24), snippet, INFERRED) — i.e. even "upfront" fares are not fully guaranteed against route/time changes, so the two models converge more than the marketing suggests.
- Spinr today: quote-locked metered pricing with a signed token exists and is VERIFIED sound (`money-cra.md` §3.1 row 6, §3.9); the gap is not the pricing *model*, it is that completion re-pricing does not recompute tax (MONEY-007) and that a booking fee is switched to $0 so "no cut" is only true by config (STRAT-002/-003).
- Benefit for Spinr at our scale: high — algorithmic upfront pricing needs the demand-forecasting and elasticity-modelling infrastructure Spinr does not have, and CLAUDE.md's own guardrail ("not a hidden-fee operator... every charge maps to a disclosed line item") is close to incompatible with a price built partly from willingness-to-pay rather than disclosed trip facts. Cost/effort: **none** (already built). Risk: none from keeping it.
- How it fails or gets gamed: a rider or driver who disputes "the fare changed" has no visibility into *why* beyond the itemised receipt — this is already the honest version of the failure mode; Uber's version hides the reason behind an opaque algorithm, which is strictly worse for dispute handling, not better.
- Guardrail check: **ADOPT is the disclosure-first choice** (G-disclosure) — a component-based fare a rider can audit against the receipt is what "every charge maps to a disclosed line item" requires; an algorithmic upfront price that a rider cannot decompose would not meet it even if legal.
- First step behind a flag: none needed to keep the model; the improvement is MONEY-007 (recompute tax on re-price), already ranked in ROADMAP X5(b) — cross-referenced here, not re-ranked.

### Algorithmic "upfront" pricing (price from willingness-to-pay signals, not disclosed trip facts) — HOLD
- What it is: set the rider price and driver pay independently and algorithmically, using signals beyond distance/time/surge (rider history, device, time of day elasticity).
- Who uses it / source: Uber's model since 2021, expanding in 2025 (same URLs as above, INFERRED).
- Spinr today: not present, and no code path suggests it (VERIFIED absence — `fare_service.py`'s `calculate_fare` takes only distance, time, surge and configured rates as inputs, no rider-specific term).
- Why HOLD: it directly threatens two "What Spinr Is NOT" guardrails at once — "not a hidden-fee operator" (every charge must map to a disclosed line item; a willingness-to-pay adjustment has no line item) and arguably "not a surge-first product" in spirit (an opaque per-rider multiplier that behaves like unbounded, undisclosed surge). It also needs demand-elasticity data Spinr does not have at two-city scale. Revisit only if Spinr's own leadership decides disclosure-compatible personalization is worth building — and even then, publish the model's inputs, per the CLAUDE.md guardrail.

### Driver-facing per-ride cost-stack disclosure ("this ride cost Spinr $X; you kept $Y") — TRIAL
- What it is: show the driver, per trip, what the platform actually spent (Stripe processing fee, any platform-funded absorption) alongside what they earned — turning the structural 0% into a *felt* 0%.
- Who uses it / source: Lyft ships an earnings summary that shows drivers "how each rider's payment is split between drivers, Lyft, and external fees," and committed in 2024 to a **70%-of-fare weekly floor** with a make-up payment if a driver falls below it ([Lyft: How Lyft Pricing Actually Works](https://www.lyft.com/blog/posts/lets-talk-about-lyft-pricing), [PYMNTS: Lyft guarantees 70%+](https://www.pymnts.com/news/ridesharing/2024/lyft-guarantees-rideshare-drivers-70percent-or-more-of-fare/), snippets, read 2026-09-25, INFERRED) — i.e. a *percentage-take* competitor is now marketing transparency and a floor as a driver-retention feature, which is a lower bar than Spinr's actual 0%.
- Spinr today: not built; `04-blueprint.md`'s Driver Earnings & Payouts epic already proposes exactly this (`driver_trip_receipt` view, cost-stack line) as a "Later" step gated on STRAT-002's Stripe-fee measurement existing first — this session concurs with that sequencing rather than reordering it.
- Benefit for Spinr at our scale: potentially the single most defensible "trust" feature in the whole audit, because Spinr's number is actually 100% (not 70%-and-rising like the competitor above) — but the claim in `04-blueprint.md` that this is a defensible competitive edge is explicitly labelled ASSUMED there (`08-hostile-review.md` U-5/U-6: no driver survey or competitor-disclosure-practice source was fetched). Cost/effort: **M** (needs `stripe_fee` captured per settlement first). Risk: low — it is a display feature over data Spinr already has once the fee column exists.
- How it fails or gets gamed: none directly (display-only); the risk is disclosure fatigue if shown on every screen rather than the trip receipt.
- Guardrail check: strengthens G-commission and G-disclosure simultaneously — the clearest guardrail-aligned technique in this file.
- First step behind a flag: unchanged from `04-blueprint.md` — add a `stripe_fee` column captured at settlement (STRAT-002 prerequisite), then the `driver_trip_receipt` view. Measure success by: the driver survey `08-hostile-review.md` flags as missing evidence — run one before claiming a retention effect.

### 2.2 Double-entry ledger patterns

### Immutable append-only journal with derived balances — ADOPT (destination), REJECT as a near-term "only writer" (endorsing `04-blueprint.md` H2, not re-litigating it)
- What it is (plain language): every money movement is one or more journal *entries* (debit one account, credit another, always balancing to zero); nothing is ever updated or deleted; any account's current balance is a `SUM()` over its entries, not a stored, independently-writable number.
- Who uses it / source: this is the standard pattern in fintech ledger design writing — "the key principle is that every balance is derived from immutable, append-only journal entries rather than stored as a mutable number in a database row," and "enforcing [the debit=credit invariant] at the database transaction layer rather than in application code means no partial failure can produce an unbalanced entry" ([Trio: Payment Ledger Architecture](https://trio.dev/payment-ledger-architecture-fintech/), [Fintechly: Ledger System Design](https://fintechly.com/infrastructure/infrastructure-ledger-system-design/), [SDK.finance: What Is a Double-Entry Ledger](https://sdk.finance/blog/what-is-a-double-entry-ledger-in-fintech/), snippets, read 2026-09-25, INFERRED — general fintech-writing consensus, not one named company's engineering blog, since none of Uber/Lyft/DoorDash/Grab publishes ledger-internals detail that surfaced in search).
- Spinr today: `04-blueprint.md` H2 already answered this exactly: `financial_events` (migration 58) is single-entry and append-only; `financial_event_entries` (the double-entry legs, migration 286) exist as a *derived projection*, correctly shaped, but gated by `ledger_double_entry_enabled` whose production value is **UNKNOWN** and which "assumes off" on any read error (`ledger_service.py:433`, VERIFIED). This is architecturally the right destination and is already the right shape — the gap is that it has never run at production volume.
- Benefit for Spinr at our scale: high, but only once exercised — the entire value of the pattern (an unbalanced entry is structurally impossible) is unrealised while the projection has never been on. Cost/effort: **S** to turn on in staging (the code exists); **M** to make it authoritative later. Risk: low in staging; medium once it gates a live settlement path (H2's own risk rating).
- How it fails or gets gamed: none directly — the risk is process, not the pattern: turning on `ledger_double_entry_enabled` in production before the FLOAT8 columns (§3.6) are fixed lets the projection silently drift from the money it is supposed to mirror, which is worse than not having it.
- First step behind a flag (already the plan; this session does not reorder it): flip `ledger_double_entry_enabled` in **staging only**, run the projection over historical rides, and watch `financial_event_entries_unbalanced_between` (the view `reconciliation.py` already has, §3.3 row m) go and stay green for 7 days before considering production. Measure success by: zero unbalanced-entry rows over the staging replay, and the leg-completeness check (row n) reporting 100% coverage.

### A named chart of accounts (rider receivable, platform cash, driver payable, tax payable, promo expense, dispute reserve) — TRIAL
- What it is: instead of ad hoc `event_type` strings, define a small fixed set of accounts the ledger can move money between, so every settlement is expressible as "debit X, credit Y" and a query like "how much do we owe drivers right now" becomes one account balance instead of a 4-table join.
- Who uses it / source: standard double-entry accounting practice; the same fintech-design sources above describe "chart of accounts" as the schema-design step that makes the invariant meaningful (INFERRED, general accounting practice, not company-specific).
- Spinr today: not present — `financial_events.event_type` is a free-form enum used descriptively, not as an account reference (INFERRED from the schema description in `money-cra.md` §3.11).
- Benefit for Spinr at our scale: this is what `04-blueprint.md` H2's "Next" step (atomic header RPCs per settlement type) needs as a prerequisite design artifact, and it is exactly the item `04-blueprint.md` itself flags: "accountant for the chart of accounts (tax payable, promo expense, dispute reserve) before Next." Cost/effort: **S** (a design document + a lookup table), but it needs the accountant, not just engineering. Risk: low — additive.
- How it fails or gets gamed: an incomplete chart of accounts (missing "dispute reserve" or "tax payable") forces a later migration once the gap is found in production, which is exactly what CORP-001 (missing idempotency on the wind-down debit) and MONEY-009 (top-up disputes with no ledger row) already show happening informally.
- First step: draft the chart of accounts alongside the accountant conversation in §4 (E-T7's "how is platform-funded absorption booked" question answers half of it directly). No code before the draft exists.

### Full event-sourced rebuild of the ride/money domain — HOLD (endorsing H1's and H2's verdicts, not re-litigating)
- What it is: replace `rides.status` and `rides.*` money columns entirely with a stream of immutable events that both state and money are projected from.
- Why HOLD: `04-blueprint.md` H1 already rejected the equivalent move for ride state ("not an event-sourced rebuild") and H2 already rejected "ledger-first, rides-as-read-model" as anything but a Rewrite-only end state, citing that 44 loops and ~20 readers compute money from `rides.*` today. This session's independent read of `08-hostile-review.md`'s U-11 (a ~9-month calendar cost for migrating just the ~13 ride-status write sites, serially) makes the money-column equivalent at least as large. Revisit only as part of a genuine rewrite, never as an incremental step on the live product.

### 2.3 Daily reconciliation against Stripe

### Reconcile against Stripe's own Payout/Balance report types, keyed on settlement, not creation — ADOPT
- What it is: instead of home-built queries that compare Stripe's `created` timestamp to Spinr's `ride_completed_at`, use Stripe's own **Payout reconciliation report** (built for platforms on automatic payouts, groups every transaction into the payout batch it settled in) or the **Balance report** (built to be read like a bank statement, one row per balance-affecting event) as the source of truth for "what actually moved," and match against `financial_events` keyed on the *same* event (capture / balance-transaction), not the booking-time authorization.
- Who uses it / source: "The Balance and Payout reconciliation reports both provide downloadable transaction history including custom metadata... The Payout reconciliation report is optimized for users on automatic payout plans who model their Stripe balance as a temporary clearing account"; "for each Stripe payout, create one deposit record totaling the amount that reached the bank... record gross charges as positive lines, with fees, refunds, and disputes entered as reductions"; "a best practice is to retrieve payouts automatically and asynchronously when the `payout.paid` or `payout.reconciliation_completed` event is received" ([Stripe: Payout reconciliation report type](https://docs.stripe.com/reports/report-types/payout-reconciliation), [Stripe: How to select a report](https://docs.stripe.com/reports/select-a-report), snippets, read 2026-09-25, INFERRED).
- Spinr today: MONEY-013 (VERIFIED / INFERRED as executed) already diagnosed the exact defect this recommendation fixes — population mismatch from keying on `created` instead of settlement, and summing `pi.amount` (authorized) instead of `amount_received` (captured). The recommended fix in `money-cra.md` §2 MONEY-013 ("key both sides on the capture date, compare `amount_received` not `amount`, filter by `metadata.scope`") is the same direction this technique points, independently arrived at from Stripe's own reporting-report design rather than from reading Spinr's bug. This session adds the concrete mechanism: use `payout.reconciliation_completed`/`payout.paid` webhooks (both already in `_STRIPE_HANDLED_EVENTS`, `money-cra.md` §3.5) as the trigger for the daily job instead of (or alongside) a blind midnight-UTC cron, since payouts settle on Stripe's own schedule, not Spinr's.
- Benefit for Spinr at our scale: directly closes MONEY-013 with a mechanism validated by Stripe's own documented design intent, not just Spinr's own bug report. Cost/effort: **S–M** (already ranked as `reconcile_v2` in ROADMAP X5(e); this session does not re-rank it, only adds the reporting-API detail). Risk: low, additive, dual-write for a week per the existing plan.
- How it fails or gets gamed: a report-API outage delays the reconciliation window; keep the existing `financial_events`-based check as a fallback rather than replacing it outright.
- First step: cross-referenced to ROADMAP X5(e) (`reconcile_v2`, both versions write for a week) — this session's addition is to also read the Payout Reconciliation Report via the Stripe API (or the existing webhook events, which is cheaper) as the ground truth for "what settled," not just Spinr's own PI list.

### Daily automatic payouts (platform-to-bank) as a reconciliation aid — ASSESS
- What it is: Stripe payout schedule set to daily rather than weekly, so each day's reconciliation batch stays small enough to review by hand if the automated job ever needs a human check.
- Who uses it / source: "Daily automatic payouts keep each batch small enough to review in a few minutes... weekly payouts group much more activity into a single settlement, which makes it harder to work through when something doesn't match" ([Stripe: How to select a report](https://docs.stripe.com/reports/select-a-report), snippet, INFERRED).
- Spinr today: Spinr's own payout to *drivers* runs through Stripe Connect transfers (separate from Spinr's own bank payout schedule, which this session did not find configured in the repo — it is a Stripe Dashboard/account setting, UNKNOWN whether daily or weekly); do not conflate the two. `docs/finance/stripe-payout-readiness.md` §7 records "$415.79 balance, 1 payout ever" (cited via `strategy.md` STRAT-002), suggesting Spinr-to-bank payouts are not yet a routine operational rhythm.
- Benefit: small at current volume; grows as ride volume grows and the daily reconciliation job (above) becomes something a human actually reads. Cost/effort: **S** (a dashboard setting, not code). Risk: none.
- First step: confirm the current Stripe account payout schedule (an OPS lane fact, not an engineering one) and set it to daily once volume justifies a human spot-check rhythm; not urgent at $415.79/lifetime volume.

### 2.4 Payment reliability

### Keep the existing three-layer Stripe idempotency and incremental-authorization tip pattern — ADOPT (keep; already best-in-class)
- What it is: the hold equals exactly the quoted fare (no buffer); tip/overage is collected via `increment_authorization` when the card supports it, else a second PaymentIntent; capture is capped at the authorized amount so a fare that grows at completion is charged as capture-plus-overflow, never silently dropped.
- Who uses it / source: this is precisely Stripe's documented pattern for exactly this use case — "Incremental authorization allows you to increase the authorized amount on a confirmed PaymentIntent before you capture it. This is particularly relevant for ride-hailing scenarios where the final charge amount may not be known when the ride begins... incremental authorization requires 3D Secure (3DS) for the initial authorization, [but] subsequent incremental authorizations on this payment is considered merchant-initiated, potentially exempting any additional SCA" ([Stripe: Increment an authorization](https://docs.stripe.com/payments/incremental-authorization), snippet, read 2026-09-25, INFERRED).
- Spinr today: VERIFIED (`money-cra.md` §3.9) — `RIDE_AUTH_BUFFER_CAD = Decimal("0.00")`, `payment_service._settle_against_hold` implements exactly this increment-else-second-PI shape.
- Benefit for Spinr at our scale: Spinr independently arrived at the vendor-recommended pattern; no change needed. Cost/effort: **none**. Risk: none from keeping it.
- How it fails or gets gamed: the zero-buffer design means a card that does **not** support incremental authorization and later declines the second, tip-only PaymentIntent leaves the tip uncollected — already the documented, deliberate trust-first policy (driver is still credited; MONEY-010's absorption class). Not a defect, a named trade-off.
- Guardrail check: keeping the hold at exactly the quote (no padding) is itself a disclosure-friendly choice — the rider's card is never reserved for more than the number they were shown (G-disclosure).

### 3D Secure and its interaction with incremental/second-PI collection after trip completion — ASSESS
- What it is: confirm the initial authorization requires 3DS where the card/issuer demands it (SCA), and that the incremental-authorization or second-PI step correctly relies on the "merchant-initiated" exemption rather than blocking on a second customer-present 3DS challenge mid-trip or post-trip, when the rider is not available to respond.
- Who uses it / source: same Stripe documentation as above — the exemption is real but conditional on the *initial* PI's 3DS having succeeded (INFERRED, snippet).
- Spinr today: `money-cra.md` scenario 28 (card declined/3DS required after trip) is marked **Handled** for the primary flow (ask the rider to change card in-app when off-session settlement needs authentication) — this session did not find a specific check of whether the incremental-auth call path correctly claims the merchant-initiated exemption versus triggering a fresh, undeliverable 3DS challenge on a post-trip tip. That is a UNKNOWN, not a finding, since it requires reading `payment_service._settle_against_hold`'s SCA handling in detail, which was out of this session's time budget.
- Benefit: closing the UNKNOWN prevents a class of "driver waits for a tip that technically failed silently" bugs. Cost/effort: **S** (a focused code read + one test with a 3DS-required test card). Risk: low to check, but the failure mode if unhandled is user-facing (a rider gets an unexpected re-authentication prompt after the trip has ended).
- First step: a `spinr-money-auditor` pass specifically on `_settle_against_hold`'s SCA branch, with a Stripe test card that requires 3DS on every transaction ([Stripe: Test card requiring 3D Secure authentication for every transaction](https://support.stripe.com/questions/test-card-requiring-3d-secure-authentication-for-every-transaction), snippet, INFERRED) exercised against the incremental-auth and second-PI paths in a staging PaymentIntent flow. Measure success by: zero SCA-blocked tip collections in a synthetic test suite.

### Stripe Account Updater (automatic card-on-file refresh) — TRIAL (confirm enabled; ops-only, not a code change)
- What it is: Stripe silently refreshes expired/reissued card numbers on file with the issuing network, so a driver-pass or corporate-subscription card does not simply start failing the month a card expires.
- Who uses it / source: "Stripe works with card networks to automatically update expired or replaced card details when banks issue new cards, running silently in the background... Stripe Account Updater is included free in most Stripe plans as of 2026, with no per-update or per-card fee... About 30% of failed subscription payments are caused by the card on file changing" ([Rebounce: Stripe Account Updater and Card Updater Guide](https://www.rebounce.dev/blog/stripe-account-updater-card-updater-guide), [Churn Buster: Stripe Dunning Management](https://churnbuster.io/articles/stripe-dunning), snippets, read 2026-09-25, INFERRED).
- Spinr today: **UNKNOWN** — this is a Stripe Dashboard-level setting, not a code integration point (`grep -rn "card_updater\|account_updater" backend/` → 0 hits this session, as expected since there is nothing to grep for). It matters most for `driver_subscriptions` (Spinr Pass) and `corporate_subscriptions` — recurring charges, not one-off ride payments.
- Benefit for Spinr at our scale: meaningful only once Spinr Pass / corporate subscriptions have real recurring volume (both are currently dark/unpriced per STRAT-010 and STRAT-012) — cheap to confirm now regardless. Cost/effort: **S** (a console check, OPS lane, not MONEY lane). Risk: none.
- First step: an OPS-lane task, not a code PR — confirm in the Stripe Dashboard whether Account Updater is enabled for the account, record the answer, and treat this as done once confirmed (no flag needed).

### Retry backoff and CAS-guarded retry counters — ADOPT (keep; already best-in-class)
- What it is: bounded retries (3 attempts) with a per-attempt idempotency key embedding the amount, and a DB compare-and-swap on the retry counter so two replicas cannot both submit the same attempt.
- Who uses it / source: matches Stripe's own Smart Retries philosophy in spirit (retry with a bounded budget, not forever) though Spinr's is deterministic/scheduled rather than ML-timed — "Smart Retries use data points to find the best time to retry the payment" ([Stripe: Automate payment retries](https://docs.stripe.com/billing/revenue-recovery/smart-retries), snippet, INFERRED) is a *subscriptions* feature (Stripe Billing), not applicable to Spinr's one-off ride PaymentIntents the same way.
- Spinr today: VERIFIED (`money-cra.md` §3.10) — `payment_retry.py`, `MAX_RETRIES = 3`, per-attempt key, Redis leader lock.
- Benefit: none needed for ride payments; **TRIAL** Stripe Billing's Smart Retries specifically for `driver_subscriptions`/`corporate_subscriptions` once those have real volume, since that is what the feature is built for (subscription dunning, not one-off charges). Cost/effort: **S** (a Billing-product feature flag, not custom code) once subscriptions are priced and live.
- First step: revisit only after STRAT-010/STRAT-012 (corporate go-to-market, driver payout-schedule decision) are resolved — no code before then.
